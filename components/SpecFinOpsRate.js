// Code generated by tools/jsxbuild from SpecFinOpsRate.jsx. DO NOT EDIT.
(function() {
  const PlaceholderBadge = window.PlaceholderBadge;
  const RATE_CSS = `
.specfin-rate { display:flex; flex-direction:column; gap:15px;
  --specfin-rate-od: var(--neon-cyan);
  --specfin-rate-cm: var(--neon-pink);
  --specfin-rate-ink: var(--neon-cyan); }
/* MEASURED, not judged. --neon-cyan is 11.8:1 on the dark panel and 3.14:1 on
   the white one; --neon-pink is 3.0:1 there. Fine for a 2px mark, thin for a
   36px numeral and far too thin for a 10px label. --specfin-rate-ink is the
   darkened form and is the ONLY thing type is ever tinted with.
   THE :not() CHAIN IS LOAD-BEARING: styles.css declares data-aesthetic terminal
   and outrun AFTER data-theme="light", at equal specificity, and both remap the
   palette back to dark. A light rule keyed on data-theme alone therefore fires
   on a dark panel and inverts figure and ground. That has shipped here once. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-rate {
  --specfin-rate-ink: color-mix(in oklab, var(--neon-cyan) 58%, #000); }

.specfin-rate-ctl { display:flex; flex-wrap:wrap; align-items:center; gap:12px; }
.specfin-rate-seg { display:inline-flex; flex-wrap:wrap; gap:3px; padding:3px;
  background:var(--bg); border:1px solid var(--line); border-radius:999px; }
.specfin-rate-seg button { appearance:none; border:0; cursor:pointer; padding:7px 15px; border-radius:999px;
  background:none; font-family:var(--font-mono); font-size:10px; letter-spacing:.1em; text-transform:uppercase;
  color:var(--text-mute); transition:background .2s ease, color .2s ease; }
.specfin-rate-seg button:hover { color:var(--text); }
.specfin-rate-seg button[aria-pressed="true"] { background:var(--specfin-rate-od); color:var(--bg); }
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-rate-seg button[aria-pressed="true"] {
  background:color-mix(in oklab, var(--neon-cyan) 84%, #000); color:#fff; }
.specfin-rate-seg button:focus-visible { outline:2px solid var(--specfin-rate-od); outline-offset:2px; }
/* --text-mute and never --text-faint: faint measures 2.16:1 on the dark panel
   and 2.67:1 on the light one, and this line is the instruction for the control
   beside it. */
.specfin-rate-hint { font-family:var(--font-mono); font-size:9.5px; letter-spacing:.1em;
  text-transform:uppercase; color:var(--text-mute); }

.specfin-rate-readout { display:flex; flex-wrap:wrap; align-items:center; gap:12px 22px;
  padding:14px 17px; background:var(--bg); border:1px solid var(--line);
  border-left:2px solid var(--specfin-rate-cm); border-radius:var(--rad-sm); }
.specfin-rate-main { display:flex; align-items:baseline; gap:10px; }
.specfin-rate-n { font-family:var(--font-mono); font-size:36px; font-weight:500; line-height:1;
  letter-spacing:-.02em; color:var(--specfin-rate-ink);
  text-shadow:0 0 22px color-mix(in oklab, var(--neon-cyan) 30%, transparent); }
[data-theme="light"] .specfin-rate-n { text-shadow:none; }
@media print { .specfin-rate-n { text-shadow:none !important; } }
.specfin-rate-unit { font-family:var(--font-mono); font-size:10.5px; letter-spacing:.09em;
  text-transform:uppercase; color:var(--text-mute); }
.specfin-rate-eq { font-family:var(--font-mono); font-size:11.5px; color:var(--text-dim); }
.specfin-rate-pill { display:inline-flex; align-items:baseline; gap:8px; padding:5px 12px; border-radius:999px;
  border:1px solid var(--line); font-family:var(--font-mono); font-size:12.5px; color:var(--text-dim); }
.specfin-rate-pill span { font-size:9.5px; letter-spacing:.08em; text-transform:uppercase; color:var(--text-mute); }
.specfin-rate-badge { margin-left:auto; }

.specfin-rate-svg { display:block; width:100%; height:auto; }
.specfin-rate-scroll { overflow-x:auto; }
/* The labels in these plots are 7\u20138.5px mono. Below ~760px they keep a 1:1
   floor and the figure scrolls sideways inside its own wrapper, so the page
   body never scrolls horizontally and an 8px label stays an 8px label. */
@media (max-width:780px) { .specfin-rate-svg { min-width:740px; } }
.specfin-rate-scrollhint { display:none; }
@media (max-width:780px) {
  .specfin-rate-scrollhint { display:block; margin:0; text-align:right;
    font-family:var(--font-mono); font-size:9px; letter-spacing:.1em;
    text-transform:uppercase; color:var(--text-mute); }
}

/* The tier table is the legend, the selector AND the table view the chart owes
   a reader who cannot use it as a picture. One component, three jobs, so they
   cannot disagree with each other. */
.specfin-rate-table { display:flex; flex-direction:column; gap:2px; }
.specfin-rate-th, .specfin-rate-tr { display:grid;
  grid-template-columns:14px minmax(0,2.1fr) .85fr .7fr .95fr .95fr; gap:6px 10px; align-items:center;
  padding:7px 10px; font-family:var(--font-mono); font-size:10.5px; }
.specfin-rate-th { color:var(--text-mute); font-size:8.5px; letter-spacing:.08em; text-transform:uppercase;
  border-bottom:1px solid var(--line); }
.specfin-rate-tr { appearance:none; border:1px solid transparent; border-radius:var(--rad-sm);
  background:none; cursor:pointer; color:var(--text-dim); text-align:left; width:100%; }
.specfin-rate-tr:hover { background:var(--bg); color:var(--text); }
.specfin-rate-tr[aria-pressed="true"] { background:var(--bg); border-color:var(--line);
  border-left:2px solid var(--specfin-rate-cm); color:var(--text); }
.specfin-rate-tr:focus-visible { outline:2px solid var(--specfin-rate-od); outline-offset:2px; }
.specfin-rate-sw { width:12px; height:3px; border-radius:2px; background:var(--specfin-rate-cm); }
.specfin-rate-nm { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }
.specfin-rate-nm b { font-weight:500; color:var(--text); }
.specfin-rate-num { text-align:right; font-variant-numeric:tabular-nums; }
.specfin-rate-num.is-key { color:var(--text); }
@media (max-width:640px) {
  .specfin-rate-th { display:none; }
  .specfin-rate-tr { grid-template-columns:14px minmax(0,1fr) auto; row-gap:2px; }
  .specfin-rate-tr .specfin-rate-num { grid-column:2 / -1; text-align:left; font-size:9.5px; color:var(--text-mute); }
}

/* The two permanent frame annotations. They are not footnotes and they are not
   a disclosure triangle: the chart is attackable without them, so they are
   always on screen, at the same weight as the caption, inside the frame. */
.specfin-rate-frame { display:grid; grid-template-columns:repeat(auto-fit, minmax(300px,1fr)); gap:14px;
  padding-top:13px; border-top:1px solid var(--line); }
.specfin-rate-note { margin:0; padding-left:11px; border-left:2px solid var(--line);
  font-family:var(--font-mono); font-size:10.5px; line-height:1.75; color:var(--text-mute); }
.specfin-rate-note b { display:block; margin-bottom:4px; font-weight:500; font-size:8.5px;
  letter-spacing:.11em; text-transform:uppercase; color:var(--text-dim); }
.specfin-rate-note em { font-style:normal; color:var(--text-dim); }

.specfin-rate-cap { margin:0; max-width:82ch; padding-left:12px;
  border-left:2px solid var(--specfin-rate-cm);
  font-size:13.5px; line-height:1.7; color:var(--text-dim); }
.specfin-rate-cap strong { font-weight:600; color:var(--text); }
.specfin-rate-foot { margin:0; max-width:84ch; font-family:var(--font-mono); font-size:10.5px;
  line-height:1.75; color:var(--text-mute); }
.specfin-rate-foot em { font-style:normal; color:var(--text-dim); }
.specfin-rate-link { appearance:none; border:0; padding:0; background:none; cursor:pointer; font:inherit;
  color:var(--text); border-bottom:1px solid var(--specfin-rate-cm); }
.specfin-rate-link:focus-visible { outline:2px solid var(--specfin-rate-od); outline-offset:2px; }

/* --- the pool cloud ---------------------------------------------------- */
/* State is one class on the wrapper and everything below is a transition off
   it, so both ends of every transition come from one cascade. Base opacity
   lives here rather than in a presentation attribute for the same reason. */
.specfin-rate .pool { fill:var(--specfin-rate-cm); opacity:.5; transition:opacity .4s ease, fill .4s ease; }
.specfin-rate.is-picked .pool { opacity:.1; }
/* r, not just fill: under data-aesthetic="terminal" --neon-pink and --neon-cyan
   resolve to the SAME hex (#72f1b8), so a selection carried by colour alone
   disappears there entirely. Size and opacity are the redundant channels, and
   they work in every aesthetic. SVG geometry properties are settable from CSS in
   every browser this site supports; where they are not, opacity still carries it. */
.specfin-rate.is-picked .pool.is-on { fill:var(--specfin-rate-od); opacity:1; r:3.4px; }
/* The light theme needs more alpha for the same ratio on white. Scoped
   :not(.is-on) deliberately \u2014 an unscoped rule TIES on specificity with the
   .is-on rule above and, being later in source order, silently wins. That
   exact tie has shipped in this codebase before. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-rate .pool:not(.is-on) { opacity:.8; }
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-rate.is-picked .pool:not(.is-on) { opacity:.14; }
.specfin-rate .lasso { opacity:0; transition:opacity .45s ease .1s; }
.specfin-rate.is-lasso .lasso { opacity:1; }
.specfin-rate .oneoff { opacity:0; transition:opacity .35s ease .1s; }
.specfin-rate.is-oneoff .oneoff { opacity:1; }

.specfin-rate-bands { display:flex; flex-wrap:wrap; gap:4px 18px;
  font-family:var(--font-mono); font-size:10px; color:var(--text-mute); }
.specfin-rate-bands b { font-weight:500; color:var(--text-dim); }

@media (max-width:640px) {
  .specfin-rate-n { font-size:28px; }
  .specfin-rate-readout { gap:10px 14px; }
  .specfin-rate-badge { margin-left:0; }
}
@media (prefers-reduced-motion: reduce) { .specfin-rate * { transition:none !important; } }
`;
  const near = (n, k) => Math.round(n * k + 1e-9) / k;
  const fmt0 = (n) => String(near(n, 1)).replace(/\B(?=(\d{3})+(?!\d))/g, ",");
  const fmt1 = (n) => near(n, 10).toFixed(1);
  const fmt2 = (n) => near(n, 100).toFixed(2);
  const usd = (n) => "$" + fmt0(n);
  const OD_RATE = 0.192;
  const HRS_YEAR = 8760;
  const WEEK = 168;
  const WPY = HRS_YEAR / WEEK;
  const TIERS = [
    { id: "csp1nu", family: "Compute Savings Plan", term: "1 yr", pay: "no upfront", rate: 0.141 },
    { id: "esp3au", family: "EC2 Instance Savings Plan", term: "3 yr", pay: "all upfront", rate: 0.072 }
  ].map((t) => {
    const annual = t.rate * HRS_YEAR;
    const d = 1 - t.rate / OD_RATE;
    return {
      id: t.id,
      family: t.family,
      term: t.term,
      pay: t.pay,
      rate: t.rate,
      annual,
      d,
      /* u* = 1 − d, and h* is that share of a 168-hour week. This is the whole
         derivation: r_c·168 = r_od·h ⇒ h = 168·(r_c/r_od) = 168·(1 − d). */
      uStar: 1 - d,
      hStar: WEEK * (1 - d)
    };
  });
  const DEEPEST = TIERS[TIERS.length - 1];
  const SHALLOW = TIERS[0];
  const ramp = (i) => TIERS.length > 1 ? i / (TIERS.length - 1) : 1;
  const OD_YEAR = OD_RATE * HRS_YEAR;
  const odAnnual = (h) => OD_RATE * h * WPY;
  const BAND = { lo: 10, hi: 20 };
  const BAND_W = BAND.hi - BAND.lo;
  const GAP_NEAR = DEEPEST.hStar - BAND.hi;
  const GAP_FAR = SHALLOW.hStar - BAND.hi;
  const RATE_SOURCE = "Published us-east-1 Linux shared-tenancy rates for one m5.xlarge. On demand is $0.192/hour \u2014 corroborated against Vantage's public instance table. The 3-year all-upfront EC2 Instance Savings Plan discount of 62.5% is AWS's own published figure. The 1-year no-upfront Compute Savings Plan rate of $0.141/hour is the weakest link here: it matches third-party listings but AWS publishes no public Savings Plans rate card, so treat it as corroborated rather than retrieved. Two commitment tiers are drawn because two are what can be sourced: the shallowest and the deepest. Intermediate rows are deliberately LEFT OUT rather than filled in with plausible numbers \u2014 this is the one chart here whose arithmetic you are invited to redo instead of trust, and an illustrative rate in the middle of it would forfeit exactly that. Every dollar drawn is a rate from that card multiplied by the 8,760 hours a one-year term contains; nothing is fitted, smoothed or estimated, and the horizontals are flat because that is what a commitment does, not because the chart was simplified. The algebra is one inequality and you can redo it in your head with your own rate card: commit only if u > 1 \u2212 d, where u is the share of the term you will actually run the workload and d is the discount. It is a division.";
  const RATE_LABEL = "A line chart. The horizontal axis is hours per week the workload actually needs to be running, from zero to one hundred and sixty-eight, with ticks at forty-five hours \u2014 nine to five, five days \u2014 sixty hours, sixty-three hours, and one hundred and sixty-eight, which is around the clock. The vertical axis is the annual cost of one m5.xlarge in us-east-1, from zero to eighteen hundred dollars. One rising diagonal starts at the origin: on-demand pricing with the instance switched off when idle, reaching one thousand six hundred and eighty-two dollars at one hundred and sixty-eight hours. Two flat horizontal lines cross it, one per commitment tier: one thousand two hundred and thirty-five dollars a year for a one-year no-upfront Compute Savings Plan, and six hundred and thirty-one dollars for a three-year all-upfront EC2 Instance Savings Plan. They are flat because a commitment bills every hour of its term whether or not it is consumed. Each line is marked where it crosses the diagonal: at one hundred and twenty-three and at sixty-three hours a week respectively. A shaded vertical band at ten to twenty hours a week is labelled: a dev or integration tier, honestly used. Between the right edge of that band and the nearest crossing there is forty-three hours a week of empty chart. A shaded region to the right of the last crossing marks where the diagonal has risen above every horizontal \u2014 a genuine round-the-clock baseline, where committing is correct.";
  const RW = 760, RH = 352;
  const PX = 96, PR = 684, PY = 44, PB = 300;
  const PW = PR - PX, PH = PB - PY;
  const YMAX = 1800;
  const rx = (h) => PX + h / WEEK * PW;
  const ry = (v) => PB - v / YMAX * PH;
  const XT = [
    { h: 0, t: "0", s: "off" },
    { h: 45, t: "45", s: "9\xD75" },
    { h: 60, t: "60", s: "12\xD75" },
    { h: 63, t: "63", s: "9h \xD7 7" },
    { h: 112, t: "112", s: "16\xD77" },
    { h: 168, t: "168", s: "24/7" }
  ];
  const YT = [0, 400, 800, 1200, 1600];
  const BreakevenVisual = () => {
    const [pick, setPick] = React.useState(DEEPEST.id);
    const sel = TIERS.find((t) => t.id === pick) || DEEPEST;
    const odAtBand = odAnnual(BAND.hi);
    const multiple = sel.annual / odAtBand;
    const atFull = sel.annual / OD_YEAR;
    const gapNear = sel.hStar - BAND.hi;
    return /* @__PURE__ */ React.createElement(
      VizFrame,
      {
        title: "A commitment is a flat line",
        note: "A Savings Plan is not a cheaper rate on the hours you use. It is an hourly spend commitment that bills every hour of its term whether or not anything consumed it \u2014 so on this axis it is a horizontal, and the horizontal does not move when you switch the workload off. The only question the chart asks is where it crosses the line that does.",
        source: RATE_SOURCE
      },
      /* @__PURE__ */ React.createElement("style", null, RATE_CSS),
      /* @__PURE__ */ React.createElement("div", { className: "specfin-rate" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-ctl" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-seg", role: "group", "aria-label": "Commitment tier" }, TIERS.map((t) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: t.id,
          type: "button",
          "aria-pressed": t.id === pick,
          onClick: () => setPick(t.id)
        },
        t.term.replace(" ", "") + " " + (t.family.charAt(0) === "C" ? "compute" : "instance") + " \xB7 " + fmt1(t.d * 100) + "%"
      ))), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-hint" }, "Pick a tier. The number that moves is where it starts paying.")), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-readout", "aria-live": "polite" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-main" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-n" }, fmt1(sel.hStar)), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-unit" }, "hours a week to break even")), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-eq" }, "u* = 1 \u2212 d = " + fmt1(sel.uStar * 100) + " % of 168 h"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-eq" }, usd(sel.annual) + "/yr floor \xB7 " + fmt1(sel.d * 100) + " % off " + usd(OD_YEAR)), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-pill" }, fmt2(multiple), "\xD7", /* @__PURE__ */ React.createElement("span", null, "on demand at " + BAND.hi + " h/wk"))), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-scrollhint", "aria-hidden": "true" }, "Scroll the chart sideways \u2192"), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-scroll" }, /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "specfin-rate-svg",
          viewBox: "0 0 " + RW + " " + RH,
          xmlns: "http://www.w3.org/2000/svg",
          role: "img",
          "aria-label": RATE_LABEL
        },
        /* @__PURE__ */ React.createElement("text", { x: "10", y: "20", fontFamily: "JetBrains Mono", fontSize: "8.5", letterSpacing: "1.3", fill: "var(--text-dim)" }, "ANNUAL COST OF ONE m5.xlarge \xB7 us-east-1 \xB7 LINUX"),
        /* @__PURE__ */ React.createElement(
          "text",
          {
            x: RW - 10,
            y: "20",
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            letterSpacing: "1.3",
            fill: "var(--text-dim)"
          },
          "X \u2014 HOURS A WEEK THE WORKLOAD ACTUALLY NEEDS TO BE UP"
        ),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: rx(SHALLOW.hStar),
            y: PY,
            width: PR - rx(SHALLOW.hStar),
            height: PH,
            fill: "var(--neon-pink)",
            opacity: ".07"
          }
        ), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + rx(SHALLOW.hStar) + " " + PY + " V" + PB,
            fill: "none",
            stroke: "var(--specfin-rate-cm)",
            strokeWidth: "1",
            strokeDasharray: "3 3",
            opacity: ".7"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(SHALLOW.hStar) + 7,
            y: PB - 67,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: "1",
            fill: "var(--text-dim)"
          },
          "A GENUINE 24/7 BASELINE \u2014 COMMIT HERE"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(SHALLOW.hStar) + 7,
            y: PB - 56,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: ".8",
            fill: "var(--text-mute)"
          },
          "EVERY TIER DRAWN HERE BEATS ON DEMAND"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(SHALLOW.hStar) + 7,
            y: PB - 45,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: ".8",
            fill: "var(--text-mute)"
          },
          "AT u = 100 % THE DEEPEST TIER COSTS"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(SHALLOW.hStar) + 7,
            y: PB - 34,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: ".8",
            fill: "var(--text-mute)"
          },
          fmt2(DEEPEST.annual / OD_YEAR) + "\xD7 THE ON-DEMAND BILL"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: rx(BAND.lo),
            y: PY,
            width: rx(BAND.hi) - rx(BAND.lo),
            height: PH,
            fill: "var(--bg-2)",
            fillOpacity: ".92",
            stroke: "var(--text-mute)",
            strokeWidth: "1",
            strokeDasharray: "4 4"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(BAND.hi) + 7,
            y: PY + 13,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: "1",
            fill: "var(--text-dim)"
          },
          BAND.lo + "\u2013" + BAND.hi + " H/WK \u2014 A DEV OR INTEGRATION"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(BAND.hi) + 7,
            y: PY + 23,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: "1",
            fill: "var(--text-dim)"
          },
          "TIER, HONESTLY USED"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, YT.map((v) => /* @__PURE__ */ React.createElement("g", { key: v }, /* @__PURE__ */ React.createElement(
          "line",
          {
            x1: PX,
            x2: PR,
            y1: ry(v),
            y2: ry(v),
            stroke: "var(--line-soft)",
            strokeWidth: "1",
            strokeDasharray: "2 3"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: PX - 8,
            y: ry(v) + 3,
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "8",
            fill: "var(--text-dim)"
          },
          usd(v)
        ))), /* @__PURE__ */ React.createElement("line", { x1: PX, x2: PR, y1: PB, y2: PB, stroke: "var(--line)", strokeWidth: "1" }), /* @__PURE__ */ React.createElement("line", { x1: PX, x2: PX, y1: PY, y2: PB, stroke: "var(--line)", strokeWidth: "1" }), XT.map((t, i) => /* @__PURE__ */ React.createElement("g", { key: t.h }, /* @__PURE__ */ React.createElement("line", { x1: rx(t.h), x2: rx(t.h), y1: PB, y2: PB + 4, stroke: "var(--line)", strokeWidth: "1" }), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(t.h),
            y: PB + (i % 2 ? 25 : 14),
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: "var(--text-dim)"
          },
          t.t
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(t.h),
            y: PB + (i % 2 ? 34 : 23),
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "6.6",
            letterSpacing: ".6",
            fill: "var(--text-mute)"
          },
          t.s
        )))),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, TIERS.map((t, i) => {
          const on = t.id === sel.id;
          return /* @__PURE__ */ React.createElement("g", { key: t.id }, /* @__PURE__ */ React.createElement(
            "line",
            {
              x1: PX,
              x2: PR,
              y1: ry(t.annual),
              y2: ry(t.annual),
              stroke: "var(--specfin-rate-cm)",
              strokeWidth: on ? 2.4 : 1.3,
              strokeOpacity: on ? 1 : 0.34 + ramp(i) * 0.4
            }
          ), /* @__PURE__ */ React.createElement(
            "text",
            {
              x: PR + 6,
              y: ry(t.annual) + 3,
              fontFamily: "JetBrains Mono",
              fontSize: "7.5",
              fill: on ? "var(--text)" : "var(--text-dim)"
            },
            usd(t.annual)
          ));
        })),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "line",
          {
            x1: rx(0),
            y1: ry(0),
            x2: rx(WEEK),
            y2: ry(OD_YEAR),
            stroke: "var(--neon-cyan)",
            strokeWidth: "2",
            strokeLinecap: "round"
          }
        ), /* @__PURE__ */ React.createElement("circle", { cx: rx(WEEK), cy: ry(OD_YEAR), r: "3", fill: "var(--neon-cyan)" }), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: PR - 4,
            y: ry(OD_YEAR) - 8,
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".9",
            fill: "var(--text-dim)"
          },
          "ON DEMAND, SHUT OFF WHEN IDLE \u2014 " + usd(OD_YEAR) + " AT 24/7"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, TIERS.map((t) => {
          const on = t.id === sel.id;
          return /* @__PURE__ */ React.createElement("g", { key: "x" + t.id }, /* @__PURE__ */ React.createElement(
            "circle",
            {
              cx: rx(t.hStar),
              cy: ry(t.annual),
              r: on ? 4.6 : 2.8,
              fill: "var(--bg-1)",
              stroke: "var(--specfin-rate-cm)",
              strokeWidth: on ? 2 : 1.2
            }
          ), /* @__PURE__ */ React.createElement(
            "text",
            {
              x: rx(t.hStar),
              y: ry(t.annual) - 9,
              textAnchor: "middle",
              fontFamily: "JetBrains Mono",
              fontSize: on ? "8.5" : "7.5",
              fill: on ? "var(--text)" : "var(--text-dim)"
            },
            fmt1(t.hStar) + " h/wk"
          ));
        })),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + rx(BAND.hi) + " " + PY + " V" + PB,
            fill: "none",
            stroke: "var(--text-mute)",
            strokeWidth: "1",
            strokeDasharray: "2 4",
            opacity: ".8"
          }
        ), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + rx(sel.hStar) + " " + ry(sel.annual) + " V" + (PB - 8),
            fill: "none",
            stroke: "var(--specfin-rate-cm)",
            strokeWidth: "1",
            strokeDasharray: "2 4"
          }
        ), /* @__PURE__ */ React.createElement(
          "line",
          {
            x1: rx(BAND.hi),
            x2: rx(sel.hStar),
            y1: PB - 14,
            y2: PB - 14,
            stroke: "var(--text-dim)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement("path", { d: "M" + (rx(BAND.hi) + 6) + " " + (PB - 17.5) + " L" + rx(BAND.hi) + " " + (PB - 14) + " L" + (rx(BAND.hi) + 6) + " " + (PB - 10.5) + " Z", fill: "var(--text-dim)" }), /* @__PURE__ */ React.createElement("path", { d: "M" + (rx(sel.hStar) - 6) + " " + (PB - 17.5) + " L" + rx(sel.hStar) + " " + (PB - 14) + " L" + (rx(sel.hStar) - 6) + " " + (PB - 10.5) + " Z", fill: "var(--text-dim)" }), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: (rx(BAND.hi) + rx(sel.hStar)) / 2 - 118,
            y: PB - 32,
            width: "236",
            height: "12",
            fill: "var(--bg-1)",
            opacity: ".94"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: (rx(BAND.hi) + rx(sel.hStar)) / 2,
            y: PB - 23,
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".8",
            fill: "var(--text)"
          },
          fmt1(gapNear) + " H/WK OF EMPTY ROAD \xB7 " + fmt1(gapNear / BAND_W) + "\xD7 THE BAND'S OWN WIDTH"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement("circle", { cx: rx(BAND.hi), cy: ry(odAtBand), r: "3", fill: "var(--neon-cyan)" }), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + rx(BAND.hi) + " " + ry(odAtBand) + " V" + ry(sel.annual),
            fill: "none",
            stroke: "var(--text-dim)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + (rx(BAND.hi) - 3.5) + " " + ry(odAtBand) + " H" + (rx(BAND.hi) + 3.5),
            stroke: "var(--text-dim)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + (rx(BAND.hi) - 3.5) + " " + ry(sel.annual) + " H" + (rx(BAND.hi) + 3.5),
            stroke: "var(--text-dim)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: rx(BAND.hi) + 4,
            y: (ry(odAtBand) + ry(sel.annual)) / 2 - 8,
            width: "86",
            height: "25",
            fill: "var(--bg-1)",
            opacity: ".94"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(BAND.hi) + 7,
            y: (ry(odAtBand) + ry(sel.annual)) / 2 + 3,
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: "var(--text)"
          },
          fmt2(multiple) + "\xD7"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: rx(BAND.hi) + 7,
            y: (ry(odAtBand) + ry(sel.annual)) / 2 + 13,
            fontFamily: "JetBrains Mono",
            fontSize: "6.6",
            letterSpacing: ".7",
            fill: "var(--text-mute)"
          },
          usd(sel.annual) + " vs " + usd(odAtBand)
        )),
        /* @__PURE__ */ React.createElement(
          "text",
          {
            x: PX,
            y: RH - 6,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: "1",
            fill: "var(--text-dim)"
          },
          "COMMIT ONLY IF u > 1 \u2212 d   \xB7   u = SHARE OF THE TERM ACTUALLY RUN   \xB7   d = DISCOUNT   \xB7   EVERY CROSSING ABOVE IS THAT DIVISION"
        )
      )), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-table" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-th", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Commitment"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, "$/hr"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, "Off"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, "$/yr floor"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, "Breaks even")), TIERS.map((t, i) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: t.id,
          type: "button",
          className: "specfin-rate-tr",
          "aria-pressed": t.id === sel.id,
          onClick: () => setPick(t.id)
        },
        /* @__PURE__ */ React.createElement(
          "span",
          {
            className: "specfin-rate-sw",
            "aria-hidden": "true",
            style: { opacity: t.id === sel.id ? 1 : 0.34 + ramp(i) * 0.4 }
          }
        ),
        /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-nm" }, /* @__PURE__ */ React.createElement("b", null, t.family), " \xB7 " + t.term + " \xB7 " + t.pay),
        /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, "$" + t.rate.toFixed(3)),
        /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, fmt1(t.d * 100) + " %"),
        /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num" }, usd(t.annual)),
        /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-num is-key" }, fmt1(t.hStar) + " h/wk")
      ))), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-cap" }, sel.family + ", " + sel.term + ", " + sel.pay + " \u2014 " + fmt1(sel.d * 100) + " % off, which is a real discount \u2014 ", /* @__PURE__ */ React.createElement("strong", null, "breaks even at " + fmt1(sel.hStar) + " h/wk"), ": " + fmt1(sel.hStar / 7) + " hours every single day, weekends included. At " + BAND.hi + " h/wk it costs ", /* @__PURE__ */ React.createElement("strong", null, fmt2(multiple) + "\xD7 more"), " than paying on demand and switching it off \u2014 " + usd(sel.annual) + " against " + usd(odAtBand) + ". Nothing about the discount is in dispute; what is in dispute is what the discount is applied to. You are not buying a cheaper rate. You are buying a floor, and the floor does not move when you stop using the thing."), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-frame" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-note" }, /* @__PURE__ */ React.createElement("b", null, "This is a sizing decision, not a verdict on Savings Plans"), "A Savings Plan is an hourly spend commitment that ", /* @__PURE__ */ React.createElement("em", null, "floats across the billing family"), " \u2014 it settles against whatever eligible compute is running, so an unused hour on this instance is only wasted if the account had no other eligible instance running in it. That makes this chart the ", /* @__PURE__ */ React.createElement("em", null, "marginal"), " question: how many hours of baseline to commit, not whether to commit at all. Size the commitment to the floor you would run anyway and the geometry above is on your side. A Reserved Instance is the harder case \u2014 it is scoped to an instance type and it genuinely strands."), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-note" }, /* @__PURE__ */ React.createElement("b", null, "Everything left of a crossing depends on the shutdown working"), "The diagonal is what on-demand costs ", /* @__PURE__ */ React.createElement("em", null, "if the thing is actually off"), " when nobody needs it. That is a claim about a mechanism, not about a price list. A scheduler that fails open \u2014 a missed tag, a Lambda that errors quietly, a lease nobody reclaimed \u2014 lands the workload at 168 h/wk, the far right of this chart, which is the worst cell on it: full on-demand rate, full time, and the commitment still billing underneath. The shutdown automation is not an optimisation on top of the rate decision; it is the precondition for it.")), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-foot" }, "Read the whole chart, not one crossing. Between the band and the nearest crossing there is", " ", /* @__PURE__ */ React.createElement("em", null, fmt1(GAP_NEAR) + " h/wk"), " of empty road \u2014 ", fmt1(GAP_NEAR / BAND_W), "\xD7 the band's own width \u2014 and to the furthest crossing, ", /* @__PURE__ */ React.createElement("em", null, fmt1(GAP_FAR) + " h/wk"), ", or", " ", fmt1(GAP_FAR / BAND_W), "\xD7 it. That interval is the entire finding, and it is a property of the geometry rather than of any estate: a floor cannot follow usage down. To the right of", " ", /* @__PURE__ */ React.createElement("em", null, fmt1(SHALLOW.hStar) + " h/wk"), " the geometry reverses and every tier on the card wins \u2014 at u = 100 % the deepest one costs ", fmt2(DEEPEST.annual / OD_YEAR), "\xD7 the on-demand bill, which is a ", fmt1(DEEPEST.d * 100), " % saving and a large one. Commit the baseline. Do not commit the part that should be switched off."))
    );
  };
  const N_POOLS = 1145;
  const MEDIAN = 63;
  const HIGH_PCT = 41;
  const MKT = 90;
  const M5_SAVE = 68;
  const BANDS = [
    { id: "b0", label: "< 5 %", share: 0.22, off: -6 },
    { id: "b1", label: "5\u201310 %", share: 0.14, off: -2 },
    { id: "b2", label: "10\u201315 %", share: 0.12, off: 1 },
    { id: "b3", label: "15\u201320 %", share: 0.11, off: 3 },
    { id: "b4", label: "> 20 %", share: HIGH_PCT / 100, off: 4, high: true }
  ];
  const QP = [0, 0.05, 0.15, 0.3, 0.5, 0.7, 0.85, 0.95, 1];
  const QV = [30, 42, 50, 57, 63, 69, 75, 82, 93];
  const quant = (p) => {
    if (p <= 0) return QV[0];
    if (p >= 1) return QV[QV.length - 1];
    for (let i = 1; i < QP.length; i++) {
      if (p <= QP[i]) {
        const f = (p - QP[i - 1]) / (QP[i] - QP[i - 1]);
        return QV[i - 1] + f * (QV[i] - QV[i - 1]);
      }
    }
    return QV[QV.length - 1];
  };
  const realPools = () => {
    const list = window.SITE_DATA && window.SITE_DATA.specializations || [];
    const fin = list.filter((s) => s && s.id === "finops")[0];
    const rows = fin && fin.spot && fin.spot.pools;
    if (!Array.isArray(rows) || !rows.length) return null;
    const ok = rows.filter((r) => r && typeof r.savings === "number" && typeof r.band === "number" && r.band >= 0 && r.band < BANDS.length);
    return ok.length === rows.length ? ok : null;
  };
  const buildCloud = () => {
    const counts = BANDS.map((b) => Math.round(b.share * N_POOLS));
    let total = counts.reduce((a, n) => a + n, 0);
    counts[counts.length - 1] += N_POOLS - total;
    const pts = [];
    BANDS.forEach((b, bi) => {
      const n = counts[bi];
      for (let j = 0; j < n; j++) {
        pts.push({ band: bi, sav: quant((j + 0.5) / n) + b.off, i: pts.length });
      }
    });
    const med = (arr) => {
      const s = arr.map((p) => p.sav).sort((a, b) => a - b);
      const m = s.length >> 1;
      return s.length % 2 ? s[m] : (s[m - 1] + s[m]) / 2;
    };
    for (let k = 0; k < 8; k++) {
      const delta = MEDIAN - med(pts);
      if (Math.abs(delta) < 1e-3) break;
      pts.forEach((p) => {
        p.sav = p.sav + delta;
      });
    }
    pts.forEach((p) => {
      p.sav = Math.max(18, Math.min(95, p.sav));
    });
    return { pts, counts, real: false };
  };
  const CLOUD = (function() {
    const real = realPools();
    if (real) {
      const counts = BANDS.map(() => 0);
      const pts = real.map((r, i) => {
        counts[r.band]++;
        return { band: r.band, sav: r.savings, i, meta: r };
      });
      return { pts, counts, real: true };
    }
    return buildCloud();
  })();
  const N_MKT = CLOUD.pts.filter((p) => p.sav >= MKT).length;
  const N_HIGH = CLOUD.counts[BANDS.length - 1];
  const ONEOFF = (function() {
    let best = CLOUD.pts[0], bd = 1e9;
    CLOUD.pts.forEach((p) => {
      const d = Math.abs(p.sav - M5_SAVE);
      if (d < bd) {
        bd = d;
        best = p;
      }
    });
    return best;
  })();
  const NODEPOOL_N = 54;
  const NODEPOOL = (function() {
    const set = {};
    const cand = CLOUD.pts.filter((p) => p.sav >= 45);
    const step = Math.max(1, Math.floor(cand.length / NODEPOOL_N));
    for (let i = 0; i < cand.length && Object.keys(set).length < NODEPOOL_N; i += step) set[cand[i].i] = 1;
    return set;
  })();
  const NODEPOOL_PTS = CLOUD.pts.filter((p) => NODEPOOL[p.i]);
  const NODEPOOL_MED = (function() {
    const s = NODEPOOL_PTS.map((p) => p.sav).sort((a, b) => a - b);
    if (!s.length) return MEDIAN;
    const m = s.length >> 1;
    return s.length % 2 ? s[m] : (s[m - 1] + s[m]) / 2;
  })();
  const SW = 760, SH = 372;
  const SX = 104, SR = 700, SY = 46, SB = 300;
  const SPW = SR - SX, SPH = SB - SY;
  const XLO = 20, XHI = 95;
  const sx = (v) => SX + (v - XLO) / (XHI - XLO) * SPW;
  const rowH = SPH / BANDS.length;
  const rowY = (b) => SY + b * rowH;
  const hash01 = (n) => {
    let h = (n ^ 2654435769) >>> 0;
    h = Math.imul(h ^ h >>> 15, 2246822507) >>> 0;
    h = Math.imul(h ^ h >>> 13, 3266489909) >>> 0;
    return ((h ^ h >>> 16) >>> 0) / 4294967296;
  };
  CLOUD.pts.forEach((pt) => {
    const dx = CLOUD.real ? 0 : (hash01(pt.i + 9973) - 0.5) * 0.9;
    pt.px = sx(pt.sav + dx);
    pt.py = rowY(pt.band) + 8 + hash01(pt.i) * (rowH - 16);
  });
  const hull = (pts) => {
    if (pts.length < 3) return null;
    const p = pts.slice().sort((a, b) => a.x - b.x || a.y - b.y);
    const cross = (o, a, b) => (a.x - o.x) * (b.y - o.y) - (a.y - o.y) * (b.x - o.x);
    const lower = [];
    for (let i = 0; i < p.length; i++) {
      while (lower.length >= 2 && cross(lower[lower.length - 2], lower[lower.length - 1], p[i]) <= 0) lower.pop();
      lower.push(p[i]);
    }
    const upper = [];
    for (let i = p.length - 1; i >= 0; i--) {
      while (upper.length >= 2 && cross(upper[upper.length - 2], upper[upper.length - 1], p[i]) <= 0) upper.pop();
      upper.push(p[i]);
    }
    return lower.slice(0, -1).concat(upper.slice(0, -1));
  };
  const lassoPath = (pts, pad) => {
    const h = hull(pts);
    if (!h) return "";
    const cx = h.reduce((a, q) => a + q.x, 0) / h.length;
    const cy = h.reduce((a, q) => a + q.y, 0) / h.length;
    const out = h.map((q) => {
      const dx = q.x - cx, dy = q.y - cy;
      const len = Math.sqrt(dx * dx + dy * dy) || 1;
      return { x: q.x + dx / len * pad, y: q.y + dy / len * pad };
    });
    const mid = (a, b) => ({ x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 });
    let d = "";
    for (let i = 0; i < out.length; i++) {
      const cur = out[i], nxt = out[(i + 1) % out.length];
      const m = mid(cur, nxt);
      d += (i === 0 ? "M" + mid(out[out.length - 1], cur).x + " " + mid(out[out.length - 1], cur).y : "") + " Q" + cur.x + " " + cur.y + " " + m.x + " " + m.y;
    }
    return d + " Z";
  };
  const SPOT_SOURCE_BASE = "AWS Spot Instance Advisor \u2014 us-east-1, Linux \u2014 accessed 2026-08-07. " + fmt0(N_POOLS) + " pools, one per instance type \xD7 availability zone. ";
  const SPOT_SOURCE_RECON = SPOT_SOURCE_BASE + "Four figures on this chart are the Advisor's and are the chart's only claims about the world: the pool count, the MEDIAN saving of " + MEDIAN + " %, the " + HIGH_PCT + " % of pools in the >20 % monthly interruption band, and m5.xlarge at " + M5_SAVE + " %. The position of each individual dot is not one of them. The cloud is a stratified reconstruction that reproduces exactly those three aggregates \u2014 the count, the median and the high-interruption share \u2014 and nothing finer; the split of the remaining pools across the four lower bands, and the savings figure attached to any single unnamed dot, are properties of the reconstruction. It is deterministic and the curve that generates it is written out in this file. The argument is the SHAPE \u2014 a median and a spread, against one marketing number \u2014 and the shape is the published part. Drop a real per-pool table into the record and this figure renders it instead, and this paragraph shortens itself.";
  const SPOT_SOURCE_REAL = SPOT_SOURCE_BASE + "Every dot is one pool, at the Advisor's own published saving and interruption band. Median " + MEDIAN + " %; " + HIGH_PCT + " % of pools in the >20 % band; m5.xlarge " + M5_SAVE + " %.";
  const SPOT_LABEL = "A scatter plot of " + fmt0(N_POOLS) + " dots, one per us-east-1 Linux spot pool \u2014 an instance type in one availability zone. The horizontal axis is the saving against on-demand, from twenty to ninety-five per cent. The vertical axis is the pool's monthly interruption-rate band, in five rows: under five per cent at the top, then five to ten, ten to fifteen, fifteen to twenty, and over twenty per cent at the bottom. The dots form a wide cloud on both axes. Two vertical rules cross it. The first, solid, is the measured median saving at sixty-three per cent, and it falls near the middle of the cloud. The second, dashed and far to the right, is the marketing figure of up to ninety per cent; " + fmt0(N_MKT) + " of the " + fmt0(N_POOLS) + " pools reach it. The bottom row, over twenty per cent interruption, is shaded and holds " + HIGH_PCT + " per cent of the dots \u2014 " + fmt0(N_HIGH) + " pools.";
  const MODES = [
    { id: "all", label: "The whole region" },
    { id: "one", label: "One-off EC2 \xB7 m5.xlarge \xB7 us-east-1a" },
    { id: "pool", label: "A Karpenter NodePool, diversified" }
  ];
  const SpotPoolCloud = () => {
    const [mode, setMode] = React.useState("all");
    const picked = mode !== "all";
    const cls = "specfin-rate" + (picked ? " is-picked" : "") + (mode === "one" ? " is-oneoff" : "") + (mode === "pool" ? " is-lasso" : "");
    const lasso = lassoPath(
      NODEPOOL_PTS.map((p) => ({ x: p.px, y: p.py })),
      15
    );
    return /* @__PURE__ */ React.createElement(
      VizFrame,
      {
        title: "The pool cloud \u2014 " + fmt0(N_POOLS) + " spot pools, and one marketing number",
        note: "Spot is a rate lever, and the honest way to describe a rate lever is a distribution rather than a headline. Every dot is one pool: an instance type in one availability zone, with the two things the Spot Instance Advisor publishes about it \u2014 what it saves, and how often it goes away. Disclosing the second is what makes the first usable.",
        source: CLOUD.real ? SPOT_SOURCE_REAL : SPOT_SOURCE_RECON
      },
      /* @__PURE__ */ React.createElement("style", null, RATE_CSS),
      /* @__PURE__ */ React.createElement("div", { className: cls }, /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-ctl" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-seg", role: "group", "aria-label": "What you are buying" }, MODES.map((m) => /* @__PURE__ */ React.createElement("button", { key: m.id, type: "button", "aria-pressed": mode === m.id, onClick: () => setMode(m.id) }, m.label))), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-hint" }, "The dispersion is the argument. Pick what you would actually run.")), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-readout", "aria-live": "polite" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-main" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-n" }, MEDIAN + " %"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-unit" }, "median saving, measured")), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-eq" }, "\u201Cup to " + MKT + " %\u201D \xB7 reached by " + fmt0(N_MKT) + " of " + fmt0(N_POOLS) + " pools"), /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-pill" }, HIGH_PCT + " %", /* @__PURE__ */ React.createElement("span", null, "interrupt > 20 %/mo \xB7 " + fmt0(N_HIGH) + " pools")), !CLOUD.real && /* @__PURE__ */ React.createElement("span", { className: "specfin-rate-badge" }, /* @__PURE__ */ React.createElement(
        PlaceholderBadge,
        {
          label: "Dot-level: reconstructed",
          title: "The pool count, the median, the >20 % share and m5.xlarge's figure are the Spot Instance Advisor's, accessed 2026-08-07. The position of each individual dot is not: the cloud is a deterministic reconstruction that reproduces those aggregates exactly and nothing finer. See the source line under this figure."
        }
      ))), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-scrollhint", "aria-hidden": "true" }, "Scroll the chart sideways \u2192"), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-scroll" }, /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "specfin-rate-svg",
          viewBox: "0 0 " + SW + " " + SH,
          xmlns: "http://www.w3.org/2000/svg",
          role: "img",
          "aria-label": SPOT_LABEL
        },
        /* @__PURE__ */ React.createElement("text", { x: "10", y: "20", fontFamily: "JetBrains Mono", fontSize: "8.5", letterSpacing: "1.3", fill: "var(--text-dim)" }, "ONE DOT = ONE SPOT POOL \xB7 INSTANCE TYPE \xD7 AZ \xB7 us-east-1 \xB7 LINUX"),
        /* @__PURE__ */ React.createElement(
          "text",
          {
            x: SW - 10,
            y: "20",
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            letterSpacing: "1.3",
            fill: "var(--text-dim)"
          },
          "X \u2014 SAVING vs ON DEMAND"
        ),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: SX,
            y: rowY(BANDS.length - 1),
            width: SPW,
            height: rowH,
            fill: "var(--neon-pink)",
            opacity: ".08"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: SR - 352,
            y: rowY(BANDS.length - 1) + rowH - 16,
            width: "346",
            height: "13",
            rx: "2",
            fill: "var(--bg-1)",
            opacity: ".93"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: SR - 6,
            y: rowY(BANDS.length - 1) + rowH - 6,
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".9",
            fill: "var(--text)"
          },
          HIGH_PCT + " % OF ALL POOLS \xB7 INTERRUPTED MORE THAN ONCE IN FIVE, MONTHLY"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, BANDS.map((b, i) => /* @__PURE__ */ React.createElement("g", { key: b.id }, /* @__PURE__ */ React.createElement(
          "line",
          {
            x1: SX,
            x2: SR,
            y1: rowY(i),
            y2: rowY(i),
            stroke: "var(--line-soft)",
            strokeWidth: "1",
            strokeDasharray: "2 3"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: SX - 10,
            y: rowY(i) + rowH / 2 + 3,
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: b.high ? "var(--text)" : "var(--text-dim)"
          },
          b.label
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: SX - 10,
            y: rowY(i) + rowH / 2 + 13,
            textAnchor: "end",
            fontFamily: "JetBrains Mono",
            fontSize: "6.6",
            fill: "var(--text-mute)"
          },
          fmt0(CLOUD.counts[i]) + " pools"
        ))), /* @__PURE__ */ React.createElement("line", { x1: SX, x2: SR, y1: SB, y2: SB, stroke: "var(--line)", strokeWidth: "1" }), /* @__PURE__ */ React.createElement("line", { x1: SX, x2: SX, y1: SY, y2: SB, stroke: "var(--line)", strokeWidth: "1" }), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: "14",
            y: SY - 10,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: ".9",
            fill: "var(--text-dim)"
          },
          "Y \u2014 INTERRUPTION / MO"
        ), [30, 45, 60, 75, 90].map((v) => /* @__PURE__ */ React.createElement("g", { key: v }, /* @__PURE__ */ React.createElement("line", { x1: sx(v), x2: sx(v), y1: SB, y2: SB + 4, stroke: "var(--line)", strokeWidth: "1" }), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: sx(v),
            y: SB + 15,
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: "var(--text-dim)"
          },
          v + " %"
        )))),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, CLOUD.pts.map((p) => /* @__PURE__ */ React.createElement(
          "circle",
          {
            key: p.i,
            className: "pool" + (mode === "one" && p.i === ONEOFF.i ? " is-on" : "") + (mode === "pool" && NODEPOOL[p.i] ? " is-on" : ""),
            cx: p.px,
            cy: p.py,
            r: "1.9"
          }
        ))),
        /* @__PURE__ */ React.createElement("g", { className: "lasso", pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "path",
          {
            d: lasso,
            fill: "var(--neon-cyan)",
            fillOpacity: ".07",
            stroke: "var(--neon-cyan)",
            strokeWidth: "1.6",
            strokeDasharray: "7 5"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: SX + 8,
            y: SY - 10,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".9",
            fill: "var(--text-dim)"
          },
          fmt0(NODEPOOL_PTS.length) + " POOLS, ACROSS FAMILIES AND ZONES"
        )),
        /* @__PURE__ */ React.createElement("g", { className: "oneoff", pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "circle",
          {
            cx: ONEOFF.px,
            cy: ONEOFF.py,
            r: "9",
            fill: "none",
            stroke: "var(--neon-cyan)",
            strokeWidth: "1.4"
          }
        ), /* @__PURE__ */ React.createElement("circle", { cx: ONEOFF.px, cy: ONEOFF.py, r: "4", fill: "var(--neon-cyan)" }), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + ONEOFF.px + " " + (ONEOFF.py - 9) + " V" + (SY - 22),
            fill: "none",
            stroke: "var(--neon-cyan)",
            strokeWidth: "1",
            strokeDasharray: "3 3"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: ONEOFF.px,
            y: SY - 26,
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".9",
            fill: "var(--text-dim)"
          },
          "m5.xlarge \xB7 us-east-1a \u2014 ONE POOL, " + M5_SAVE + " % OFF"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "line",
          {
            x1: sx(MKT),
            x2: sx(MKT),
            y1: SY - 4,
            y2: SB,
            stroke: "var(--text-mute)",
            strokeWidth: "1.2",
            strokeDasharray: "5 4"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: sx(MKT) - 96,
            y: SB + 24,
            width: "150",
            height: "26",
            rx: "3",
            fill: "var(--bg-1)",
            stroke: "var(--line)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: sx(MKT) - 88,
            y: SB + 36,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".9",
            fill: "var(--text-dim)"
          },
          "AWS MARKETING \u2014 \u201CUP TO " + MKT + " %\u201D"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: sx(MKT) - 88,
            y: SB + 46,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: ".8",
            fill: "var(--text-mute)"
          },
          fmt0(N_MKT) + " OF " + fmt0(N_POOLS) + " POOLS REACH IT"
        ), /* @__PURE__ */ React.createElement(
          "line",
          {
            x1: sx(MEDIAN),
            x2: sx(MEDIAN),
            y1: SY - 4,
            y2: SB,
            stroke: "var(--text)",
            strokeWidth: "1.6"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: sx(MEDIAN) - 68,
            y: SB + 24,
            width: "140",
            height: "26",
            rx: "3",
            fill: "var(--bg-1)",
            stroke: "var(--text)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: sx(MEDIAN) - 60,
            y: SB + 36,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: ".9",
            fill: "var(--text)"
          },
          "MEASURED MEDIAN \u2014 " + MEDIAN + " %"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: sx(MEDIAN) - 60,
            y: SB + 46,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: ".8",
            fill: "var(--text-mute)"
          },
          "WHERE YOU WOULD ACTUALLY STAND"
        )),
        /* @__PURE__ */ React.createElement(
          "text",
          {
            x: "14",
            y: SH - 6,
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: "1",
            fill: "var(--text-dim)"
          },
          "THE CLOUD IS WIDE ON BOTH AXES \xB7 THAT DISPERSION IS THE ARGUMENT, NOT THE CAVEAT"
        )
      )), /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-bands" }, BANDS.map((b, i) => /* @__PURE__ */ React.createElement("span", { key: b.id }, /* @__PURE__ */ React.createElement("b", null, b.label), " \xB7 " + fmt0(CLOUD.counts[i]) + " pools \xB7 " + Math.round(CLOUD.counts[i] / N_POOLS * 100) + " %"))), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-cap" }, mode === "one" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("strong", null, "One pool."), " m5.xlarge in us-east-1a: " + M5_SAVE + " % below on-demand" + (CLOUD.real ? ", " + BANDS[ONEOFF.band].label + " interruption." : ". Its interruption band is the Advisor's second field; the row it is drawn in here comes from the reconstruction, not from a lookup.") + " That saving is real and it is better than the median. The problem is not the number, it is the denominator: when this pool goes dark \u2014 and every pool on this chart goes dark sometimes \u2014 the workload goes dark, because there is nothing else on the chart holding it up. A hand-placed spot instance buys the rate and skips the mechanism."), mode === "pool" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("strong", null, fmt0(NODEPOOL_PTS.length) + " pools."), " A NodePool that names families and zones rather than an instance type stands on all of them at once, at a median of " + fmt1(NODEPOOL_MED) + " % \u2014 the region's own middle, which is the number you should plan against and is nothing like \u201Cup to " + MKT + " %\u201D. Any one of them going dark is a reschedule onto the rest, not an outage. What makes that true is unglamorous and is exactly what a one-off setup skips: an SQS interruption queue and the EventBridge rules that feed it, so the two-minute warning becomes a drain and a replacement instead of a surprise. Diversification is not a discount strategy. It is what converts a distribution into a rate you are allowed to quote."), mode === "all" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("strong", null, "\u201CUp to " + MKT + " %\u201D is one dot at the edge of a cloud."), " " + fmt0(N_MKT) + " of " + fmt0(N_POOLS) + " pools reach it. The median is " + MEDIAN + " %, and the median is where you would actually stand. Then read the other axis: " + HIGH_PCT + " % of these pools \u2014 " + fmt0(N_HIGH) + " of them \u2014 interrupt more than one time in five every month. Both facts are the same fact. The cloud is wide on both axes, and that dispersion is not the caveat attached to the argument, it is the argument.")), /* @__PURE__ */ React.createElement("p", { className: "specfin-rate-foot" }, "This page claims the ", /* @__PURE__ */ React.createElement("em", null, "rate"), ": ", MEDIAN, " % median, measured, never \u201Cup to ", MKT, " %\u201D. What interruption costs to ", /* @__PURE__ */ React.createElement("em", null, "operate"), " \u2014 how a build or a batch is made to survive a pool disappearing under it, and what that discipline is worth on a cluster that is mostly CI \u2014 is argued in", " ", /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: "specfin-rate-link",
          onClick: () => window.GO && window.GO("specializations", { specId: "cicd" })
        },
        "CI/CD"
      ), ", and belongs there. What belongs here is the consequence for the rate: a median is only a number you may quote if any single pool going dark is survivable, and making it survivable is a property of the platform, not of the price."))
    );
  };
  window.BreakevenVisual = BreakevenVisual;
  window.SpotPoolCloud = SpotPoolCloud;
})();
