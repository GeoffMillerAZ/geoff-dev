// Code generated by tools/jsxbuild from OrgLogo.jsx. DO NOT EDIT.
var __defProp = Object.defineProperty;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
(function() {
  const { useState, useEffect } = React;
  const ASSET_DIR = "assets/orgs/";
  const isDarkSurface = () => {
    const root = document.documentElement;
    const theme = root.getAttribute("data-theme");
    const aes = root.getAttribute("data-aesthetic");
    if (aes === "terminal" || aes === "outrun") return true;
    return theme !== "light";
  };
  const subs = /* @__PURE__ */ new Set();
  let observing = false;
  const startObserving = () => {
    if (observing || typeof MutationObserver === "undefined") return;
    observing = true;
    new MutationObserver(() => {
      subs.forEach((fn) => fn(isDarkSurface()));
    }).observe(document.documentElement, { attributes: true, attributeFilter: ["data-theme", "data-aesthetic"] });
  };
  const useDarkSurface = () => {
    const [dark, setDark] = useState(isDarkSurface);
    useEffect(() => {
      startObserving();
      subs.add(setDark);
      setDark(isDarkSurface());
      return () => {
        subs.delete(setDark);
      };
    }, []);
    return dark;
  };
  const norm = (s) => String(s || "").trim().toLowerCase().replace(/\s+/g, " ").replace(/[.,]/g, "");
  let index = null;
  const buildIndex = () => {
    const m = window.ORG_LOGOS || {};
    const ix = /* @__PURE__ */ new Map();
    Object.keys(m).forEach((name) => {
      const entry = m[name];
      ix.set(norm(name), entry);
      (entry.alsoKnownAs || []).forEach((a) => ix.set(norm(a), entry));
    });
    return ix;
  };
  const lookup = (org) => {
    if (!index) index = buildIndex();
    return index.get(norm(org)) || null;
  };
  const KINDS = ["wordmark", "mono", "icon"];
  const pick = (entry, wanted, dark, allowChip) => {
    const variants = entry && entry.variants || [];
    const order = wanted ? [wanted].concat(KINDS.filter((k) => k !== wanted)) : KINDS;
    const surface = dark ? "dark" : "light";
    for (const kind of order) {
      const v = variants.find((x) => x.kind === kind && (x.on === "any" || x.on === surface));
      if (v) return { v, chip: null };
    }
    if (!allowChip) return null;
    for (const kind of order) {
      const v = variants.find((x) => x.kind === kind);
      if (v) return { v, chip: v.on === "dark" ? "dark" : "light" };
    }
    return null;
  };
  const CSS = `
.orglogo { display:inline-flex; align-items:center; justify-content:center; flex:0 0 auto; }
.orglogo-img { display:block; object-fit:contain; }
.orglogo.is-icon { width:var(--orglogo-size,26px); height:var(--orglogo-size,26px); }
.orglogo.is-icon .orglogo-img { width:auto; height:auto; max-width:100%; max-height:100%; }
.orglogo.is-wide { height:var(--orglogo-size,26px); }
.orglogo.is-wide .orglogo-img { height:100%; width:auto;
  max-width:calc(var(--orglogo-size,26px) * 6.5); }
.orglogo-chip { padding:calc(var(--orglogo-size,26px) * .14); border-radius:var(--rad-sm,4px);
  box-sizing:border-box; }
.orglogo-chip.on-light { background:#ffffff; box-shadow:0 0 0 1px rgba(0,0,0,.10); }
.orglogo-chip.on-dark  { background:#12101a; box-shadow:0 0 0 1px rgba(255,255,255,.14); }
`;
  let cssDone = false;
  const ensureCSS = () => {
    if (cssDone || typeof document === "undefined") return;
    cssDone = true;
    const el = document.createElement("style");
    el.setAttribute("data-orglogo", "");
    el.textContent = CSS;
    document.head.appendChild(el);
  };
  const Monogram = ({ org }) => {
    if (window.OrgMark) return React.createElement(window.OrgMark, { org });
    const initials = String(org || "").replace(/[^A-Za-z0-9 /]/g, " ").split(/[\s/]+/).filter(Boolean).slice(0, 3).map((w) => w[0]).join("").toUpperCase();
    return /* @__PURE__ */ React.createElement("span", { className: "rec-orgmark", "aria-hidden": "true" }, initials);
  };
  const OrgLogo = (_a) => {
    var _b = _a, { org, variant, size, chip = "auto", alt = "", className = "" } = _b, rest = __objRest(_b, ["org", "variant", "size", "chip", "alt", "className"]);
    ensureCSS();
    const dark = useDarkSurface();
    const [failed, setFailed] = useState(null);
    const entry = lookup(org);
    const chosen = entry ? pick(entry, variant, dark, chip !== false) : null;
    if (!chosen || failed === chosen.v.file) return /* @__PURE__ */ React.createElement(Monogram, { org });
    const { v } = chosen;
    const cls = [
      "orglogo",
      v.kind === "icon" ? "is-icon" : "is-wide",
      chosen.chip ? "orglogo-chip on-" + chosen.chip : "",
      className
    ].filter(Boolean).join(" ");
    return /* @__PURE__ */ React.createElement(
      "span",
      __spreadValues({
        className: cls,
        style: size ? { "--orglogo-size": size + "px" } : void 0
      }, rest),
      /* @__PURE__ */ React.createElement(
        "img",
        {
          className: "orglogo-img",
          src: ASSET_DIR + v.file,
          alt,
          loading: "lazy",
          decoding: "async",
          onError: () => setFailed(v.file)
        }
      )
    );
  };
  const orgLogoCaution = (org) => {
    const e = lookup(org);
    return e && e.caution || null;
  };
  const hasOrgLogo = (org, variant, allowChip = true) => {
    const e = lookup(org);
    return !!(e && pick(e, variant, isDarkSurface(), allowChip));
  };
  window.OrgLogo = OrgLogo;
  window.orgLogoCaution = orgLogoCaution;
  window.hasOrgLogo = hasOrgLogo;
})();
