// Code generated by tools/jsxbuild from SpecDevXTool.jsx. DO NOT EDIT.
(function() {
  const Icon = window.Icon;
  const SRC = {
    install: {
      kind: "vendor documentation",
      title: "nix.dev \u2014 Install Nix",
      author: "NixOS",
      publisher: "NixOS",
      year: "2026",
      method: "the installation paths the page offers",
      url: "https://nix.dev/install-nix",
      fetched: "2026-08-12",
      short: "nix.dev install",
      measured: "Four installation paths are offered: Linux, macOS, Windows (WSL2), and Docker. There is no native Windows path \u2014 the Windows section is headed \u201CWindows (WSL2)\u201D and contains WSL2 instructions.",
      against: "WSL2 is a supported, first-class path, not an absence, and most Windows developers at a technology employer already have it. The cost is real specifically at a regulated employer, where enabling a Linux kernel on a managed endpoint is a policy decision rather than a checkbox \u2014 which is the reader this page is written for."
    },
    devboxinst: {
      kind: "vendor documentation",
      title: "Jetify \u2014 Installing Devbox",
      author: "Jetify",
      publisher: "Jetify",
      year: "2026",
      method: "the supported-platform list",
      url: "https://www.jetify.com/docs/devbox/installing-devbox/",
      fetched: "2026-08-12",
      short: "Devbox install docs",
      measured: "Supported: Linux, macOS, Windows/WSL2, NixOS. \u201CYou can use Devbox on Windows using Windows Subsystem for Linux 2.\u201D And: \u201CIf Nix is not detected when running a command, Devbox will automatically install it for you.\u201D",
      against: "Auto-installing Nix is convenience for an individual and a change-management event for an organisation: something that installs a multi-user daemon on first run is a thing a platform team must decide about deliberately, not discover."
    },
    survey: {
      kind: "report",
      title: "2025 NixOS Community Survey",
      author: "NixOS Foundation",
      publisher: "NixOS Foundation",
      year: "2025",
      method: "3,399 full responses, fielded 1 September \u2013 15 December 2025",
      url: "https://nixos.org/surveys/community/2025/",
      fetched: "2026-08-12",
      short: "NixOS Community Survey 2025",
      measured: "Asked \u201CYou can afford about 3 improvements. Which aspects of the Nix package manager do you pick?\u201D, respondents prioritise flakes (50.3%) \u2014 the most-cited single area of the survey \u2014 alongside error messages (47.6%) and the reference manual (41.5%).",
      against: "It measures the opinions of people who already chose Nix: a self-selected population, and opinions are not delivery outcomes. It is also a pick-about-three question, so the percentages are shares of respondents rather than shares of a whole and do not sum to 100. Cited anyway because it is the learning-curve cost stated in Nix\u2019s own instrument by Nix\u2019s own users, which is the strongest available form of this particular objection."
    },
    jetify: {
      kind: "vendor documentation",
      title: "Jetify \u2014 Devbox FAQ",
      author: "Jetify",
      publisher: "Jetify",
      year: "2026",
      method: "the vendor\u2019s own FAQ answers",
      url: "https://www.jetify.com/docs/devbox/faq/",
      fetched: "2026-08-12",
      short: "Devbox FAQ",
      measured: "\u201CDevbox generates isolated, reproducible development environments using the Nix package manager\u201D; on Docker, \u201CNo. Since Devbox uses Nix to install packages and create isolated environments, Docker is not required\u201D; and on packages outside nixpkgs, \u201CYou can add customized packages to your Devbox environment using our Flake support.\u201D",
      against: "It is a vendor\u2019s own documentation, not an outcome study, and it puts a second lockfile format and a second vendor between you and nixpkgs. The word \u201Creproducible\u201D in its first sentence is exactly the conflation this section\u2019s prose declines to repeat."
    },
    nixexp: {
      kind: "vendor documentation",
      title: "Nix Reference Manual \u2014 Experimental Features",
      author: "NixOS",
      publisher: "NixOS",
      year: "2026 (stable channel as fetched)",
      method: "the stable manual\u2019s own feature list",
      url: "https://nix.dev/manual/nix/stable/development/experimental-features",
      fetched: "2026-08-12",
      short: "Nix manual, experimental features",
      measured: "\u201CExperimental features are considered unstable, which means that they can be changed or removed at any time.\u201D Both `flakes` and `nix-command` appear in the list of currently available experimental features.",
      against: "An experimental feature in wide production use for years is not the same risk as a new one, and treating the label as a verdict would be unfair to flakes. But the manual\u2019s sentence is the one a bank\u2019s architecture review will read, and it is the reason devbox \u2014 \u201Cmature Nix-without-flakes\u201D in my own ADR \u2014 was the pick."
    },
    flakesdoc: {
      kind: "vendor documentation",
      title: "nix.dev \u2014 Flakes",
      author: "NixOS",
      publisher: "NixOS",
      year: "2026",
      method: "the project\u2019s own concept page",
      url: "https://nix.dev/concepts/flakes.html",
      fetched: "2026-08-12",
      short: "nix.dev on flakes",
      measured: "\u201CFlakes are an experimental extension format with outstanding issues. Its functionality can generally be achieved without them as well.\u201D The page names its own drawbacks, including that \u201Cas an experimental feature, flakes can still change\u201D and that \u201Cbuilds copy the whole flake directory to the Nix store\u201D, which \u201Ccan be slower for large repositories like Nixpkgs.\u201D",
      against: "This is the official documentation being conservative, not the community\u2019s practice; a large amount of production Nix is flake-based today. It is cited because the caution comes from the project itself rather than from a critic."
    },
    malka: {
      kind: "peer-reviewed preprint",
      title: "Does Functional Package Management Enable Reproducible Builds at Scale? Yes",
      author: "Malka, Zacchiroli & Zimmermann",
      publisher: "arXiv:2501.15919",
      year: "2025",
      method: "709,816 packages rebuilt from historical nixpkgs snapshots, sampled 2017\u20132023",
      url: "https://arxiv.org/abs/2501.15919",
      fetched: "2026-08-12",
      short: "Malka et al. 2025",
      measured: "\u201CWe obtain very high bitwise reproducibility rates, between 69 and 91% with an upward trend, and even higher rebuildability rates, over 99%,\u201D across \u201C709 816 packages from historical snapshots of the nixpkgs repository \u2026 sampled in the period 2017-2023.\u201D The authors also report that \u201Cabout 15% of failures are due to embedded build dates.\u201D",
      against: "It measures nixpkgs, not your monorepo, and it ends in 2023. Sampling historical snapshots cannot detect short-lived reproducibility regressions between the revisions sampled. It is cited for the RELATIONSHIP between two properties, not as a number anyone should expect their own build to hit."
    },
    r13y: {
      kind: "continuously-generated dataset",
      title: "Is nixos.iso_minimal.x86_64-linux reproducible?",
      author: "NixOS Reproducible Builds",
      publisher: "reproducible.nixos.org",
      year: "data generated 2025-02-09 as fetched",
      method: "one curated build closure, rebuilt and compared against the official cache",
      url: "https://reproducible.nixos.org/nixos-iso-minimal-r13y/",
      fetched: "2026-08-12",
      short: "r13y, the NixOS minimal ISO",
      measured: "\u201C2551 out of 2641 (96.59%)\u201D paths in the build closure reproduce. The remainder is 89 derivations that differ from the official cache, and one path left unchecked.",
      against: "One curated closure, one architecture, and it is the BEST case: a project that has spent years on exactly this problem. It is also a live page whose numbers move, so the figure beside it is only true as of the generation date \u2014 which is why that date is printed."
    },
    nixadv: {
      kind: "vendor documentation",
      title: "Nix Reference Manual 2.22 \u2014 Advanced Attributes",
      author: "NixOS",
      publisher: "NixOS",
      year: "2024",
      method: "the manual\u2019s own definition of impureEnvVars",
      url: "https://nix.dev/manual/nix/2.22/language/advanced-attributes.html",
      fetched: "2026-08-12",
      short: "Nix manual, advanced attributes",
      measured: "On `impureEnvVars`: \u201CThis attribute is only allowed in fixed-output derivations (see below), where impurities such as these are okay since (the hash of) the output is known in advance.\u201D",
      against: "Cited AGAINST this page\u2019s own subject. It is the sanctioned escape hatch by which any source tarball is fetched at all, and it means \u201Chermetic, full stop\u201D is contradicted by the documentation of the thing making the claim. A page that only cited Nix\u2019s strengths would be a brochure."
    }
  };
  const SRC2 = [
    {
      kind: "vendor licence",
      title: "Docker Desktop license agreement",
      publisher: "Docker",
      year: "accessed 2026",
      url: "https://docs.docker.com/subscription/desktop-license/",
      fetched: "2026-08-12",
      why: "The obvious alternative, priced. Docker Desktop is free for personal use, education, non-commercial open source, and \u201CSmall businesses (fewer than 250 employees AND less than $10 million in annual revenue)\u201D; above either threshold a paid subscription is required. \u201CJust use Docker\u201D is a per-seat line item at the scale this page\u2019s reader operates at, which is why the container alternative is named honestly rather than dismissed."
    },
    {
      kind: "open specification",
      title: "Development Containers Specification",
      publisher: "containers.dev",
      year: "accessed 2026",
      url: "https://containers.dev/",
      fetched: "2026-08-12",
      why: "\u201CAn open specification for enriching containers with development specific content and settings.\u201D It is the convergence target named in my own client-facing assessment, and ADR-0020 makes devcontainer.json an OUTPUT generated from devbox rather than a second source of truth. Cited so the recommendation is visibly not exclusive of the obvious alternative."
    },
    {
      kind: "vendor documentation",
      title: "mise-en-place \u2014 Comparison to asdf",
      publisher: "mise-en-place",
      year: "accessed 2026",
      url: "https://mise.jdx.dev/dev-tools/comparison-to-asdf.html",
      fetched: "2026-08-12",
      why: "The honest smaller answer. If a build needs node and python, mise is right and a pinned store is over-engineering. Its own documentation is candid about what it replaced: asdf\u2019s shims \u201Chave terrible performance, adding ~120ms to every runtime call\u201D, and \u201Casdf plugins involve shell code which can essentially do anything on your machine.\u201D That second sentence is the boundary where a pinned store starts paying."
    }
  ];
  const COLS = [
    { id: "shell", label: "the shell" },
    { id: "first", label: "the first command" },
    { id: "ci", label: "CI" },
    { id: "mate", label: "a teammate\u2019s machine" },
    { id: "person", label: "someone has to explain it" }
  ];
  const ROWS = [
    {
      id: "ver",
      label: "which version of go, cue or kubectl?",
      stop: 1,
      dur: null,
      freq: null,
      refusedBy: "devbox.json in platform-engineering declares nine packages, every one of them @latest. devbox.lock resolves all nine to a single nixpkgs commit, a5cbcfe9\u2026. Entering the shell is the install. The version question gets one answer per repository instead of one answer per laptop, and it gets it before anything runs."
    },
    {
      id: "plat",
      label: "the same tool resolving differently on macOS and Linux",
      stop: 1,
      dur: null,
      freq: null,
      refusedBy: "The lock records a /nix/store path PER PLATFORM, not one path for everybody. cue 0.17.1 in platform-engineering\u2019s devbox.lock is three distinct store paths \u2014 aarch64-darwin, aarch64-linux, x86_64-linux \u2014 for one version. Per-package coverage varies across a lock, so this is that package in that repository rather than a property of locks in general. Devbox pins the inputs and writes down the outputs; it does not pretend two machines produce the same bytes."
    },
    {
      id: "hook",
      label: "a clone whose git hooks were never installed",
      stop: 2,
      dur: null,
      freq: null,
      refusedBy: "In platform-engineering the environment definition installs them: the shell\u2019s init_hook runs lefthook install --force. A DOCUMENTED hook is one that some fraction of clones lack, and that fraction grows with headcount; an installed one has no fraction. Two caveats, because they are mine. That line ends >/dev/null 2>&1 || true, so an install failure is currently silent \u2014 in the repository whose own lefthook.yml says a silently-disabled gate is worse than no gate. And resume_as_code\u2019s init_hook does not install hooks at all; it prints a banner and exports the workspace paths. This row is true of one repository, and it is one line short of being true without a footnote."
    },
    {
      id: "mute",
      label: "a gate that runs and quietly checks nothing",
      stop: 2,
      dur: null,
      freq: null,
      refusedBy: "Every guard announces itself. In platform-engineering\u2019s lefthook.yml the formatters print to stderr and exit 0 when their binary is absent \u2014 \u201Cgofmt missing \u2014 GATE DISABLED (enter devbox shell)\u201D, the same line for cue, and \u201Cgo-task missing \u2014 PUSH GATE DISABLED (enter devbox shell)\u201D for the task runner. The plan gate goes the other way: \u201Cfathom missing \u2014 PLAN GATE NOT RUN\u201D, and it refuses the push, because an unrunnable gate that returns success is indistinguishable from a clean verification. Which of the two a guard does is decided per command, not per file \u2014 the formatters fail open and say so out loud, the verifier fails closed. A missing tool cannot be silent."
    },
    {
      id: "parity",
      label: "the laptop and the shared runner resolving different toolchains",
      stop: null,
      dur: null,
      freq: null,
      designed: true,
      refusedBy: "DESIGNED, NOT BUILT. ADR-0020, accepted 2026-07-29, says one lockfile is meant to be \u201Cthe single toolchain fingerprint used by dev shells, CI jobs \u2026 and ci-kit hash keys \u2014 env drift becomes a cache miss, not a mystery.\u201D In these repositories there is no CI of any kind for it to be the fingerprint of: platform-engineering has no .gitlab-ci.yml and no .github/workflows. The lane is open across every column because nothing committed here refuses this difference today. I have built and run pinned toolchains against shared runners professionally; in THIS repository the mechanism is specified and not built, and the drawing says so rather than leaving the row out."
    }
  ];
  const COSTS = [
    {
      id: "win",
      label: "a developer on Windows",
      stop: 1,
      dur: null,
      freq: null,
      src: ["install", "devboxinst"],
      what: "There is no native Windows install path. nix.dev offers Linux, macOS, Windows VIA WSL2, and Docker; devbox says the same in its own words \u2014 \u201CYou can use Devbox on Windows using Windows Subsystem for Linux 2.\u201D WSL2 is a supported path, not an absence. At a regulated employer it is an endpoint-policy conversation, and it happens before anybody types a command."
    },
    {
      id: "lang",
      label: "having to change the environment rather than use it",
      stop: 2,
      dur: null,
      freq: null,
      src: ["survey"],
      what: "You meet Nix. In the NixOS Foundation\u2019s own 2025 community survey of 3,399 respondents, asked to pick about three areas they would improve, respondents prioritise flakes (50.3%) \u2014 the most-cited single area of that survey \u2014 alongside error messages (47.6%) and the reference manual (41.5%). Those are Nix\u2019s own users, naming the learning surface, in Nix\u2019s own instrument."
    },
    {
      id: "flake",
      label: "something you need that is not a package in nixpkgs",
      stop: 2,
      dur: null,
      freq: null,
      src: ["jetify", "nixexp", "flakesdoc"],
      what: "The documented answer is flakes \u2014 the experimental feature devbox was chosen to avoid. Jetify: \u201CYou can add customized packages to your Devbox environment using our Flake support.\u201D Nix\u2019s manual: \u201CExperimental features are considered unstable, which means that they can be changed or removed at any time,\u201D with flakes on that list. nix.dev is blunter: \u201CFlakes are an experimental extension format with outstanding issues. Its functionality can generally be achieved without them as well.\u201D"
    }
  ];
  const WORDS = [
    "zero",
    "one",
    "two",
    "three",
    "four",
    "five",
    "six",
    "seven",
    "eight",
    "nine",
    "ten",
    "eleven",
    "twelve",
    "thirteen",
    "fourteen",
    "fifteen",
    "sixteen",
    "seventeen",
    "eighteen",
    "nineteen",
    "twenty"
  ];
  const spell = (n) => WORDS[n] || String(n);
  const cap = (s) => s.charAt(0).toUpperCase() + s.slice(1);
  const N_ROWS = ROWS.length;
  const N_REF = ROWS.filter((r) => r.stop !== null).length;
  const N_OPEN = ROWS.filter((r) => r.stop === null).length;
  const N_COSTS = COSTS.length;
  const READOUT = cap(spell(N_ROWS)) + " differences \xB7 " + spell(N_REF) + " refused in the first two columns \xB7 " + spell(N_OPEN) + " open lane, and it is the one I have designed and not built \xB7 " + spell(N_COSTS) + " costs, and I pay all " + spell(N_COSTS) + ".";
  const PE = {
    repo: "platform-engineering",
    groups: [
      { hash: "a5cbcfe9", pkgs: [
        "cue",
        "fluxcd",
        "go",
        "go-task",
        "istioctl",
        "kubectl",
        "kubernetes-helm",
        "lefthook",
        "timoni"
      ] }
    ],
    cue: "cue 0.17.1"
  };
  const RAC = {
    repo: "resume_as_code",
    groups: [
      { hash: "12a55407", pkgs: [
        "go",
        "go-task",
        "golangci-lint",
        "gomplate",
        "gosec",
        "gum",
        "nushell",
        "pandoc",
        "shellcheck",
        "trivy"
      ] },
      { hash: "edb3633f", pkgs: ["cue"] },
      { hash: "3a641def", pkgs: ["goimports"] },
      { hash: "e0042ded", pkgs: ["hugo"] },
      { hash: "5929de97", pkgs: ["lefthook"] },
      { hash: "3866ad91", pkgs: ["nodejs"] },
      { hash: "6bfd7663", pkgs: ["revive"] }
    ],
    cue: "cue 0.13.0"
  };
  const OVERLAP = [
    { tool: "go@latest", pe: "1.26.5", rac: "1.24.3" },
    { tool: "cue@latest", pe: "0.17.1", rac: "0.13.0" },
    { tool: "go-task@latest", pe: "3.48.0", rac: "3.43.3" },
    { tool: "lefthook@latest", pe: "2.1.10", rac: "1.11.13" }
  ];
  const sum = (groups) => groups.reduce((a, g) => a + g.pkgs.length, 0);
  const READOUT2 = cap(spell(sum(PE.groups))) + " packages on one nixpkgs commit \xB7 " + spell(sum(RAC.groups)) + " packages across " + spell(RAC.groups.length) + " \xB7 " + spell(OVERLAP.length) + " tools declared identically in both, and " + spell(OVERLAP.length) + " pairs of different versions.";
  const FRAME_TITLE = "How far a difference between two machines can travel";
  const FRAME_NOTE = "Select any line for the mechanism that stops it, and where it stops.";
  const FRAME_SOURCE = "MECHANISM DIAGRAM \u2014 no measurement is drawn here. Bands one and two are counted off two of my own repositories as committed; those repositories are private, so this is a first-person report of files rather than a link you can open. The three costs are cited to Nix\u2019s and Jetify\u2019s own published documentation, linked below. The columns are ordered by who finds out \u2014 you, then your clone, then the runner, then somebody else, then a conversation. It is an order, not a duration; no column is any number of minutes wide. No duration, frequency, onboarding time or saving is claimed anywhere in this figure.";
  const FRAME2_TITLE = "One declaration, two answers";
  const FRAME2_NOTE = "Every package in two of my own lockfiles, grouped by the nixpkgs commit it resolved to.";
  const FRAME2_SOURCE = "Counted off two of my own repositories as committed, on 2026-08-12. Those repositories are private, so the versions and hashes here are a first-person report of two files rather than a link you can open; the COUNTS are on the drawing, and they are meant to be counted. No duration, no saving, no cache-hit rate and no rate of any kind appears in this figure.";
  const FOOT2_A = "one tick per package, grouped by the nixpkgs commit it resolved to.";
  const FOOT2_B = "@latest does not mean latest; it means latest on the day the package was added.";
  const FOOT2_C = "A toolchain re-locked in one pass is one group. A toolchain grown a package at a time is pinned to as many different nixpkgs as it had additions \u2014 and the second strip is mine.";
  const FOOT2 = FOOT2_A + " " + FOOT2_B + " " + FOOT2_C;
  const CSS = `
.specfl {
  --g1: #7d63a0; --g2: #9d7cc4; --g3: #bf9ae4; --g4: #dcbcff;
  /* --spec-tone resolves to --neon-purple, which sits near 2.2:1 on a white
     panel. Everything tone-coloured here goes through --tone-ink so the light
     theme has somewhere to override it. */
  --tone-ink: var(--spec-tone, var(--accent));
  --specfl-lab: 15.5rem;
  --specfl-meas: 84px;
  /* A custom property on the host, NOT a literal in the rule. A light-gated rule
     at specificity 0,6,0 would otherwise outrank the state rule at 0,4,0 and two
     states would paint at once. */
  --specfl-rest: .55;
  --specfl-tick: 20px;
  --specfl-gapin: 3px;
  --specfl-gapout: 16px;
  display: flex; flex-direction: column; gap: 14px;
}
/* THE :not() CHAIN IS LOAD-BEARING. terminal and outrun remap the neon tokens to
   DARK palettes and are selectable underneath data-theme="light", so a rule keyed
   on data-theme alone fires on a dark panel and inverts figure and ground.
   SpecDevX.jsx's bare [data-theme="light"] rules are a precedent NOT to copy. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfl {
  --tone-ink: #5f3a90;
  /* The ramp inverts on white: on the dark panel the step nearest the last
     column is the brightest, and on white the same step has to be the darkest,
     or contrast falls off in the direction the eye reads as "less important". */
  --g1: #9b7ec0; --g2: #7d5aa8; --g3: #5f3a90; --g4: #3f1f6e;
}

/* ---- the plot ---- */
.specfl-plot { position: relative; display: flex; flex-direction: column; }
.specfl-hrow { display: flex; align-items: stretch; gap: 10px; }
.specfl-lab {
  flex: 0 0 var(--specfl-lab); max-width: var(--specfl-lab); min-width: 0;
  text-align: right;
}
.specfl-plotcell {
  flex: 1 1 auto; min-width: 0;
  display: grid; grid-template-columns: repeat(5, 1fr); align-items: center;
  position: relative;
}
.specfl-meas {
  flex: 0 0 var(--specfl-meas); max-width: var(--specfl-meas);
  text-align: center;
  font-family: var(--font-mono); font-size: 11px; line-height: 1.5;
  color: var(--text-src);
}
.specfl-head { padding-bottom: 6px; align-items: flex-end; }
/* --text-mute, NOT --text-faint. Section 01 puts its axis caption in --text-faint
   and its stage NAMES in a stronger token, and these are the stage names: they
   are what the whole encoding is read against. Measured on the real panel,
   --text-faint is 2.16 / 2.67 / 2.33 / 2.62 \u2014 a label nobody can read is not a
   label. --text-mute is 3.42 / 4.82 / 3.08 / 4.59 on the same ground. */
.specfl-head .specfl-plotcell > span,
.specfl-head .specfl-meas {
  font-family: var(--font-mono); font-size: 9px; letter-spacing: .12em;
  text-transform: uppercase; color: var(--text-mute); line-height: 1.5;
  align-self: end; padding-right: 6px;
}
.specfl-head .specfl-meas { padding-right: 0; }

/* Every guide is explicitly placed in grid ROW 1. Without that, five children
   with overlapping explicit column placement are pushed onto five successive
   auto rows and each dashed rule becomes a fifth-height stub. */
.specfl-guides {
  position: absolute; top: -4px; bottom: -4px;
  left: calc(var(--specfl-lab) + 10px);
  right: calc(var(--specfl-meas) + 10px);
  display: grid; grid-template-columns: repeat(5, 1fr);
  pointer-events: none;
}
.specfl-guides > * { grid-row: 1; }
.specfl-guide { border-right: 1px dashed var(--line); }
.specfl-endband {
  grid-column: 5;
  background: color-mix(in oklab, var(--tone-ink) 7%, transparent);
  border-left: 1px dashed color-mix(in oklab, var(--tone-ink) 45%, var(--line));
  border-radius: 0 var(--rad-sm) var(--rad-sm) 0;
  display: grid; place-items: center;
}
/* The payoff, written inside the last column so a reader cannot miss what it is
   claiming: this is where a difference goes when nothing refuses it. */
.specfl-endband span {
  writing-mode: vertical-rl; text-orientation: mixed;
  font-family: var(--font-mono); font-size: 9px; letter-spacing: .22em;
  text-transform: uppercase;
  color: color-mix(in oklab, var(--tone-ink) 62%, var(--text-mute));
}

.specfl-bandlab {
  font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .11em;
  text-transform: uppercase; color: var(--text-src);
  padding: 0 0 6px;
}
.specfl-sep { margin: 18px 0; border: 0; border-top: 1px solid var(--line-soft); }

.specfl-row {
  appearance: none; font: inherit; text-align: inherit; cursor: pointer;
  background: transparent; border: 0; margin: 0;
  display: flex; align-items: center; gap: 10px;
  min-height: 36px; padding: 2px 0;
  border-radius: var(--rad-sm);
  transition: opacity .14s ease;
}
.specfl-row:focus-visible { outline: 2px solid var(--c); outline-offset: 2px; }
.specfl-row-l {
  flex: 0 0 var(--specfl-lab); max-width: var(--specfl-lab); min-width: 0;
  text-align: right;
  font-size: 12px; line-height: 1.35; color: var(--text-mute);
  transition: color .14s ease;
}
.specfl-row:hover .specfl-row-l { color: var(--text-dim); }
.specfl-row.is-on .specfl-row-l { color: var(--text); }
/* THE RESTING STATE DIMS THE MARK, NOT THE ROW. Section 01 fades the whole row,
   and measured on this ground that puts an unselected label at 1.74\u20132.24 \u2014 these
   labels are the content of the figure, not chrome, and a reader has to read all
   eight of them without clicking anything. So the de-emphasis lives on the plot
   cell and the label carries the state in colour alone, which is the channel
   section 01 already uses (--text-mute resting, --text selected: 3.42 / 4.82 /
   3.08 / 4.59 and 15.04 / 17.98 / 15.07 / 16.58). The MEASURED em-dash stays at
   full strength in every state, because the emptiness of that column is the
   argument and it must not fade with selection. */
.specfl-row.is-off .specfl-plotcell { opacity: var(--specfl-rest); }
.specfl-plotcell { transition: opacity .14s ease; }

/* The mark is a positioned wrapper; only the fill scales, so the left cap and the
   terminator glyph are never distorted by the entrance. */
.specfl-mark { position: relative; height: 5px; }
.specfl-fill {
  position: absolute; inset: 0; background: var(--c);
  border-radius: 1px 3px 3px 1px;
  transform: scaleX(0); transform-origin: left;
  transition: transform .42s ease var(--d, 0ms), box-shadow .14s ease;
}
.specfl.is-in .specfl-fill { transform: scaleX(1); }
.specfl-cap {
  position: absolute; left: 0; top: -3px; bottom: -3px; width: 2px;
  background: var(--c); opacity: .55; border-radius: 1px;
}
.specfl-term {
  position: absolute; right: 0; top: 50%;
  transform: translate(55%, -50%);
  font-family: var(--font-mono); font-size: 12.5px; font-weight: 600;
  font-style: normal; line-height: 1; color: var(--c);
  text-shadow: 0 0 8px color-mix(in oklab, var(--c) 60%, transparent);
  opacity: 0; transition: opacity .3s ease calc(var(--d, 0ms) + 180ms);
}
.specfl.is-in .specfl-term { opacity: 1; }
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfl-term {
  text-shadow: none;
}
/* Glow on white reads as blur, so the selected state drops it in the light theme
   exactly as section 01 does. */
.specfl-row.is-on .specfl-fill { box-shadow: 0 0 12px color-mix(in oklab, var(--c) 55%, transparent); }
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfl-row.is-on .specfl-fill {
  box-shadow: none;
}

/* A cost is an outline, not a fill: a different mark, not a different hue,
   because data-aesthetic="terminal" collapses hue. Its border is the mark's only
   boundary, so it takes a text token. */
.specfl-mark.is-cost .specfl-fill {
  background: transparent;
  border: 1px solid var(--text-dim);
}
.specfl-mark.is-cost .specfl-cap { background: var(--text-dim); }
.specfl-mark.is-cost .specfl-term { color: var(--text-dim); text-shadow: none; font-size: 9px; }

/* The unbuilt mechanism. It spans every column because nothing here refuses it,
   and it FADES rather than growing left to right: a mark whose position carries
   no information must not be revealed by an animation that implies reach. */
.specfl-mark.is-open {
  grid-column: 1 / -1;
  background: transparent;
  border-top: 1px dashed var(--text-mute);
  border-bottom: 1px dashed var(--text-mute);
  opacity: 0; transition: opacity .42s ease var(--d, 0ms);
}
.specfl.is-in .specfl-mark.is-open { opacity: 1; }
.specfl-arrow {
  position: absolute; top: 50%; transform: translateY(-50%);
  font-family: var(--font-mono); font-size: 11px; font-style: normal;
  line-height: 1; color: var(--text-mute);
}
.specfl-arrow.is-l { left: -2px; }
.specfl-arrow.is-r { right: -2px; }

/* The chip: clipped above 700px, visible below it, and the SAME element in both
   places \u2014 so what a screen reader announces cannot drift from what a phone
   shows. No aria-label anywhere in this figure. */
.specfl-at {
  position: absolute; width: 1px; height: 1px;
  overflow: hidden; clip-path: inset(50%); white-space: nowrap;
}

.specfl-key {
  list-style: none; margin: 12px 0 0; padding: 0;
  display: flex; flex-wrap: wrap; gap: 8px 22px;
}
.specfl-key li {
  display: flex; align-items: center; gap: 10px;
  font-size: 11.5px; line-height: 1.5; color: var(--text-mute);
}
.specfl-key .specfl-mark { width: 34px; flex: 0 0 34px; }
.specfl-key .specfl-fill { transform: scaleX(1); transition: none; }
.specfl-key .specfl-term { opacity: 1; transition: none; }
.specfl-key .specfl-mark.is-open { opacity: 1; transition: none; }

.specfl-ro {
  display: block; margin: 12px 0 0; padding: 11px 13px;
  background: var(--bg-1); border: 1px solid var(--line);
  border-radius: var(--rad-sm);
  font-size: 12.5px; line-height: 1.6; color: var(--text-dim);
}

.specfl-foot {
  margin-top: 14px; padding-top: 10px;
  border-top: 1px solid var(--line-soft);
  display: flex; flex-direction: column; gap: 9px;
  font-size: 11.5px; line-height: 1.5; color: var(--text-mute);
}
.specfl-foot p { margin: 0; max-width: 82ch; }
.specfl-foot b { color: var(--text-dim); font-weight: 500; }
.specfl-foot em {
  font-style: normal; font-family: var(--font-mono); font-size: 10px;
  letter-spacing: .1em; text-transform: uppercase; color: var(--tone-ink);
}

/* ---- the detail panel. .gf-detail is site chrome from components.css, reused
   so a reader who has seen the border diagram finds the same shape here. ---- */
.specfl-det-m {
  margin: 12px 0 0; padding-top: 10px; border-top: 1px solid var(--line-soft);
  display: flex; flex-wrap: wrap; gap: 4px 18px;
  font-family: var(--font-mono); font-size: 10px; letter-spacing: .06em;
  text-transform: uppercase; color: var(--text-src);
}
.specfl-det-src { margin: 10px 0 0; display: flex; flex-wrap: wrap; gap: 6px 14px; font-size: 12px; }

/* ---- section 06: two strips of ticks ---- */
.specfl-strip { display: flex; flex-direction: column; gap: 4px; }
.specfl-strip + .specfl-strip { margin-top: 18px; }
.specfl-strip-h {
  display: flex; flex-wrap: wrap; align-items: baseline; gap: 4px 14px;
  font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .11em;
  text-transform: uppercase; color: var(--text-src);
}
.specfl-strip-k {
  letter-spacing: .04em; text-transform: none; font-size: 10px;
  color: var(--text-dim);
}
.specfl-groups { display: flex; align-items: flex-start; gap: var(--specfl-gapout); }
.specfl-grp { display: flex; flex-direction: column; }
.specfl-carets, .specfl-ticks { display: flex; gap: var(--specfl-gapin); }
.specfl-carets i, .specfl-ticks b {
  flex: 0 0 var(--specfl-tick); width: var(--specfl-tick);
}
.specfl-carets i {
  height: 12px; font-style: normal; line-height: 1;
  font-family: var(--font-mono); font-size: 10px; text-align: center;
  color: var(--text-dim);
}
.specfl-ticks b {
  height: 26px; border-radius: 1px; font-weight: 400;
  background: color-mix(in oklab, var(--tone-ink) 78%, var(--text-mute));
  opacity: 0; transition: opacity .34s ease var(--d, 0ms);
}
.specfl.is-in .specfl-ticks b { opacity: 1; }
/* The headline tick, marked three ways: a caret above it, an outline on it, and
   a label beside the strip. One declaration, two answers, visible without
   reading a word of prose. */
.specfl-ticks b.is-key { outline: 1px solid var(--text-dim); outline-offset: 1px; }
.specfl-num {
  margin-top: 4px; height: 14px;
  font-family: var(--font-mono); font-size: 9.5px; line-height: 14px;
  text-align: center; color: var(--text-src);
}

.specfl-tbl {
  margin-top: 20px; width: 100%; border-collapse: collapse;
  font-family: var(--font-mono); font-size: 11px;
}
.specfl-tbl caption {
  text-align: left; padding-bottom: 8px;
  font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .11em;
  text-transform: uppercase; color: var(--text-src);
}
.specfl-tbl th, .specfl-tbl td {
  text-align: left; padding: 6px 12px 6px 0;
  border-bottom: 1px solid var(--line-soft);
}
.specfl-tbl th { font-weight: 400; color: var(--text-src); }
.specfl-tbl td { color: var(--text-dim); }
.specfl-tbl td:first-child { color: var(--text-mute); width: 12rem; }

/* ---- links. An anchor with a visible arrow LEAVES THE SITE to a source; a
   chevron button goes elsewhere on this site. :visited is deliberately not
   restyled \u2014 a citation you have already opened is not a lesser citation. ---- */
.specfl a {
  display: inline-flex; align-items: center; gap: 5px;
  color: var(--text-dim); text-decoration: none;
  border-bottom: 1px solid color-mix(in oklab, var(--tone-ink) 55%, var(--line));
}
.specfl a:hover { color: var(--tone-ink); border-bottom-color: var(--tone-ink); }
.specfl a svg { flex: 0 0 auto; }
.specfl :focus-visible { outline: 2px solid var(--tone-ink); outline-offset: 2px; }

/* ---- the source ledger. Same apparatus as section 04's, under its own
   selectors \u2014 see the file header for why it is not the same selectors. ---- */
.specfl-block { display: flex; flex-direction: column; gap: 10px; }
.specfl-h {
  font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .11em;
  text-transform: uppercase; color: var(--text-src);
  padding-bottom: 6px; border-bottom: 1px solid var(--line-soft);
}
.specfl-srcs { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; }
.specfl-src {
  display: grid; grid-template-columns: 132px 1fr auto; gap: 4px 12px; align-items: baseline;
  padding: 12px 0; border-bottom: 1px solid var(--line-soft);
}
.specfl-src-kind {
  font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .06em;
  text-transform: uppercase; color: var(--text-src);
}
.specfl-src-title { grid-column: 2; font-size: 13.5px; line-height: 1.45; }
.specfl-src-date {
  grid-row: 1; grid-column: 3; white-space: nowrap;
  font-family: var(--font-mono); font-size: 10px; color: var(--text-src);
}
.specfl-src-pub { grid-column: 2; font-family: var(--font-mono); font-size: 10px; color: var(--text-src); }
.specfl-src-meas, .specfl-src-agst, .specfl-src-why {
  grid-column: 2; font-size: 12.5px; line-height: 1.6; color: var(--text-mute); max-width: 82ch;
}
.specfl-src-meas b, .specfl-src-agst b {
  font-family: var(--font-mono); font-size: 9.5px; font-weight: 500; letter-spacing: .09em;
  color: var(--text-src); margin-right: 6px;
}
/* The rebuttal is the row this page emphasises. */
.specfl-src-agst b { color: var(--tone-ink); }

.specfl-read { margin: 0; font-size: 13.5px; line-height: 1.7; color: var(--text-dim); max-width: 76ch; }
.specfl-read em { font-style: normal; color: var(--text); }
.specfl-read + .specfl-read { margin-top: 14px; }

.specfl-xref { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 10px; }
.specfl-xref li {
  display: flex; flex-wrap: wrap; align-items: baseline; gap: 6px 10px;
  font-size: 12.5px; line-height: 1.65; color: var(--text-mute); max-width: 82ch;
}
.specfl-go {
  appearance: none; font: inherit; cursor: pointer; white-space: nowrap;
  font-family: var(--font-mono); font-size: 10px; letter-spacing: .06em;
  padding: 4px 9px; background: transparent; color: var(--text-dim);
  /* The border is this button's ONLY boundary, so the 3:1 floor applies to it
     and --line-strong does not clear it. */
  border: 1px solid var(--text-mute); border-radius: var(--rad-sm);
  transition: color .16s ease, border-color .16s ease;
}
.specfl-go:hover { color: var(--tone-ink); border-color: var(--tone-ink); }

@media (max-width: 860px) {
  .specfl { --specfl-lab: 11rem; }
}
/* Below 700px the plot is replaced, not scrolled: an ordinal axis read through a
   390px window is not an axis. Each row grows the chip that was clipped above,
   and the MEASURED column collapses into it \u2014 so the empty-cell argument survives
   the breakpoint as text rather than vanishing with the grid. */
@media (max-width: 700px) {
  .specfl-head, .specfl-guides { display: none; }
  .specfl-row .specfl-plotcell, .specfl-row .specfl-meas { display: none; }
  .specfl-row {
    flex-direction: column; align-items: stretch; gap: 3px;
    padding: 8px 0; min-height: 44px;
    border-bottom: 1px solid var(--line-soft);
  }
  .specfl-row-l { flex: 1 1 auto; max-width: none; text-align: left; font-size: 12.5px; }
  .specfl-at {
    position: static; width: auto; height: auto; overflow: visible;
    clip-path: none; white-space: normal;
    display: inline-flex; flex-wrap: wrap; gap: 6px;
    font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .06em;
    text-transform: uppercase; color: var(--c);
  }
  .specfl-row.is-lane .specfl-at { color: var(--text-mute); }
  .specfl { --specfl-tick: 14px; --specfl-gapin: 2px; --specfl-gapout: 10px; }
  .specfl-tbl td:first-child { width: auto; }
}
@media (max-width: 640px) {
  .specfl-src { grid-template-columns: 1fr; }
  .specfl-src-title, .specfl-src-pub, .specfl-src-date,
  .specfl-src-meas, .specfl-src-agst, .specfl-src-why { grid-column: 1; grid-row: auto; }
}
/* Both states must be legible with no motion at all, so the entrance is cancelled
   rather than shortened. */
@media (prefers-reduced-motion: reduce) {
  .specfl * { transition: none !important; }
  .specfl-fill { transform: scaleX(1) !important; }
  .specfl-term, .specfl-mark.is-open, .specfl-ticks b { opacity: 1 !important; }
}
/* Any interaction cancels the remainder of the entrance permanently. */
.specfl.is-touched .specfl-fill,
.specfl.is-touched .specfl-term,
.specfl.is-touched .specfl-mark.is-open,
.specfl.is-touched .specfl-ticks b { transition-delay: 0s; }
`;
  const ALL_MARKS = ROWS.concat(COSTS);
  const TIME_UNIT = /\b(ms|sec|second|minute|hour|day|week|month)s?\b/i;
  const SELFTEST = [
    ["five differences", N_ROWS === 5],
    ["three costs", N_COSTS === 3],
    ["four have a terminator", N_REF === 4],
    ["exactly one open lane", N_OPEN === 1],
    [
      "no terminator lands past column two",
      Math.max.apply(null, ROWS.filter((r) => r.stop).map((r) => r.stop)) === 2
    ],
    ["five columns", COLS.length === 5],
    [
      "every stop is on the axis",
      ALL_MARKS.every((r) => r.stop === null || r.stop >= 1 && r.stop <= COLS.length)
    ],
    [
      "every cost cites a source",
      COSTS.every((c) => c.src.every((k) => SRC[k] && SRC[k].url && SRC[k].fetched))
    ],
    ["the unbuilt row says so", ROWS.find((r) => r.stop === null).designed === true],
    [
      "readout words match counts",
      READOUT.toLowerCase().indexOf(spell(N_ROWS)) >= 0 && READOUT.indexOf(spell(N_COSTS)) >= 0
    ],
    /* governance — these make the page's refusals fail loudly instead of
       depending on a reviewer noticing */
    [
      "nothing on this drawing is measured",
      ALL_MARKS.every((r) => r.dur === null && r.freq === null)
    ],
    [
      "no time unit appears in any mark label",
      !ALL_MARKS.concat(COLS).some((r) => TIME_UNIT.test(r.label))
    ],
    [
      "nothing on this drawing is shaded",
      !/(hatch|fill-opacity|area|gradient)/i.test(CSS)
    ],
    /* LockCoherence */
    ["nine packages in the first strip", sum(PE.groups) === 9],
    ["one commit group in the first strip", PE.groups.length === 1],
    ["sixteen packages in the second", sum(RAC.groups) === 16],
    ["seven commit groups in the second", RAC.groups.length === 7],
    ["the numerals sum to the readout", spell(sum(RAC.groups)) === "sixteen"],
    ["four tools appear in both repos", OVERLAP.length === 4],
    ["every overlap row is two versions", OVERLAP.every((o) => o.pe !== o.rac)],
    [
      "one cue tick is marked in each strip",
      PE.groups.concat(RAC.groups).filter((g) => g.pkgs.indexOf("cue") >= 0).length === 2
    ],
    [
      "no duration or saving is drawn",
      !/\b(saved|faster|per hour|minutes?|hours?)\b/i.test(READOUT2 + FOOT2 + FRAME2_SOURCE)
    ]
  ];
  const SELFTEST_FAILED = SELFTEST.filter((t) => !t[1]).map((t) => t[0]);
  if (SELFTEST_FAILED.length && typeof console !== "undefined" && console.error) {
    console.error("SpecDevXTool \u2014 self-check FAILED: " + SELFTEST_FAILED.join("; "));
  }
  const reduced = () => typeof window.matchMedia === "function" && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const Ext = ({ href, children }) => /* @__PURE__ */ React.createElement("a", { href, target: "_blank", rel: "noopener noreferrer" }, children, " ", /* @__PURE__ */ React.createElement(Icon, { name: "ext", size: 10 }));
  const useReveal = (touched) => {
    const host = React.useRef(null);
    const watch = React.useRef(null);
    const [inView, setInView] = React.useState(false);
    React.useEffect(() => {
      if (inView) return void 0;
      const target = watch.current || host.current;
      if (!target) return void 0;
      if (reduced() || typeof IntersectionObserver !== "function") {
        setInView(true);
        return void 0;
      }
      const io = new IntersectionObserver((entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            io.disconnect();
            setInView(true);
          }
        });
      }, { threshold: 0.3 });
      io.observe(target);
      return () => io.disconnect();
    }, [inView]);
    React.useEffect(() => {
      if (touched) setInView(true);
    }, [touched]);
    return [host, watch, inView];
  };
  const delay = (i) => Math.min(i, 3) * 60 + "ms";
  const cvar = (stop) => "var(--g" + Math.min(stop || 1, 4) + ")";
  const rowc = (r) => r.stop === null ? "var(--text-mute)" : cvar(r.stop);
  const chip = (r, isCost) => {
    if (r.stop === null) return "not refused anywhere here \u2014 designed, not built \xB7 not measured";
    return (isCost ? "paid at " : "refused at ") + COLS[r.stop - 1].label + " \xB7 not measured";
  };
  const glyph = (r, isCost) => r.stop === null ? "\u2039\u254C\u203A" : isCost ? "\u25A0" : "\u2715";
  const ToolchainFloor = () => {
    const [sel, setSel] = React.useState(ROWS[0].id);
    const [touched, setTouched] = React.useState(false);
    const [host, watch, inView] = useReveal(touched);
    const pick = (id) => {
      setTouched(true);
      setSel(id);
    };
    const row = ALL_MARKS.find((r) => r.id === sel) || ROWS[0];
    const isCost = COSTS.indexOf(row) >= 0;
    const Mark = ({ r, i, cost }) => {
      if (r.stop === null) {
        return /* @__PURE__ */ React.createElement("span", { className: "specfl-mark is-open", style: { "--d": delay(i) }, "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("i", { className: "specfl-arrow is-l" }, "\u2039"), /* @__PURE__ */ React.createElement("i", { className: "specfl-arrow is-r" }, "\u203A"));
      }
      return /* @__PURE__ */ React.createElement(
        "span",
        {
          className: "specfl-mark" + (cost ? " is-cost" : ""),
          style: { gridColumn: "1 / span " + r.stop, "--d": delay(i) },
          "aria-hidden": "true"
        },
        /* @__PURE__ */ React.createElement("i", { className: "specfl-fill" }),
        /* @__PURE__ */ React.createElement("i", { className: "specfl-cap" }),
        /* @__PURE__ */ React.createElement("i", { className: "specfl-term" }, cost ? "\u25A0" : "\u2715")
      );
    };
    const Row = ({ r, i, cost }) => /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "specfl-row" + (sel === r.id ? " is-on" : " is-off") + (r.stop === null ? " is-lane" : ""),
        style: { "--c": rowc(r) },
        "aria-pressed": sel === r.id,
        onClick: () => pick(r.id)
      },
      /* @__PURE__ */ React.createElement("span", { className: "specfl-row-l" }, r.label),
      /* @__PURE__ */ React.createElement("span", { className: "specfl-at" }, /* @__PURE__ */ React.createElement("i", { "aria-hidden": "true" }, glyph(r, cost)), " ", chip(r, cost)),
      /* @__PURE__ */ React.createElement("span", { className: "specfl-plotcell" }, /* @__PURE__ */ React.createElement(Mark, { r, i, cost })),
      /* @__PURE__ */ React.createElement("span", { className: "specfl-meas", "aria-hidden": "true" }, "\u2014")
    );
    return /* @__PURE__ */ React.createElement("div", { className: "specfl" + (inView ? " is-in" : "") + (touched ? " is-touched" : ""), ref: host }, /* @__PURE__ */ React.createElement("style", { dangerouslySetInnerHTML: { __html: CSS } }), /* @__PURE__ */ React.createElement(VizFrame, { title: FRAME_TITLE, note: FRAME_NOTE, source: FRAME_SOURCE }, /* @__PURE__ */ React.createElement("div", { className: "specfl-hrow specfl-head", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("span", { className: "specfl-lab" }), /* @__PURE__ */ React.createElement("span", { className: "specfl-plotcell" }, COLS.map((c) => /* @__PURE__ */ React.createElement("span", { key: c.id }, c.label))), /* @__PURE__ */ React.createElement("span", { className: "specfl-meas" }, "measured")), /* @__PURE__ */ React.createElement("div", { className: "specfl-plot", ref: watch }, /* @__PURE__ */ React.createElement("div", { className: "specfl-guides", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("span", { className: "specfl-endband" }, /* @__PURE__ */ React.createElement("span", null, "someone has to explain it")), [1, 2, 3, 4].map((k) => /* @__PURE__ */ React.createElement("span", { key: k, className: "specfl-guide", style: { gridColumn: "1 / span " + k } }))), /* @__PURE__ */ React.createElement("div", { className: "specfl-bandlab" }, "What the environment definition refuses"), ROWS.map((r, i) => /* @__PURE__ */ React.createElement(Row, { key: r.id, r, i, cost: false })), /* @__PURE__ */ React.createElement("hr", { className: "specfl-sep" }), /* @__PURE__ */ React.createElement("div", { className: "specfl-bandlab" }, "And what it costs to have it"), COSTS.map((r, i) => /* @__PURE__ */ React.createElement(Row, { key: r.id, r, i: ROWS.length + i, cost: true }))), /* @__PURE__ */ React.createElement("ul", { className: "specfl-key" }, /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", { className: "specfl-mark", style: { "--c": "var(--g1)" }, "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("i", { className: "specfl-fill" }), /* @__PURE__ */ React.createElement("i", { className: "specfl-cap" }), /* @__PURE__ */ React.createElement("i", { className: "specfl-term" }, "\u2715")), /* @__PURE__ */ React.createElement("span", null, "refused by something committed")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", { className: "specfl-mark is-cost", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("i", { className: "specfl-fill" }), /* @__PURE__ */ React.createElement("i", { className: "specfl-cap" }), /* @__PURE__ */ React.createElement("i", { className: "specfl-term" }, "\u25A0")), /* @__PURE__ */ React.createElement("span", null, "a cost you pay, and where you find out")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", { className: "specfl-mark is-open", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("i", { className: "specfl-arrow is-l" }, "\u2039"), /* @__PURE__ */ React.createElement("i", { className: "specfl-arrow is-r" }, "\u203A")), /* @__PURE__ */ React.createElement("span", null, "designed, not built \u2014 position carries no information"))), /* @__PURE__ */ React.createElement("p", { className: "specfl-ro" }, READOUT), /* @__PURE__ */ React.createElement("div", { className: "specfl-foot" }, /* @__PURE__ */ React.createElement("p", null, /* @__PURE__ */ React.createElement("em", null, "read as"), " ", "bar length is ", /* @__PURE__ */ React.createElement("b", null, "reach"), ", not time: how far a difference between two machines can travel before something deterministic refuses it. The columns are an order \u2014 who finds out \u2014 not a duration; no column is any number of minutes wide."), /* @__PURE__ */ React.createElement("p", null, "Four of the five differences are refused in the first two columns, before a second machine ever sees them. The last three columns contain exactly one mark, and it is the mechanism I have specified and not built."), /* @__PURE__ */ React.createElement("p", null, /* @__PURE__ */ React.createElement("b", null, "MEASURED is empty everywhere, and that is the finding rather than an omission."), " The figure above this one is made of published durations. This class has none: an environment failure has no characteristic length, and no frequency anybody has published. Nothing on this drawing is a length. The fifth column \u2014 ", /* @__PURE__ */ React.createElement("b", null, "someone has to explain it"), " \u2014 is where a difference goes when nothing refuses it: somebody has to notice it, diagnose it and explain it. That has no fixed duration, which is exactly why this page declines to put a number on it."), /* @__PURE__ */ React.createElement("p", null, /* @__PURE__ */ React.createElement("b", null, "There is no \u201Cbefore\u201D state on this figure."), " A before state would have to draw how far each difference travels when nothing refuses it, and that reach is not measured. Only the refusals are visible in committed files."))), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "gf-detail",
        style: { borderLeftColor: rowc(row), "--c": rowc(row) },
        role: "status",
        "aria-live": "polite",
        "aria-atomic": "true"
      },
      /* @__PURE__ */ React.createElement("div", { className: "gf-detail-head" }, /* @__PURE__ */ React.createElement("span", { className: "gf-detail-title" }, row.label), /* @__PURE__ */ React.createElement("span", { className: "gf-detail-border", style: { color: rowc(row) } }, row.stop === null ? "not refused anywhere here" : (isCost ? "paid at \u25B8 " : "stops at \u25B8 ") + COLS[row.stop - 1].label)),
      /* @__PURE__ */ React.createElement("p", { className: "gf-detail-body" }, isCost ? row.what : row.refusedBy),
      isCost && /* @__PURE__ */ React.createElement("p", { className: "specfl-det-src" }, row.src.map((k) => /* @__PURE__ */ React.createElement(Ext, { key: k, href: SRC[k].url }, SRC[k].short))),
      /* @__PURE__ */ React.createElement("p", { className: "specfl-det-m" }, /* @__PURE__ */ React.createElement("span", null, "duration \u2014"), /* @__PURE__ */ React.createElement("span", null, "frequency \u2014"), /* @__PURE__ */ React.createElement("span", null, "this page publishes neither for this class"))
    ), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, "Nix\u2019s guarantee is ", /* @__PURE__ */ React.createElement("em", null, "rebuildability, not bit-identity \u2014 and rebuildability is the one a developer loop actually needs."), " Bit-identity lets a stranger verify that a binary came from the source it claims: a supply-chain property, and the reason the number is worth driving up. The property that ends \u201Cworks on my machine\u201D is the cheaper one \u2014 the same declared inputs producing a working environment on somebody else\u2019s laptop \u2014 and that is the one measuring ", /* @__PURE__ */ React.createElement("em", null, "over 99%"), " across 709,816 packages rebuilt from historical snapshots of nixpkgs sampled between 2017 and 2023. Bitwise reproducibility over the same corpus runs", " ", /* @__PURE__ */ React.createElement("em", null, "between 69 and 91%, with an upward trend"), " ", "(", /* @__PURE__ */ React.createElement(Ext, { href: SRC.malka.url }, "Malka et al. 2025"), "); the best-case curated closure, which is the NixOS minimal ISO, sits at ", /* @__PURE__ */ React.createElement("em", null, "96.59% \u2014 2,551 of 2,641 paths"), ", as generated on 2025-02-09 and fetched for this page on 2026-08-12 (", /* @__PURE__ */ React.createElement(Ext, { href: SRC.r13y.url }, "r13y"), "). Conflating the two properties is how Nix advocacy overclaims, and it is why \u201Creproducible builds\u201D is on this page\u2019s declined list rather than in its headline."), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, "And a hermetic claim has a stated limit, in Nix\u2019s own manual: ", /* @__PURE__ */ React.createElement("code", null, "impureEnvVars"), " is", " ", /* @__PURE__ */ React.createElement("em", null, "\u201Conly allowed in fixed-output derivations \u2026 where impurities such as these are okay since (the hash of) the output is known in advance\u201D"), " ", "(", /* @__PURE__ */ React.createElement(Ext, { href: SRC.nixadv.url }, "Nix manual, advanced attributes"), "). That is the sanctioned escape hatch by which any source tarball is fetched at all. \u201CHermetic, full stop\u201D is contradicted by the documentation of the thing making the claim."), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, "And if your build only needs node and python, ", /* @__PURE__ */ React.createElement("em", null, "mise is the right answer and this is over-engineering."), " It pins language runtimes; it does not pin the C toolchain, the system libraries, or the CLI tools your build shells out to. That difference is the entire case for a pinned store, and it is not a difference every team has."), /* @__PURE__ */ React.createElement("div", { className: "specfl-block" }, /* @__PURE__ */ React.createElement("span", { className: "specfl-h" }, "Where the costs came from \xB7 ", spell(Object.keys(SRC).length), " sources, every URL fetched 2026-08-12"), /* @__PURE__ */ React.createElement("ul", { className: "specfl-srcs" }, Object.keys(SRC).map((k) => {
      const s = SRC[k];
      return /* @__PURE__ */ React.createElement("li", { key: k, className: "specfl-src" }, /* @__PURE__ */ React.createElement("span", { className: "specfl-src-kind" }, s.kind), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-title" }, /* @__PURE__ */ React.createElement(Ext, { href: s.url }, s.author + " \u2014 " + s.title)), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-date" }, "fetched ", s.fetched), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-pub" }, s.publisher + ", " + s.year + " \xB7 " + s.method), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-meas" }, /* @__PURE__ */ React.createElement("b", null, "MEASURED"), s.measured), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-agst" }, /* @__PURE__ */ React.createElement("b", null, "AGAINST IT"), s.against));
    }))), /* @__PURE__ */ React.createElement("div", { className: "specfl-block" }, /* @__PURE__ */ React.createElement("span", { className: "specfl-h" }, "Cited, and nothing is drawn from them"), /* @__PURE__ */ React.createElement("ul", { className: "specfl-srcs" }, SRC2.map((s) => /* @__PURE__ */ React.createElement("li", { key: s.url, className: "specfl-src" }, /* @__PURE__ */ React.createElement("span", { className: "specfl-src-kind" }, s.kind), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-title" }, /* @__PURE__ */ React.createElement(Ext, { href: s.url }, s.title)), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-date" }, "fetched ", s.fetched), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-pub" }, s.publisher + ", " + s.year), /* @__PURE__ */ React.createElement("span", { className: "specfl-src-why" }, s.why))))));
  };
  const LockCoherence = () => {
    const [host, watch, inView] = useReveal(false);
    const Strip = ({ data }) => {
      let n = -1;
      return /* @__PURE__ */ React.createElement("div", { className: "specfl-strip" }, /* @__PURE__ */ React.createElement("div", { className: "specfl-strip-h" }, /* @__PURE__ */ React.createElement("span", null, data.repo), /* @__PURE__ */ React.createElement("span", { className: "specfl-strip-k" }, "\u25BE ", data.cue)), /* @__PURE__ */ React.createElement("div", { className: "specfl-groups", "aria-hidden": "true" }, data.groups.map((g) => /* @__PURE__ */ React.createElement("div", { key: g.hash, className: "specfl-grp" }, /* @__PURE__ */ React.createElement("div", { className: "specfl-carets" }, g.pkgs.map((p) => /* @__PURE__ */ React.createElement("i", { key: p }, p === "cue" ? "\u25BE" : ""))), /* @__PURE__ */ React.createElement("div", { className: "specfl-ticks" }, g.pkgs.map((p) => {
        n += 1;
        return /* @__PURE__ */ React.createElement(
          "b",
          {
            key: p,
            title: p,
            style: { "--d": delay(n) },
            className: p === "cue" ? "is-key" : ""
          }
        );
      })), /* @__PURE__ */ React.createElement("div", { className: "specfl-num" }, g.pkgs.length)))));
    };
    return /* @__PURE__ */ React.createElement("div", { className: "specfl" + (inView ? " is-in" : ""), ref: host }, /* @__PURE__ */ React.createElement("style", { dangerouslySetInnerHTML: { __html: CSS } }), /* @__PURE__ */ React.createElement(VizFrame, { title: FRAME2_TITLE, note: FRAME2_NOTE, source: FRAME2_SOURCE }, /* @__PURE__ */ React.createElement("div", { ref: watch }, /* @__PURE__ */ React.createElement(Strip, { data: PE }), /* @__PURE__ */ React.createElement(Strip, { data: RAC })), /* @__PURE__ */ React.createElement("table", { className: "specfl-tbl" }, /* @__PURE__ */ React.createElement("caption", null, "The same declaration in both repositories, and what it resolved to"), /* @__PURE__ */ React.createElement("thead", null, /* @__PURE__ */ React.createElement("tr", null, /* @__PURE__ */ React.createElement("th", { scope: "col" }, "declared"), /* @__PURE__ */ React.createElement("th", { scope: "col" }, PE.repo), /* @__PURE__ */ React.createElement("th", { scope: "col" }, RAC.repo))), /* @__PURE__ */ React.createElement("tbody", null, OVERLAP.map((o) => /* @__PURE__ */ React.createElement("tr", { key: o.tool }, /* @__PURE__ */ React.createElement("td", null, o.tool), /* @__PURE__ */ React.createElement("td", null, o.pe), /* @__PURE__ */ React.createElement("td", null, o.rac))))), /* @__PURE__ */ React.createElement("p", { className: "specfl-ro" }, READOUT2), /* @__PURE__ */ React.createElement("div", { className: "specfl-foot" }, /* @__PURE__ */ React.createElement("p", null, /* @__PURE__ */ React.createElement("em", null, "read as"), " ", FOOT2_A, " ", /* @__PURE__ */ React.createElement("b", null, FOOT2_B), " ", FOOT2_C))), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, "A cost paid per person is one you can hire into. A cost paid at a shared server is not, and the difference is ", /* @__PURE__ */ React.createElement("em", null, "shape rather than size"), "."), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, "Three shapes turn up in a platform organisation. The first is ", /* @__PURE__ */ React.createElement("em", null, "per person"), ": onboarding, a laptop, a licence. The total grows with headcount and each engineer\u2019s experience is unchanged \u2014 expensive, budgetable, and it never gets worse for the individual. The second is a", " ", /* @__PURE__ */ React.createElement("em", null, "queue whose capacity grows with the organisation"), ": more reviewers arrive with more authors, and no amount of hiring makes the wait worse. The third is a", " ", /* @__PURE__ */ React.createElement("em", null, "queue with a fixed shared server"), " \u2014 one platform team reviewing every change, one shared environment, one person who knows how the toolchain works. Here the load rises ", /* @__PURE__ */ React.createElement("em", null, "with"), " ", "headcount while the server does not, and the wait does not merely grow in proportion: it grows faster than the load, and past a point it has no steady value at all. Section 02 already calls the first of these a queue; section 03 already says a shared environment turns a development loop into a booking system. This is the name for the shape those two sections were drawing."), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, /* @__PURE__ */ React.createElement("em", null, "Self-service does not shorten the third shape. It converts the third shape into the first."), " ", "A lockfile does not make the person who knows the toolchain answer faster \u2014 it takes the question off their desk and puts a deterministic answer on every laptop. A gate installed by the environment definition does not make a reviewer faster \u2014 it deletes the class of review. That is the whole of what this page claims for the work, and ", /* @__PURE__ */ React.createElement("em", null, "it is a claim about the shape of a cost, not its size. No curve of it appears anywhere on this page"), ", because the load on any shared server here was never measured, and the shape does not need a magnitude to be the point."), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, "ADR-0020, accepted 2026-07-29, gives that lockfile a second job:", " ", /* @__PURE__ */ React.createElement("em", null, "\u201Cone lockfile is the single toolchain fingerprint used by dev shells, CI jobs \u2026 and ci-kit hash keys \u2014 env drift becomes a cache miss, not a mystery.\u201D"), " Read against the figure above, that turns a tidiness problem into a mechanical one: a cache key derived from the lock cannot hit across two repositories that resolve the same declaration to different commits. The figure and the organisational argument are the same fact seen twice."), /* @__PURE__ */ React.createElement("p", { className: "specfl-read" }, /* @__PURE__ */ React.createElement("em", null, "And the honest half: the engine that would consume that key does not exist in this repository."), " ci-kit is a specification \u2014 architecture documents and accepted ADRs \u2014 with no implementation on disk; the architecture index places it in a phase for which no plan file has been written yet. The lockfile is real and in use today; the fingerprint it is designed to be is designed. I have built and run pinned toolchains against shared runners professionally; what this repository currently proves is the floor, not the engine. What an engine does with a proof belongs to the CI/CD page; what it costs a team to get one belongs here."), /* @__PURE__ */ React.createElement("ul", { className: "specfl-xref" }, /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "The environment the FinOps page counts as a standing copy billing 168 hours a week is the same object this page counts as rows three and four of the loop \u2014 waiting for it to exist, and waiting for whoever is in it. One mechanism, two ledgers.", " ", /* @__PURE__ */ React.createElement("em", { style: { fontStyle: "normal", color: "var(--text-dim)" } }, "That page owns the money. This page owns the time.")), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "specfl-go",
        onClick: () => window.GO && window.GO("specializations", { specId: "finops" })
      },
      "FinOps \u203A"
    ))));
  };
  window.ToolchainFloor = ToolchainFloor;
  window.LockCoherence = LockCoherence;
})();
