// Code generated by tools/jsxbuild from SpecDevX.jsx. DO NOT EDIT.
(function() {
  const SpecPageShell = window.SpecPageShell;
  const SpecSection = window.SpecSection;
  const SpecSignature = window.SpecSignature;
  const PlaceholderBadge = window.PlaceholderBadge;
  const CardGrid = window.CardGrid;
  const BORDERS = [
    {
      id: "g1",
      label: "pre-commit",
      title: "Local \u2192 Commit",
      border: "installed by the harness, runs on the machine",
      detail: "The cheapest border to hold, and the one teams most often leave to good intentions. The harness installs the hook set rather than documenting it, so the check exists on every clone without anyone opting in \u2014 including the clone an agent is working in.",
      kills: [
        {
          id: "k-secret",
          label: "A secret in git history",
          how: "The scan runs before the object exists. This is the one class of mistake that cannot be taken back once it has left the machine, which is why it is held at the cheapest border rather than the loudest one."
        },
        {
          id: "k-dep",
          label: "An unapproved package in the dependency tree",
          how: "The allow-list is read from the platform's own documentation, so an unapproved library fails seconds after it is added rather than days later in a review thread."
        },
        {
          id: "k-fmt",
          label: "A review that spends its attention on formatting",
          how: "Style is decided by a program at the border, so nothing downstream is allowed to have an opinion about it. Human attention arrives at the design question with nothing in front of it."
        }
      ]
    },
    {
      id: "g2",
      label: "tests",
      title: "Commit \u2192 Pull request",
      border: "every layer this kind of service owes",
      detail: "Testing standards usually fail not because tests are absent but because the wrong layers are present \u2014 plenty of unit tests, no contract test, and an integration suite nobody ran. The harness knows which layers this service type owes and requires all of them.",
      kills: [
        {
          id: "k-shape",
          label: "A service shipping with the wrong shape of test",
          how: "Unit, integration, contract and end-to-end are required by what the service is, not by what its author felt like writing that week."
        },
        {
          id: "k-green",
          label: "A green suite that never touched the change",
          how: "The tests have to exercise the diff. A suite that passes without executing the new code is a green light with nothing behind it."
        },
        {
          id: "k-cov",
          label: "One coverage number standing in for test quality",
          how: "Coverage is applied to the layers where it means something instead of averaged into a single figure that can be satisfied without testing anything that matters."
        }
      ]
    },
    {
      id: "g3",
      label: "policy + evidence",
      title: "Pull request \u2192 Main",
      border: "policy-as-code, and the artifacts an approval needs",
      detail: "The border where an organisation's governance normally turns into a queue. The change arrives carrying its own evidence, generated as structured artifacts rather than written by hand afterwards, so the human review is about design instead of paperwork.",
      kills: [
        {
          id: "k-plan",
          label: "Infrastructure meeting its policy for the first time at deploy",
          how: "The plan is evaluated against policy-as-code before the merge, so a violation is a failed check on a pull request rather than a rollback in an environment."
        },
        {
          id: "k-doc",
          label: "An approval waiting on a document nobody has written yet",
          how: "Architecture and logging artifacts are generated with the change, in one shape, so a review board reads the same artifact every time instead of whatever each author invented."
        },
        {
          id: "k-intent",
          label: "A change whose author cannot be asked what it was for",
          how: "Agent-authored diffs carry their reasoning across the border with them. An approver sees what was intended and not only what was produced \u2014 the difference between reviewing a change and receiving one."
        }
      ]
    },
    {
      id: "g4",
      label: "deploy patterns",
      title: "Main \u2192 Production",
      border: "the golden path, applied at creation",
      detail: "The last border, and the one where hand-rolled variation is most expensive. Deployment shape comes from the golden path rather than from whatever the last service happened to do.",
      kills: [
        {
          id: "k-rollout",
          label: "A team re-deriving rollout, health gating and rollback",
          how: "The deployment pattern comes from the golden path. Progressive delivery is the platform's problem, solved once, rather than each team's re-derivation of it."
        },
        {
          id: "k-posture",
          label: "A resource that exists before its tagging, encryption and network posture do",
          how: "The controls an audit asks about are applied at creation. There is no window in which the resource is real and its posture is still pending."
        },
        {
          id: "k-signal",
          label: "A service that ships with no signal",
          how: "Observability is wired in by the pattern rather than added after the first incident that needed it. A service nobody can operate is not finished."
        }
      ]
    }
  ];
  const STAGES = [
    { id: "local", label: "Local", sub: "laptop + agent" },
    { id: "commit", label: "Commit", sub: "on the machine" },
    { id: "pr", label: "Pull request", sub: "CI" },
    { id: "main", label: "Main", sub: "release candidate" },
    { id: "prod", label: "Production", sub: "cloud" }
  ];
  const KILL_COUNT = BORDERS.reduce((a, b) => a + b.kills.length, 0);
  const WORDS = [
    "zero",
    "one",
    "two",
    "three",
    "four",
    "five",
    "six",
    "seven",
    "eight",
    "nine",
    "ten",
    "eleven",
    "twelve",
    "thirteen",
    "fourteen",
    "fifteen"
  ];
  const spell = (n) => WORDS[n] || String(n);
  const CSS = `
.specdx-pb {
  --g1: #7d63a0; --g2: #9d7cc4; --g3: #bf9ae4; --g4: #dcbcff;
  /* The page tone as INK. --spec-tone resolves to --neon-purple, which the
     light theme does not redefine \u2014 #b893ce sits at roughly 2.2:1 on a white
     panel and fails the 3:1 floor for text and graphical objects. Everything
     tone-coloured in this file goes through --tone-ink so the light theme has
     somewhere to override it. */
  --tone-ink: var(--spec-tone, var(--accent));
  --specdx-lab: 15.5rem;
  display: flex; flex-direction: column; gap: 10px;
}
[data-theme="light"] .specdx-pb,
[data-theme="light"] .specdx-swap,
[data-theme="light"] .specdx-refuse { --tone-ink: #5f3a90; }

/* The light-theme ramp. It has to INVERT: on the dark panel the ordinal step
   nearest production is the brightest, and on white the same step has to be the
   darkest, or contrast falls off in exactly the direction the eye reads as
   "less important". Without this block the dark ramp painted onto the white
   plot, and the two steps that carry the argument were the two that vanished \u2014
   measured in the browser at 2.34:1 (--g3) and 1.66:1 (--g4) against #ffffff,
   both under the 3:1 floor for a graphical object. Measured again with this
   block: 3.35 / 4.94 / 7.34 / 11.62, monotone and all clearing 3:1. */
[data-theme="light"] .specdx-pb {
  --g1: #9b7ec0; --g2: #7d5aa8; --g3: #5f3a90; --g4: #3f1f6e;
}

.specdx-hrow { display: flex; align-items: stretch; gap: 10px; }
.specdx-lab {
  flex: 0 0 var(--specdx-lab); max-width: var(--specdx-lab);
  min-width: 0; text-align: right;
}
.specdx-axis {
  display: flex; align-items: flex-end; justify-content: flex-end;
  font-family: var(--font-mono); font-size: 9px; letter-spacing: .12em;
  text-transform: uppercase; color: var(--text-faint); line-height: 1.5;
  padding-bottom: 4px;
}
.specdx-tr {
  flex: 1 1 auto; min-width: 0;
  display: grid; grid-template-columns: repeat(10, 1fr); align-items: center;
  position: relative;
}

.specdx-stage {
  grid-column: span 2;
  display: flex; flex-direction: column; gap: 2px;
  padding: 8px 9px; min-width: 0;
  background: var(--bg-2); border: 1px solid var(--line);
  border-radius: var(--rad-sm);
  margin-right: 3px;
}
.specdx-stage:last-child { margin-right: 0; }
.specdx-stage b {
  font-family: var(--font-mono); font-size: 9.5px; font-weight: 500;
  letter-spacing: .07em; text-transform: uppercase; color: var(--text-dim);
}
.specdx-stage i { font-style: normal; font-size: 9.5px; line-height: 1.3; color: var(--text-faint); }
.specdx-stage.is-prod {
  background: transparent;
  border-style: dashed;
  border-color: color-mix(in oklab, var(--tone-ink) 45%, var(--line));
}
.specdx-stage.is-prod b { color: var(--tone-ink); }

.specdx-gates { margin-top: 6px; }
.specdx-gate {
  appearance: none; font: inherit; cursor: pointer;
  justify-self: center; align-self: start;
  display: inline-flex; flex-direction: column; align-items: center; gap: 5px;
  max-width: 100%;
  padding: 7px 10px;
  background: var(--bg-1); border: 1px dashed var(--line); border-radius: var(--rad-sm);
  color: var(--text-mute);
  transition: border-color .14s ease, color .14s ease, background .14s ease;
}
.specdx-gate:hover { border-color: var(--c); color: var(--text-dim); }
.specdx-gate:focus-visible { outline: 2px solid var(--c); outline-offset: 2px; }
.specdx-gate.is-on {
  border-style: solid; border-color: var(--c);
  background: color-mix(in oklab, var(--c) 14%, transparent);
  color: var(--text);
}
.specdx-gate-n {
  display: grid; place-items: center; flex: 0 0 auto;
  width: 16px; height: 16px; border-radius: 50%;
  border: 1px solid var(--c); color: var(--c);
  font-family: var(--font-mono); font-size: 9px; line-height: 1;
}
.specdx-gate.is-on .specdx-gate-n { background: var(--c); color: var(--bg); border-color: var(--c); }
.specdx-gate-l {
  font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .03em;
  line-height: 1.35; text-align: center; text-wrap: balance;
}

.specdx-plot { position: relative; display: flex; flex-direction: column; gap: 5px; margin-top: 10px; }
.specdx-guides {
  position: absolute; top: -4px; bottom: -4px; right: 0;
  left: calc(var(--specdx-lab) + 10px);
  display: grid; grid-template-columns: repeat(10, 1fr);
  pointer-events: none;
}
/* Every guide is explicitly placed in grid ROW 1. Without that, five children
   with overlapping explicit column placement are pushed onto five successive
   auto rows and each dashed border becomes a fifth-height stub. */
.specdx-guides > * { grid-row: 1; }
.specdx-guide { border-right: 1px dashed var(--line); }
.specdx-prodband {
  background: color-mix(in oklab, var(--tone-ink) 7%, transparent);
  border-left: 1px dashed color-mix(in oklab, var(--tone-ink) 45%, var(--line));
  border-radius: 0 var(--rad-sm) var(--rad-sm) 0;
  display: grid; place-items: center;
}
/* The payoff, written inside the empty column so the reader cannot miss what
   the emptiness is claiming. */
.specdx-prodband span {
  writing-mode: vertical-rl; text-orientation: mixed;
  font-family: var(--font-mono); font-size: 9px; letter-spacing: .22em;
  text-transform: uppercase;
  color: color-mix(in oklab, var(--tone-ink) 62%, var(--text-mute));
}

.specdx-row {
  appearance: none; font: inherit; text-align: inherit; cursor: pointer;
  background: transparent; border: 0; padding: 2px 0; margin: 0;
  display: flex; align-items: center; gap: 10px;
  border-radius: var(--rad-sm);
  transition: opacity .14s ease;
}
.specdx-row:focus-visible { outline: 2px solid var(--c); outline-offset: 2px; }
.specdx-row-l {
  flex: 0 0 var(--specdx-lab); max-width: var(--specdx-lab); min-width: 0;
  text-align: right;
  font-size: 12px; line-height: 1.35; color: var(--text-mute);
  transition: color .14s ease;
}
.specdx-row.is-on .specdx-row-l { color: var(--text); }
.specdx-row.is-off { opacity: .5; }
.specdx-row:hover .specdx-row-l { color: var(--text-dim); }
.specdx-bar {
  position: relative; height: 5px; border-radius: 1px 3px 3px 1px;
  background: var(--c);
  transition: box-shadow .14s ease;
}
.specdx-bar::before {
  content: ""; position: absolute; left: 0; top: -3px; bottom: -3px; width: 2px;
  background: var(--c); opacity: .55; border-radius: 1px;
}
.specdx-bar::after {
  content: "\u2715"; position: absolute; right: 0; top: 50%;
  transform: translate(55%, -50%);
  font-family: var(--font-mono); font-size: 12.5px; font-weight: 600;
  line-height: 1; color: var(--c);
  text-shadow: 0 0 8px color-mix(in oklab, var(--c) 60%, transparent);
}
[data-theme="light"] .specdx-bar::after { text-shadow: none; }
.specdx-row.is-on .specdx-bar { box-shadow: 0 0 12px color-mix(in oklab, var(--c) 55%, transparent); }
[data-theme="light"] .specdx-row.is-on .specdx-bar { box-shadow: none; }
.specdx-at { display: none; }

.specdx-foot {
  display: flex; flex-wrap: wrap; gap: 6px 14px; align-items: baseline;
  margin-top: 14px; padding-top: 10px; border-top: 1px solid var(--line-soft);
  font-size: 11.5px; line-height: 1.5; color: var(--text-mute);
}
.specdx-foot b { color: var(--text-dim); font-weight: 500; }
.specdx-foot em {
  font-style: normal; font-family: var(--font-mono); font-size: 10px;
  letter-spacing: .1em; text-transform: uppercase; color: var(--tone-ink);
}

.specdx-kills { margin: 12px 0 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: 2px; }
.specdx-kills li {
  display: flex; flex-direction: column; gap: 2px;
  padding: 7px 9px; border-radius: var(--rad-sm);
  border-left: 2px solid transparent;
}
.specdx-kills li.is-focus { background: var(--bg-1); border-left-color: var(--c); }
.specdx-kill-w { font-family: var(--font-mono); font-size: 11.5px; line-height: 1.45; color: var(--c); }
.specdx-kill-h { font-size: 12px; line-height: 1.55; color: var(--text-mute); max-width: 74ch; }

/* Comparison panel \u2014 local development. */
.specdx-swap {
  --tone-ink: var(--spec-tone, var(--accent));
  display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 12px;
}
.specdx-col { padding: 12px 14px; background: var(--bg-2); border: 1px solid var(--line); border-radius: var(--rad-sm); }
.specdx-col-h {
  display: block; font-family: var(--font-mono); font-size: 9.5px;
  letter-spacing: .1em; text-transform: uppercase; color: var(--text-faint);
  margin-bottom: 9px;
}
.specdx-col.is-now { border-left: 2px solid var(--tone-ink); }
.specdx-col.is-now .specdx-col-h { color: var(--tone-ink); }
.specdx-col ul { margin: 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: 8px; }
.specdx-col li { display: flex; gap: 8px; font-size: 12.5px; line-height: 1.5; color: var(--text-dim); }
.specdx-col li span:first-child {
  flex: 0 0 auto; font-family: var(--font-mono); font-size: 11px; color: var(--text-faint);
}
.specdx-col.is-now li span:first-child { color: var(--tone-ink); }

/* The refused number. Amber + dashed is this site's mark for "draft, not
   verified" and this panel is nothing but that. */
.specdx-refuse {
  padding: 16px 18px;
  background: var(--bg-1);
  border: 1px dashed color-mix(in oklab, var(--neon-amber) 45%, var(--line));
  border-radius: var(--rad);
}
.specdx-refuse p { margin: 0 0 12px; max-width: 74ch; font-size: 13.5px; line-height: 1.7; color: var(--text-dim); }
.specdx-refuse p:last-of-type { margin-bottom: 0; }
.specdx-refuse-n {
  display: inline-flex; align-items: baseline; gap: 9px; margin-bottom: 12px;
}
.specdx-refuse-v {
  font-family: var(--font-mono); font-size: 30px; line-height: 1;
  color: var(--neon-amber);
}
[data-theme="light"] .specdx-refuse-v { color: var(--neon-amber); }
.specdx-refuse-src {
  margin: 14px 0 0; padding-top: 10px; border-top: 1px solid var(--line-soft);
  font-family: var(--font-mono); font-size: 10.5px; line-height: 1.6; color: var(--text-faint);
}

@media (max-width: 860px) {
  .specdx-pb { --specdx-lab: 11rem; }
  .specdx-gate-l { font-size: 9px; }
}
@media (max-width: 700px) {
  .specdx-hrow.specdx-stages .specdx-lab,
  .specdx-hrow.specdx-gates .specdx-lab { display: none; }
  .specdx-hrow.specdx-stages .specdx-tr { display: flex; flex-wrap: wrap; gap: 5px; }
  .specdx-hrow.specdx-gates .specdx-tr { display: flex; flex-wrap: wrap; gap: 5px; }
  .specdx-stage { flex: 1 1 40%; margin-right: 0; }
  .specdx-gate { justify-self: stretch; }
  .specdx-guides { display: none; }
  .specdx-plot { gap: 2px; }
  .specdx-row { flex-direction: column; align-items: stretch; gap: 3px; padding: 8px 0; border-bottom: 1px solid var(--line-soft); }
  .specdx-row-l { flex: 1 1 auto; max-width: none; text-align: left; font-size: 12.5px; }
  .specdx-row .specdx-tr { display: none; }
  .specdx-at {
    display: inline-flex; align-items: center; gap: 6px;
    font-family: var(--font-mono); font-size: 9.5px; letter-spacing: .06em;
    text-transform: uppercase; color: var(--c);
  }
  .specdx-at::before { content: "\u2715"; font-size: 10px; }
}
`;
  const DevXSignature = () => {
    const [sel, setSel] = React.useState(BORDERS[0].id);
    const [focus, setFocus] = React.useState(null);
    const gate = BORDERS.find((b) => b.id === sel) || BORDERS[0];
    const gi = BORDERS.indexOf(gate);
    const cvar = (i) => "var(--g" + (i + 1) + ")";
    const rows = [];
    BORDERS.forEach((b, i) => {
      b.kills.forEach((k) => rows.push({ b, i, k }));
    });
    return /* @__PURE__ */ React.createElement("div", { className: "specdx-pb" }, /* @__PURE__ */ React.createElement("div", { className: "specdx-hrow specdx-stages" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-lab specdx-axis" }, "what cannot", /* @__PURE__ */ React.createElement("br", null), "reach production"), /* @__PURE__ */ React.createElement("span", { className: "specdx-tr" }, STAGES.map((s, i) => /* @__PURE__ */ React.createElement("span", { key: s.id, className: "specdx-stage" + (i === STAGES.length - 1 ? " is-prod" : "") }, /* @__PURE__ */ React.createElement("b", null, s.label), /* @__PURE__ */ React.createElement("i", null, s.sub))))), /* @__PURE__ */ React.createElement("div", { className: "specdx-hrow specdx-gates" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-lab", "aria-hidden": "true" }), /* @__PURE__ */ React.createElement("span", { className: "specdx-tr" }, BORDERS.map((b, i) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: b.id,
        type: "button",
        className: "specdx-gate" + (sel === b.id ? " is-on" : ""),
        style: { gridColumn: 2 * (i + 1) + " / span 2", "--c": cvar(i) },
        "aria-pressed": sel === b.id,
        "aria-label": "Border " + (i + 1) + ", " + b.title + ". Show what it makes impossible.",
        onClick: () => {
          setSel(b.id);
          setFocus(null);
        }
      },
      /* @__PURE__ */ React.createElement("span", { className: "specdx-gate-n" }, i + 1),
      /* @__PURE__ */ React.createElement("span", { className: "specdx-gate-l" }, b.label)
    )))), /* @__PURE__ */ React.createElement("div", { className: "specdx-plot" }, /* @__PURE__ */ React.createElement("div", { className: "specdx-guides", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-prodband", style: { gridColumn: "9 / span 2" } }, /* @__PURE__ */ React.createElement("span", null, "nothing arrives here")), BORDERS.map((b, i) => /* @__PURE__ */ React.createElement("span", { key: b.id, className: "specdx-guide", style: { gridColumn: "1 / span " + 2 * (i + 1) } }))), rows.map((r) => {
      const on = r.b.id === sel;
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: r.k.id,
          type: "button",
          className: "specdx-row" + (on ? " is-on" : " is-off"),
          style: { "--c": cvar(r.i) },
          "aria-pressed": focus === r.k.id,
          "aria-label": r.k.label + " \u2014 stopped at border " + (r.i + 1) + ", " + r.b.title + ".",
          onClick: () => {
            setSel(r.b.id);
            setFocus(r.k.id);
          }
        },
        /* @__PURE__ */ React.createElement("span", { className: "specdx-row-l" }, r.k.label),
        /* @__PURE__ */ React.createElement("span", { className: "specdx-tr" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-bar", style: { gridColumn: "1 / span " + 2 * (r.i + 1) } })),
        /* @__PURE__ */ React.createElement("span", { className: "specdx-at" }, r.b.label)
      );
    })), /* @__PURE__ */ React.createElement("p", { className: "specdx-foot" }, /* @__PURE__ */ React.createElement("em", null, "read as"), /* @__PURE__ */ React.createElement("span", null, "bar length is ", /* @__PURE__ */ React.createElement("b", null, "reach"), ", not time \u2014 how far a change can carry the mistake before something deterministic refuses it. ", spell(KILL_COUNT).charAt(0).toUpperCase() + spell(KILL_COUNT).slice(1), " ", "classes of mistake, and the furthest any of them gets is a border. The claim is not that the team became more careful; it is that these particular mistakes stopped being available.")), /* @__PURE__ */ React.createElement("div", { className: "gf-detail", style: { borderLeftColor: cvar(gi), "--c": cvar(gi) } }, /* @__PURE__ */ React.createElement("div", { className: "gf-detail-head" }, /* @__PURE__ */ React.createElement("span", { className: "gf-detail-title" }, gate.title), /* @__PURE__ */ React.createElement("span", { className: "gf-detail-border", style: { color: cvar(gi) } }, gate.border)), /* @__PURE__ */ React.createElement("p", { className: "gf-detail-body" }, gate.detail), /* @__PURE__ */ React.createElement("ul", { className: "specdx-kills" }, gate.kills.map((k) => /* @__PURE__ */ React.createElement("li", { key: k.id, className: focus === k.id ? "is-focus" : "" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-kill-w" }, "\u2715\xA0 ", k.label), /* @__PURE__ */ React.createElement("span", { className: "specdx-kill-h" }, k.how))))));
  };
  const HARNESS_CARDS = {
    title: "Why the gate is on every repository, not just the disciplined ones",
    note: "A harness is the governed environment an agent and a developer both work inside: one organisation's approved toolchain, required test layers, hooks and deployment patterns. The meta-harness builds harnesses \u2014 which is the half that scales, because a governed environment only helps if every team can have one.",
    source: "Mechanism as described on the Agentic Coding Harness & Meta-Harness project page. Deliberately contains no figures: it describes what the environment enforces, not how much faster anyone got.",
    min: 236,
    cards: [
      {
        id: "std",
        tone: "cyan",
        label: "ORG STANDARDS",
        what: "Security, compliance and SDLC rules, read once into the harness.",
        why: "Compliance degrades with distance from whoever wrote the rule. Reading it into the workspace removes the distance."
      },
      {
        id: "docs",
        tone: "cyan",
        label: "PLATFORM DOCS",
        what: "Approved tooling, read over MCP from the platform's own documentation.",
        why: "An allow-list that is derived cannot drift from what the platform actually approves. A copied one always does."
      },
      {
        id: "team",
        tone: "cyan",
        label: "TEAM CONVENTIONS",
        what: "Required test layers, repository shape, the local run.",
        why: "The conventions a new joiner normally absorbs by osmosis, made executable instead."
      },
      {
        id: "meta",
        tone: "pink",
        label: "META-HARNESS",
        what: "Builds a harness per team and per platform.",
        why: "One governed workspace is a demo. Every team having one is a platform, and that is the difference this box makes."
      },
      {
        id: "regen",
        tone: "purple",
        label: "WHEN A STANDARD CHANGES",
        what: "Regenerate the harnesses; do not re-educate the teams.",
        why: "The usual model costs an announcement, a wiki edit, a quarter of drift and a follow-up audit. This one costs a rebuild."
      }
    ]
  };
  const SpecDevX = ({ spec }) => /* @__PURE__ */ React.createElement(SpecPageShell, { spec }, /* @__PURE__ */ React.createElement("style", { dangerouslySetInnerHTML: { __html: CSS } }), /* @__PURE__ */ React.createElement(SpecSection, { title: "The promotion border", hint: "one visual, and it is the argument" }, /* @__PURE__ */ React.createElement(SpecSignature, { spec }), /* @__PURE__ */ React.createElement("p", { style: { maxWidth: "74ch", marginTop: 6, fontSize: 13, lineHeight: 1.7, color: "var(--text-mute)" } }, "Nothing in that picture is a measurement, and that is deliberate. A percentage on this page would be a number a reader cannot check; a border with a named mechanism on it is a claim they can argue with. The second kind is worth more.")), /* @__PURE__ */ React.createElement(SpecSection, { title: "Why there is a gate on every repo", hint: "the harness, and the thing that builds harnesses" }, /* @__PURE__ */ React.createElement("p", { className: "spec-standfirst" }, "A platform team that reviews every change is a queue with a nicer name. Every deterministic question \u2014 is this library approved, are the right test layers here, is this deployment shaped correctly \u2014 was being answered by a serial human round trip, which caps throughput at specialist availability rather than at typing speed."), /* @__PURE__ */ React.createElement("p", { style: { maxWidth: "74ch", fontSize: 13.5, lineHeight: 1.7, color: "var(--text-dim)" } }, "So the standards moved into the workspace. Following them became the shape of the environment rather than something a person remembers, and the checks that decide conformance run at the borders above. What is left for a human reviewer is the design \u2014 the only part that needed one."), /* @__PURE__ */ React.createElement(CardGrid, { data: HARNESS_CARDS })), /* @__PURE__ */ React.createElement(SpecSection, { title: "The other half of the day", hint: "local development against systems you cannot run" }, /* @__PURE__ */ React.createElement("p", { style: { maxWidth: "74ch", fontSize: 13.5, lineHeight: 1.7, color: "var(--text-dim)" } }, "Gates decide what leaves the laptop. The rest of a developer's day is spent on the laptop, and in an enterprise most of what their service talks to cannot run there: core banking, payment rails, entitlement and SSO, a vendor API with a quarterly sandbox. The usual answer is a shared environment, which turns a development loop into a booking system."), /* @__PURE__ */ React.createElement(
    VizFrame,
    {
      title: "High-fidelity mocks for the systems that cannot come to the laptop",
      note: "The point of fidelity is that the mock is worth developing against: same contract, same error taxonomy, same latency shape, same awkward pagination. A mock that only returns the happy path moves the discovery of the real behaviour later, which is the opposite of the goal.",
      source: "Mechanism only. No timing, throughput or environment-cost figure is attached to this panel \u2014 none has been measured for publication."
    },
    /* @__PURE__ */ React.createElement("div", { className: "specdx-swap" }, /* @__PURE__ */ React.createElement("div", { className: "specdx-col" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-col-h" }, "Shared environment"), /* @__PURE__ */ React.createElement("ul", null, /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "01"), /* @__PURE__ */ React.createElement("span", null, "A feature waits on an environment booking, and two developers share one set of test data.")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "02"), /* @__PURE__ */ React.createElement("span", null, "Test state is other people's. Reproducing a bug means asking who else was in there.")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "03"), /* @__PURE__ */ React.createElement("span", null, "The integration contract is discovered in the shared environment, which is the most expensive place to discover it.")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "04"), /* @__PURE__ */ React.createElement("span", null, "Nothing runs offline, so the loop is only as fast as the slowest dependency's sandbox.")))), /* @__PURE__ */ React.createElement("div", { className: "specdx-col is-now" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-col-h" }, "Mocked at fidelity, on the laptop"), /* @__PURE__ */ React.createElement("ul", null, /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "01"), /* @__PURE__ */ React.createElement("span", null, "The whole dependency graph starts locally, isolated per developer and per agent.")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "02"), /* @__PURE__ */ React.createElement("span", null, "Test state is owned and disposable: seed it, break it, throw it away.")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "03"), /* @__PURE__ */ React.createElement("span", null, "Failure modes \u2014 timeouts, partial responses, the vendor's favourite 4xx \u2014 are things you can ask for on purpose.")), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("span", null, "04"), /* @__PURE__ */ React.createElement("span", null, "The loop is a process restart, which is what makes an agent-driven inner loop viable at all.")))))
  ), /* @__PURE__ */ React.createElement("p", { style: { maxWidth: "74ch", fontSize: 13, lineHeight: 1.7, color: "var(--text-mute)" } }, "The honest caveat, because it is the first thing a good engineer will ask: a mock is a claim about somebody else's system, and claims drift. That is precisely what the contract-test layer at border\xA02 is for. The local experience is only trustworthy because something deterministic is checking, on every promotion, that the mock still tells the truth \u2014 which is why these two sections are one idea and not two.")), /* @__PURE__ */ React.createElement(SpecSection, { title: "The number this page will not draw", hint: "and why the figures below are marked" }, /* @__PURE__ */ React.createElement("div", { className: "specdx-refuse" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-refuse-n" }, /* @__PURE__ */ React.createElement("span", { className: "specdx-refuse-v" }, "3\u20135\xD7"), /* @__PURE__ */ React.createElement(PlaceholderBadge, { label: "Draft \u2014 invented", title: "Placeholder \u2014 a draft figure, not a verified or published number." })), /* @__PURE__ */ React.createElement("p", null, "That is the throughput multiplier this work is worth in conversation: what a small team can carry once the deterministic questions stop being answered by people. It is written here as a sentence, at the size of a sentence, and it is not a chart \u2014 which is the entire point of this section."), /* @__PURE__ */ React.createElement("p", null, "An earlier version of this site shipped a smooth spend-down curve against a smooth workload-up curve. Neither line had been measured. It looked exactly like what a made-up chart looks like, it read as made up, and it was deleted. A chart on a r\xE9sum\xE9 is a claim, and an unsourced claim is worth less than no claim: it invites the reader to wonder what else here was drawn rather than measured."), /* @__PURE__ */ React.createElement("p", null, "The lead-time figure in the next section has the same shape of problem. ", /* @__PURE__ */ React.createElement("b", null, "6\u201312 weeks to days"), " ", /* @__PURE__ */ React.createElement(PlaceholderBadge, null), " is claimed on the First Citizens role and is very probably right \u2014 but the delivery record that would demonstrate it belongs to a bank and is not Geoff's to publish. So it stays marked, and the weight of this page rests on the border diagram instead: what was built, and the class of mistake it structurally prevents. That is checkable against the mechanism rather than against a spreadsheet nobody can show you."), /* @__PURE__ */ React.createElement("p", { className: "specdx-refuse-src" }, "PLACEHOLDER \u2014 the 3\u20135\xD7 multiplier is invented for this draft and no delivery record backs it. It appears here as prose precisely because it does not deserve a chart. Every figure in the sections below carries its own provenance line for the same reason."))));
  window.DevXSignature = DevXSignature;
  window.SpecDevX = SpecDevX;
})();
