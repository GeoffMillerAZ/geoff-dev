// Code generated by tools/jsxbuild from SpecCICD.jsx. DO NOT EDIT.
(function() {
  const { useState } = React;
  const SpecPageShell = window.SpecPageShell;
  const SpecSection = window.SpecSection;
  const SpecSignature = window.SpecSignature;
  const go = (id) => window.GO && window.GO("specializations", { specId: id });
  const CSS = `
.speccc-ctl { display:flex; flex-wrap:wrap; gap:8px; align-items:center; margin:0 0 14px; }
.speccc-ctl-label { font-family:var(--font-mono); font-size:10px; letter-spacing:.08em;
  text-transform:uppercase; color:var(--text-dim); margin-right:2px; }
.speccc-sw { display:inline-flex; border:1px solid var(--line); border-radius:999px; padding:2px; gap:2px;
  background:var(--bg-1); }
.speccc-sw button { font-family:var(--font-mono); font-size:11px; line-height:1; padding:7px 13px;
  border:0; border-radius:999px; background:transparent; color:var(--text-dim); cursor:pointer; }
.speccc-sw button:hover { color:var(--text); }
.speccc-sw button.is-on { background:var(--bg-3); color:var(--text);
  box-shadow: inset 0 0 0 1px color-mix(in oklab, var(--spec-tone, var(--accent)) 45%, var(--line)); }
.speccc-sw button:focus-visible { outline:2px solid var(--spec-tone, var(--accent)); outline-offset:2px; }

.speccc-toggle { font-family:var(--font-mono); font-size:11px; line-height:1; padding:8px 13px;
  border:1px solid var(--line); border-radius:999px; background:var(--bg-1); color:var(--text-dim);
  cursor:pointer; }
.speccc-toggle:hover { color:var(--text); }
.speccc-toggle.is-on { color:var(--text);
  border-color: color-mix(in oklab, var(--spec-tone, var(--accent)) 55%, var(--line)); }
.speccc-toggle:focus-visible { outline:2px solid var(--spec-tone, var(--accent)); outline-offset:2px; }

.speccc-cap { margin:12px 0 0; font-size:13.5px; line-height:1.62; color:var(--text-dim); max-width:74ch; }
.speccc-cap b { color:var(--text); font-weight:600; }
.speccc-cap-mono { font-family:var(--font-mono); font-size:11px; letter-spacing:.04em;
  color:var(--text-dim); display:block; margin-top:6px; }

.speccc-illus { display:inline-flex; align-items:center; gap:6px; font-family:var(--font-mono);
  font-size:9.5px; letter-spacing:.1em; text-transform:uppercase; color:var(--text-dim);
  border:1px dashed var(--line); border-radius:999px; padding:3px 9px; }

.speccc-legend { display:grid; gap:10px; margin:16px 0 0;
  grid-template-columns: repeat(auto-fit, minmax(min(230px,100%), 1fr)); }
.speccc-fam { border:1px solid var(--line); border-radius:var(--rad); padding:10px 12px; background:var(--bg-1); }
.speccc-fam-h { font-family:var(--font-mono); font-size:9.5px; letter-spacing:.08em; text-transform:uppercase;
  color:var(--text-dim); display:block; margin-bottom:8px; }
.speccc-row { display:flex; align-items:baseline; gap:8px; margin-top:6px; }
/* Same split as the tiles: the border carries the class colour, the glyph wears
   a text token. currentColor would have painted both, and two of the six tones
   measure under 4.5:1 as small text on the light panel. */
.speccc-key { flex:none; width:26px; height:16px; border-radius:3px;
  border:1.4px solid currentColor; color:var(--text);
  font-family:var(--font-mono); font-size:8.5px; display:inline-flex; align-items:center;
  justify-content:center; }
.speccc-row-n { font-family:var(--font-mono); font-size:11.5px; color:var(--text); }
.speccc-row-w { font-size:12px; color:var(--text-dim); line-height:1.5; }

.speccc-split { display:grid; gap:12px; margin-top:4px;
  grid-template-columns: repeat(auto-fit, minmax(min(260px,100%), 1fr)); }
.speccc-card { border:1px solid var(--line); border-radius:var(--rad); background:var(--bg-1);
  padding:14px 15px; display:flex; flex-direction:column; gap:8px; }
.speccc-card-k { font-family:var(--font-mono); font-size:9.5px; letter-spacing:.1em; text-transform:uppercase;
  color:var(--text-dim); }
.speccc-card.is-built .speccc-card-k { color: var(--spec-tone, var(--accent)); }
.speccc-card h3 { margin:0; font-size:14.5px; line-height:1.35; color:var(--text); font-weight:600; }
.speccc-card p { margin:0; font-size:13px; line-height:1.62; color:var(--text-dim); }
.speccc-card code { font-family:var(--font-mono); font-size:11.5px; color:var(--text-dim); }

.speccc-p { max-width:74ch; font-size:14px; line-height:1.72; color:var(--text-dim); }
.speccc-p + .speccc-p { margin-top:14px; }
.speccc-p strong { color:var(--text); font-weight:600; }
.speccc-src { max-width:74ch; margin-top:12px; font-family:var(--font-mono); font-size:10.5px;
  line-height:1.6; color:var(--text-dim); border-left:2px solid var(--line); padding-left:10px; }

.speccc-tile, .speccc-cell, .speccc-edge, .speccc-lin { transition: transform 560ms cubic-bezier(.2,.72,.2,1),
  opacity 320ms linear; }
@media (prefers-reduced-motion: reduce) {
  .speccc-tile, .speccc-cell, .speccc-edge, .speccc-lin { transition: none; }
}
.speccc-hit { cursor:pointer; }
.speccc-hit:focus-visible rect { stroke-width:2.4; }
`;
  const EXEC = [
    {
      id: "engine-pool",
      code: "EP",
      tone: "var(--spec-tone-cyan, var(--neon-cyan))",
      fam: 0,
      what: "The shared build engine. Content-addressed inputs, warm workers, and the default for anything whose inputs are already trusted."
    },
    {
      id: "ci-warm",
      code: "CW",
      tone: "var(--spec-tone-blue, var(--neon-blue))",
      fam: 0,
      what: "Capacity held on purpose, sized to the p50 of the first wave. It exists so the first job of a run does not wait for a node to boot."
    },
    {
      id: "trusted-spot",
      code: "TS",
      tone: "var(--spec-tone-purple, var(--neon-purple))",
      fam: 1,
      what: "Trusted work that can be killed and restarted without anybody noticing, because the step is re-runnable by construction."
    },
    {
      id: "vcluster",
      code: "VC",
      tone: "var(--spec-tone-pink, var(--neon-pink))",
      fam: 2,
      what: "A virtual control plane, for the tests that need to be a cluster admin somewhere and must not be one here."
    },
    {
      id: "sandbox-gvisor",
      code: "GV",
      tone: "var(--spec-tone-red, var(--neon-red))",
      fam: 3,
      what: "A syscall-intercepting sandbox. Where a fork's code runs, chosen because the input is untrusted and not because a node had room."
    },
    {
      id: "scan-isolated",
      code: "SC",
      tone: "var(--spec-tone-red, var(--neon-red))",
      tex: true,
      fam: 3,
      what: "Scanners, given no credentials and no egress. The one class whose job is to read a thing that may be hostile and report on it."
    }
  ];
  const FAMS = [
    "Trusted input, shared capacity",
    "Trusted input, capacity that can be taken away",
    "Isolated by tenancy",
    "Untrusted input, isolated by construction"
  ];
  const EX = {};
  EXEC.forEach((e) => {
    EX[e.id] = e;
  });
  const RANKS = [
    ["engine-pool", "engine-pool", "scan-isolated", "engine-pool", "engine-pool", "scan-isolated"],
    [
      "engine-pool",
      "engine-pool",
      "engine-pool",
      "sandbox-gvisor",
      "trusted-spot",
      "trusted-spot",
      "trusted-spot",
      "engine-pool",
      "engine-pool",
      "sandbox-gvisor",
      "trusted-spot",
      "trusted-spot"
    ],
    [
      "ci-warm",
      "ci-warm",
      "trusted-spot",
      "vcluster",
      "ci-warm",
      "ci-warm",
      "sandbox-gvisor",
      "trusted-spot",
      "ci-warm",
      "ci-warm",
      "trusted-spot",
      "vcluster",
      "sandbox-gvisor",
      "trusted-spot"
    ],
    [
      "engine-pool",
      "scan-isolated",
      "vcluster",
      "ci-warm",
      "engine-pool",
      "scan-isolated",
      "vcluster",
      "ci-warm",
      "engine-pool",
      "scan-isolated"
    ]
  ];
  const HOT = { "0:0": 1, "1:0": 1, "1:6": 1, "2:0": 1, "2:12": 1, "3:0": 1 };
  const TW = 31, TH = 14, PITCH = 38;
  const RANK_Y = [158, 194, 230, 266];
  const SHELF_Y = [76, 96, 116];
  const SHELF_X0 = 156, SHELF_PER = 12;
  const TILES = (function() {
    const out = [];
    let proven = 0;
    RANKS.forEach((row, r) => {
      const x0 = 380 - (row.length * PITCH - (PITCH - TW)) / 2;
      row.forEach((cls, i) => {
        const hot = !!HOT[r + ":" + i];
        let sx = 0, sy = 0;
        if (!hot) {
          const k = proven++;
          sx = SHELF_X0 + k % SHELF_PER * PITCH;
          sy = SHELF_Y[Math.floor(k / SHELF_PER)];
        }
        out.push({
          key: r + ":" + i,
          r,
          i,
          cls,
          hot,
          px: Math.round(x0 + i * PITCH),
          py: RANK_Y[r],
          sx,
          sy
        });
      });
    });
    return out;
  })();
  const NODE_FREE = [6, 8, 5, 7, 9, 7];
  const NODE_X0 = 26, NODE_W = 109, NODE_GAP = 11, NODE_Y = 318, NODE_H = 106;
  const CELL_W = 31, CELL_H = 14, CELL_DX = 34, CELL_DY = 16, GRID_Y = 324;
  const cellXY = (node, k) => ({
    x: NODE_X0 + node * (NODE_W + NODE_GAP) + 5 + k % 3 * CELL_DX,
    y: GRID_Y + Math.floor(k / 3) * CELL_DY
  });
  const FREE_CELLS = (function() {
    const out = [];
    NODE_FREE.forEach((free, n) => {
      for (let k = 0; k < free; k++) out.push(Object.assign({ node: n }, cellXY(n, k)));
    });
    return out;
  })();
  const RESIDENTS = (function() {
    const out = [];
    NODE_FREE.forEach((free, n) => {
      for (let k = free; k < 18; k++) out.push(Object.assign({ node: n }, cellXY(n, k)));
    });
    return out;
  })();
  const EDGES = (function() {
    const out = [];
    for (let r = 1; r < RANKS.length; r++) {
      const prev = TILES.filter((t) => t.r === r - 1);
      TILES.filter((t) => t.r === r).forEach((t, i) => {
        const p = prev[i % prev.length];
        out.push({ key: t.key, from: p, to: t });
      });
    }
    return out;
  })();
  const CICDSignature = () => {
    const [mode, setMode] = useState("first");
    const [plan, setPlan] = useState(false);
    const changed = mode === "changed";
    const lit = changed ? 6 : 42;
    const litCells = FREE_CELLS.slice(0, lit);
    const hotTiles = TILES.filter((t) => t.hot);
    const hotX = (function() {
      const out = {};
      RANK_Y.forEach((y, r) => {
        const row = hotTiles.filter((t) => t.r === r);
        const w = row.length * PITCH - (PITCH - TW);
        const x0 = 380 - w / 2;
        row.forEach((t, i) => {
          out[t.key] = Math.round(x0 + i * PITCH);
        });
      });
      return out;
    })();
    const posOf = (t) => changed ? t.hot ? { x: hotX[t.key], y: t.py } : { x: t.sx, y: t.sy } : { x: t.px, y: t.py };
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "speccc-ctl" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-ctl-label" }, "The run is"), /* @__PURE__ */ React.createElement("span", { className: "speccc-sw", role: "group", "aria-label": "Which run is drawn" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: changed ? "" : "is-on",
        "aria-pressed": !changed,
        onClick: () => setMode("first")
      },
      "first run"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: changed ? "is-on" : "",
        "aria-pressed": changed,
        onClick: () => setMode("changed")
      },
      "one file changed"
    )), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        className: "speccc-toggle" + (plan ? " is-on" : ""),
        "aria-pressed": plan,
        onClick: () => setPlan(!plan)
      },
      plan ? "hide the plan at t=0" : "show the plan at t=0"
    )), /* @__PURE__ */ React.createElement("div", { className: "dgx" }, /* @__PURE__ */ React.createElement(
      "svg",
      {
        viewBox: "0 0 760 470",
        xmlns: "http://www.w3.org/2000/svg",
        role: "img",
        "aria-label": changed ? "One file has changed. Thirty-six of the forty-two tiles in the plan resolve at the top of the vendor runner strip against the run that already proved them, and only six descend into the cluster. Five of the six cluster nodes are drawn with no CI work in them at all." : "A first run. A deliberately thin vendor runner strip at the top carries five duties and no compute. Below it a forty-two node plan descends, each tile coloured and coded by its execution class, into the free cells between workloads the cluster is already running."
      },
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "26",
          y: "22",
          fontFamily: "JetBrains Mono",
          fontSize: "9.5",
          fill: "var(--text-dim)",
          letterSpacing: ".08em"
        },
        "THE CI-HOST PORT \u2014 FIVE DUTIES, NONE OF THEM COMPUTE"
      ),
      /* @__PURE__ */ React.createElement(
        "rect",
        {
          x: "26",
          y: "36",
          width: "708",
          height: "24",
          rx: "4",
          fill: "var(--bg-2)",
          stroke: "var(--line)",
          strokeWidth: "1"
        }
      ),
      /* @__PURE__ */ React.createElement("text", { x: "38", y: "52", fontFamily: "JetBrains Mono", fontSize: "10", fill: "var(--text-dim)" }, "vendor runner"),
      ["trigger", "log relay", "status fan-out", "artifact re-upload", "cancel relay"].map((d, i) => {
        const x = 176 + i * 112;
        return /* @__PURE__ */ React.createElement("g", { key: d }, /* @__PURE__ */ React.createElement("line", { x1: x - 12, x2: x - 12, y1: "36", y2: "60", stroke: "var(--line-soft)", strokeWidth: "1" }), /* @__PURE__ */ React.createElement("text", { x, y: "52", fontFamily: "JetBrains Mono", fontSize: "9", fill: "var(--text-dim)" }, d));
      }),
      !changed && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "734",
          y: "74",
          textAnchor: "end",
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: "var(--text-dim)"
        },
        "the strip is this thin on purpose \u2014 it is a trigger, not a computer"
      ),
      !changed && TILES.filter((t) => t.r === 0).map((t) => {
        const tx = t.px + TW / 2;
        return /* @__PURE__ */ React.createElement(
          "path",
          {
            key: "fan" + t.key,
            className: "speccc-edge",
            d: "M380 60 C380 112 " + tx + " 106 " + tx + " " + t.py,
            fill: "none",
            stroke: "var(--text-dim)",
            strokeWidth: "0.75",
            opacity: "0.68"
          }
        );
      }),
      !changed && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "26",
          y: "120",
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: "var(--text-dim)"
        },
        "one trigger, and the whole plan below it"
      ),
      changed && /* @__PURE__ */ React.createElement("g", { className: "speccc-lin" }, /* @__PURE__ */ React.createElement(
        "path",
        {
          d: "M56 70 L56 132",
          fill: "none",
          stroke: "var(--text-mute)",
          strokeWidth: "1",
          strokeDasharray: "3 4"
        }
      ), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "56",
          y: "146",
          textAnchor: "middle",
          fontFamily: "JetBrains Mono",
          fontSize: "8.5",
          fill: "var(--text-dim)"
        },
        "run N\u22121"
      ), SHELF_Y.map((y, i) => /* @__PURE__ */ React.createElement(
        "path",
        {
          key: i,
          d: "M56 " + (y + 7) + " L" + (SHELF_X0 - 6) + " " + (y + 7),
          fill: "none",
          stroke: "var(--text-mute)",
          strokeWidth: "1",
          strokeDasharray: "3 4"
        }
      )), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: SHELF_X0,
          y: "66",
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: "var(--text-dim)"
        },
        "already proven \u2014 each one linked to the run that proved it"
      )),
      EDGES.map((e) => {
        const dim = changed && !(e.from.hot && e.to.hot);
        const a = posOf(e.from), b = posOf(e.to);
        return /* @__PURE__ */ React.createElement(
          "path",
          {
            key: e.key,
            className: "speccc-edge",
            d: "M" + (a.x + TW / 2) + " " + (a.y + TH) + " C" + (a.x + TW / 2) + " " + (a.y + TH + 16) + " " + (b.x + TW / 2) + " " + (b.y - 16) + " " + (b.x + TW / 2) + " " + b.y,
            fill: "none",
            stroke: "var(--text-dim)",
            strokeWidth: "0.75",
            opacity: dim ? 0 : 0.68
          }
        );
      }),
      TILES.map((t) => {
        const e = EX[t.cls];
        const dock = changed && !t.hot;
        const pos = posOf(t);
        const x = pos.x, y = pos.y;
        return /* @__PURE__ */ React.createElement(
          "g",
          {
            key: t.key,
            className: "speccc-tile",
            style: { transform: "translate(" + x + "px," + y + "px)" }
          },
          /* @__PURE__ */ React.createElement(
            "rect",
            {
              width: TW,
              height: TH,
              rx: "3",
              fill: dock ? "none" : "var(--bg-1)",
              stroke: e.tone,
              strokeWidth: dock ? 1 : 1.4,
              strokeDasharray: dock ? "3 3" : e.tex ? "6 3" : void 0,
              opacity: dock ? 0.55 : 1
            }
          ),
          /* @__PURE__ */ React.createElement(
            "text",
            {
              x: TW / 2,
              y: "10",
              textAnchor: "middle",
              fontFamily: "JetBrains Mono",
              fontSize: "8.5",
              fill: "var(--text)",
              opacity: dock ? 0.55 : 1
            },
            e.code
          )
        );
      }),
      plan && /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, RANK_Y.map((y, r) => {
        const n = changed ? hotTiles.filter((t) => t.r === r).length : RANKS[r].length;
        if (!n) return null;
        const w = n * PITCH - (PITCH - TW);
        const x = 380 - w / 2;
        return /* @__PURE__ */ React.createElement("g", { key: r }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: x - 7,
            y: y - 6,
            width: w + 14,
            height: TH + 12,
            rx: "6",
            fill: "none",
            stroke: "var(--spec-tone, var(--accent))",
            strokeWidth: "1",
            strokeDasharray: "5 4",
            opacity: "0.75"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: x + w + 12,
            y: y + 11,
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: "var(--spec-tone, var(--accent))",
            opacity: "0.9"
          },
          "r",
          r
        ));
      })),
      (function() {
        const tail = hotTiles.filter((t) => t.r === 3)[0];
        const ox = changed && tail ? posOf(tail).x + TW / 2 : 380;
        return (changed ? [0] : [0, 1, 2, 3, 4, 5]).map((n) => {
          const cx = NODE_X0 + n * (NODE_W + NODE_GAP) + NODE_W / 2;
          return /* @__PURE__ */ React.createElement(
            "path",
            {
              key: n,
              className: "speccc-edge",
              d: "M" + ox + " 288 C" + ox + " 302 " + cx + " 302 " + cx + " 314",
              fill: "none",
              stroke: "var(--text-dim)",
              strokeWidth: "0.9",
              opacity: "0.72"
            }
          );
        });
      })(),
      plan && /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "26",
          y: "300",
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          pointerEvents: "none",
          fill: "var(--spec-tone, var(--accent))",
          stroke: "var(--bg)",
          strokeWidth: "3",
          paintOrder: "stroke"
        },
        "every rank\u2019s width is readable at t=0 \u2014 the shape of the descent, before the first node runs"
      ),
      NODE_FREE.map((free, n) => {
        const nx = NODE_X0 + n * (NODE_W + NODE_GAP);
        return /* @__PURE__ */ React.createElement("g", { key: n }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: nx,
            y: NODE_Y,
            width: NODE_W,
            height: NODE_H,
            rx: "6",
            fill: "var(--bg-1)",
            stroke: "var(--line)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: nx + NODE_W / 2,
            y: NODE_Y + NODE_H + 14,
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: "var(--text-dim)"
          },
          "node ",
          n + 1
        ));
      }),
      RESIDENTS.map((c, i) => /* @__PURE__ */ React.createElement(
        "rect",
        {
          key: "res" + i,
          x: c.x,
          y: c.y,
          width: CELL_W,
          height: CELL_H,
          rx: "2",
          fill: "var(--text-faint)",
          fillOpacity: "0.45",
          stroke: "var(--text-faint)",
          strokeWidth: "0.5",
          strokeOpacity: "0.6"
        }
      )),
      FREE_CELLS.map((c, i) => /* @__PURE__ */ React.createElement(
        "rect",
        {
          key: "free" + i,
          x: c.x,
          y: c.y,
          width: CELL_W,
          height: CELL_H,
          rx: "2",
          fill: "none",
          stroke: "var(--line)",
          strokeWidth: "0.75",
          strokeDasharray: "2 3"
        }
      )),
      litCells.map((c, i) => {
        const t = changed ? hotTiles[i] : TILES[i];
        if (!t) return null;
        const e = EX[t.cls];
        return /* @__PURE__ */ React.createElement(
          "rect",
          {
            key: "lit" + i,
            className: "speccc-cell",
            x: c.x,
            y: c.y,
            width: CELL_W,
            height: CELL_H,
            rx: "2",
            fill: e.tone,
            fillOpacity: "0.16",
            stroke: e.tone,
            strokeWidth: "1.2",
            strokeDasharray: e.tex ? "6 3" : void 0
          }
        );
      }),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "26",
          y: "462",
          fontFamily: "JetBrains Mono",
          fontSize: "9.5",
          fill: "var(--text-dim)",
          letterSpacing: ".08em"
        },
        "COMPUTE YOU ARE ALREADY PAYING FOR"
      ),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "734",
          y: "462",
          textAnchor: "end",
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: "var(--text-dim)"
        },
        changed ? "five of six nodes are drawn with no CI work in them at all" : "the faded cells are what the cluster already runs; CI takes the rest"
      )
    )), /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, changed ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("b", null, "6 descend. 36 are already proven."), " One file moved, so thirty-six of the tiles never leave the top of the strip: each resolves against the run that already proved it, and the link back to that run is the whole of its execution. Only the subgraph the change actually reaches falls. Caches make work faster; a proof makes the work unnecessary, and the emptiest thing on this page \u2014 five cluster nodes with nothing of mine in them \u2014 is the strongest cost lever in CI drawn as an absence.") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("b", null, "42 nodes execute."), " The work left the vendor runner, which is doing the five things it is actually good at, and landed in the gaps between workloads the cluster was already running. Nothing was bought to make that happen. And notice what decided where each tile went: a fork\u2019s untrusted input descends into ", /* @__PURE__ */ React.createElement("code", null, "sandbox-gvisor"), " ", "because of what it ", /* @__PURE__ */ React.createElement("b", null, "is"), ", not because that node had room. Routing is a type."), /* @__PURE__ */ React.createElement("span", { className: "speccc-cap-mono" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-illus" }, "Illustrative"), " ", "42 tiles are drawn here and 42 tiles can be counted off the picture. The number is the drawing\u2019s. There is no measured DAG behind this page and no wall-clock anywhere on it.")), plan && /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, /* @__PURE__ */ React.createElement("b", null, "Pre-scaling."), " A pipeline enumerates itself before it executes \u2014 the jobs, their order, and the class of machine each one needs are all readable at t=0. So the width of every rank is known before the first node runs, which means capacity decisions get made before they are needed instead of half-way through a run, when the only remaining move is to wait. I have run it this way; the obvious implementation, holding the space with pause pods, is the one I would no longer reach for \u2014 Karpenter shipping", " ", /* @__PURE__ */ React.createElement("code", null, "CapacityBuffer"), " superseded it, and a first-class buffer beats a workload impersonating one. No time saving is claimed for this, here or anywhere else on the page."), /* @__PURE__ */ React.createElement("div", { className: "speccc-legend" }, FAMS.map((f, fi) => /* @__PURE__ */ React.createElement("div", { key: fi, className: "speccc-fam" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-fam-h" }, f), EXEC.filter((e) => e.fam === fi).map((e) => /* @__PURE__ */ React.createElement("div", { key: e.id, className: "speccc-row" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-key", style: {
      borderColor: e.tone,
      borderStyle: e.tex ? "dashed" : "solid"
    } }, e.code), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { className: "speccc-row-n" }, "#", e.id), " ", /* @__PURE__ */ React.createElement("span", { className: "speccc-row-w" }, e.what))))))), /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-cap-mono" }, "Six classes, five hues, and every tile carries its two-letter code \u2014 the two isolation classes share a hue because they are one family, and are told apart by texture and by the code rather than by colour. Nothing in this drawing is identified by colour alone.")));
  };
  const SVC = [
    { n: "gateway", u: 3 },
    { n: "auth", u: 2 },
    { n: "ledger", u: 2 },
    { n: "notify", u: 1 },
    { n: "search", u: 3 },
    { n: "billing", u: 2 },
    { n: "audit", u: 1 },
    { n: "sync", u: 2 },
    { n: "report", u: 3 },
    { n: "webhook", u: 1 },
    { n: "vault-proxy", u: 2 },
    { n: "admin", u: 2 }
  ];
  const ENVS = ["dev", "test", "stage", "prod"];
  const C = 9, CG = 1, COLS = 4, ROWS = 6;
  const tileW = (u) => u * C + (u - 1) * CG;
  const packNodes = (list) => {
    const nodes = [];
    let node = null, row = 0, used = 0;
    const fresh = () => {
      node = { tiles: [], holes: [] };
      nodes.push(node);
      row = 0;
      used = 0;
    };
    fresh();
    list.forEach((t) => {
      while (used + t.u > COLS) {
        for (let k = used; k < COLS; k++) node.holes.push({ r: row, c: k });
        row++;
        used = 0;
        if (row >= ROWS) fresh();
      }
      node.tiles.push({ r: row, c: used, u: t.u, n: t.n, env: t.env });
      used += t.u;
    });
    for (let k = used; k < COLS; k++) node.holes.push({ r: row, c: k });
    for (let r = row + 1; r < ROWS; r++)
      for (let k = 0; k < COLS; k++) node.holes.push({ r, c: k });
    return nodes;
  };
  const ALL = [];
  ENVS.forEach((env) => SVC.forEach((s) => ALL.push({ n: s.n, u: s.u, env })));
  const LIVE = ALL.filter((t) => t.env === "prod" || t.env === "dev" && t.u === 3);
  const IDLE = ALL.filter((t) => LIVE.indexOf(t) < 0);
  const PACK2 = packNodes(ALL);
  const PACK3 = packNodes(LIVE);
  const Hatch = () => /* @__PURE__ */ React.createElement("defs", null, /* @__PURE__ */ React.createElement(
    "pattern",
    {
      id: "speccc-hatch",
      width: "6",
      height: "6",
      patternUnits: "userSpaceOnUse",
      patternTransform: "rotate(45)"
    },
    /* @__PURE__ */ React.createElement("line", { x1: "0", y1: "0", x2: "0", y2: "6", stroke: "var(--text-faint)", strokeWidth: "1.4", opacity: "0.5" })
  ));
  const CICDSubstrates = () => {
    const [beat, setBeat] = useState(1);
    const [hov, setHov] = useState(null);
    const P1_CAP = 5;
    const P1_NW = P1_CAP * C + (P1_CAP - 1) * CG + 6;
    const P1_X = 28, P1_Y = 60, P1_DX = 59, P1_DY = 22;
    const NW = COLS * C + (COLS - 1) * CG + 6;
    const NH = ROWS * C + (ROWS - 1) * CG + 6;
    const P2_X = 28, P2_Y = 186, P2_GAP = 10;
    const P3_NX = 28, P3_NY = 320, P3_TY = 290;
    const ANCHOR_X = 596;
    const idlePos = (function() {
      const half = Math.ceil(IDLE.length / 2), rows = [IDLE.slice(0, half), IDLE.slice(half)];
      const out = [];
      rows.forEach((row, ri) => {
        let x = 62;
        row.forEach((t, i) => {
          out.push({ t, key: ri + ":" + i, x, y: P3_TY + ri * 13 });
          x += tileW(t.u) + 4;
        });
      });
      return out;
    })();
    const ANCHORS = [
      { id: "dns", label: "DNS", y: 326 },
      { id: "trust", label: "trust", y: 346 },
      { id: "route", label: "route", y: 366 }
    ];
    const head = (y, n, title, sub) => /* @__PURE__ */ React.createElement("g", null, /* @__PURE__ */ React.createElement(
      "text",
      {
        x: "26",
        y,
        fontFamily: "JetBrains Mono",
        fontSize: "9.5",
        fill: "var(--text-dim)",
        letterSpacing: ".08em"
      },
      n
    ), /* @__PURE__ */ React.createElement("text", { x: "52", y, fontFamily: "JetBrains Mono", fontSize: "10.5", fill: "var(--text)" }, title), /* @__PURE__ */ React.createElement("text", { x: "52", y: y + 13, fontFamily: "JetBrains Mono", fontSize: "9", fill: "var(--text-dim)" }, sub));
    const nodeBox = (x, y, key, accent, dash) => /* @__PURE__ */ React.createElement(
      "rect",
      {
        key,
        x,
        y,
        width: NW,
        height: NH,
        rx: "4",
        fill: "var(--bg-1)",
        stroke: accent || "var(--line)",
        strokeWidth: accent ? 1.6 : 1,
        strokeDasharray: dash ? "5 4" : void 0
      }
    );
    const packedNode = (pack, x, y, keyp) => /* @__PURE__ */ React.createElement("g", { key: keyp }, pack.holes.map((h, i) => /* @__PURE__ */ React.createElement(
      "rect",
      {
        key: "h" + i,
        x: x + 3 + h.c * (C + CG),
        y: y + 3 + h.r * (C + CG),
        width: C,
        height: C,
        fill: "url(#speccc-hatch)",
        opacity: beat === 1 ? 1 : 0.45
      }
    )), pack.tiles.map((t, i) => /* @__PURE__ */ React.createElement(
      "rect",
      {
        key: "t" + i,
        x: x + 3 + t.c * (C + CG),
        y: y + 3 + t.r * (C + CG),
        width: tileW(t.u),
        height: C,
        rx: "1.5",
        fill: "var(--spec-tone, var(--accent))",
        fillOpacity: "0.28",
        stroke: "var(--spec-tone, var(--accent))",
        strokeWidth: "0.8"
      }
    )));
    const hovTile = hov ? idlePos.find((p) => p.key === hov) : null;
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "speccc-ctl" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-ctl-label" }, "Beat"), /* @__PURE__ */ React.createElement("span", { className: "speccc-sw", role: "group", "aria-label": "Which reading of the three panels" }, [[1, "the hatch collapses"], [2, "reachability"], [3, "the empty node"]].map((b) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: b[0],
        type: "button",
        className: beat === b[0] ? "is-on" : "",
        "aria-pressed": beat === b[0],
        onClick: () => setBeat(b[0])
      },
      b[1]
    )))), /* @__PURE__ */ React.createElement("div", { className: "dgx" }, /* @__PURE__ */ React.createElement(
      "svg",
      {
        viewBox: "0 0 760 410",
        xmlns: "http://www.w3.org/2000/svg",
        role: "img",
        "aria-label": "Three panels at one scale, showing the same twelve services in four environments. First, one host per service per environment: forty-eight nodes, each mostly hatched. Second, a shared cluster on static nodes: five larger nodes with the same work packed into them and a much smaller hatched margin. Third, a claim-gated and consolidating cluster: most services are drawn as grey outlines at zero replicas, the live ones pack into two nodes, and one node is deliberately empty and labelled as headroom."
      },
      /* @__PURE__ */ React.createElement(Hatch, null),
      head(
        36,
        "01",
        "one host per service, per environment",
        "12 services \xD7 4 environments \xB7 48 nodes drawn, each sized for its own peak. the hatching is what you are paying for."
      ),
      ALL.map((t, i) => {
        const r = Math.floor(i / 12), c = i % 12;
        const x = P1_X + c * P1_DX, y = P1_Y + r * P1_DY;
        return /* @__PURE__ */ React.createElement("g", { key: "p1" + i }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x,
            y,
            width: P1_NW,
            height: C + 6,
            rx: "3",
            fill: "var(--bg-1)",
            stroke: "var(--line)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: x + 3,
            y: y + 3,
            width: tileW(t.u),
            height: C,
            rx: "1.5",
            fill: "var(--spec-tone, var(--accent))",
            fillOpacity: "0.28",
            stroke: "var(--spec-tone, var(--accent))",
            strokeWidth: "0.8"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: x + 3 + tileW(t.u) + CG,
            y: y + 3,
            width: Math.max(0, P1_NW - 6 - tileW(t.u) - CG),
            height: C,
            fill: "url(#speccc-hatch)",
            opacity: beat === 1 ? 1 : 0.45
          }
        ));
      }),
      head(
        162,
        "02",
        "shared cluster, static nodes",
        "consolidation removed nodes. it did not remove idleness."
      ),
      PACK2.map((p, i) => {
        const x = P2_X + i * (NW + P2_GAP);
        return /* @__PURE__ */ React.createElement("g", { key: "p2" + i }, nodeBox(x, P2_Y, "b" + i), packedNode(p, x, P2_Y, "n" + i));
      }),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: P2_X + PACK2.length * (NW + P2_GAP) + 14,
          y: P2_Y + 26,
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: "var(--text-dim)"
        },
        "the width you just got back is consolidation"
      ),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: P2_X + PACK2.length * (NW + P2_GAP) + 14,
          y: P2_Y + 40,
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: "var(--text-dim)"
        },
        "\u2014 and the hatch inside the nodes is what it could not reach"
      ),
      head(
        268,
        "03",
        "claim-gated + consolidating",
        "most of the estate is present, correctly wired, and at zero replicas."
      ),
      idlePos.map((p) => {
        const on = hov === p.key;
        return /* @__PURE__ */ React.createElement(
          "g",
          {
            key: p.key,
            className: "speccc-hit",
            tabIndex: 0,
            role: "button",
            "aria-label": p.t.env + " " + p.t.n + ": declared, wired, at zero replicas. Show its reachability edges.",
            onMouseEnter: () => setHov(p.key),
            onMouseLeave: () => setHov(null),
            onFocus: () => setHov(p.key),
            onBlur: () => setHov(null)
          },
          /* @__PURE__ */ React.createElement(
            "rect",
            {
              x: p.x - 2,
              y: p.y - 3,
              width: tileW(p.t.u) + 4,
              height: C + 6,
              fill: "transparent"
            }
          ),
          /* @__PURE__ */ React.createElement(
            "rect",
            {
              x: p.x,
              y: p.y,
              width: tileW(p.t.u),
              height: C,
              rx: "1.5",
              fill: "none",
              pointerEvents: "none",
              stroke: on ? "var(--spec-tone, var(--accent))" : "var(--text-faint)",
              strokeWidth: on ? 1.6 : 1,
              opacity: beat === 2 ? 1 : 0.6
            }
          )
        );
      }),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "26",
          y: P3_TY + 8,
          fontFamily: "JetBrains Mono",
          fontSize: "8.5",
          fill: "var(--text-dim)"
        },
        "at 0"
      ),
      hovTile && /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, ANCHORS.map((a) => /* @__PURE__ */ React.createElement(
        "path",
        {
          key: a.id,
          d: "M" + (hovTile.x + tileW(hovTile.t.u)) + " " + (hovTile.y + C / 2) + " C" + (ANCHOR_X - 20) + " " + (hovTile.y + C / 2) + " " + (ANCHOR_X - 60) + " " + a.y + " " + (ANCHOR_X - 4) + " " + a.y,
          fill: "none",
          stroke: "var(--spec-tone, var(--accent))",
          strokeWidth: "1.2"
        }
      ))),
      ANCHORS.map((a) => /* @__PURE__ */ React.createElement("g", { key: a.id }, /* @__PURE__ */ React.createElement(
        "circle",
        {
          cx: ANCHOR_X,
          cy: a.y,
          r: "3",
          fill: "var(--spec-tone, var(--accent))",
          opacity: hovTile ? 1 : 0.4
        }
      ), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: ANCHOR_X + 10,
          y: a.y + 3.5,
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          fill: hovTile ? "var(--text)" : "var(--text-dim)"
        },
        a.label
      ))),
      /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "740",
          y: "392",
          textAnchor: "end",
          fontFamily: "JetBrains Mono",
          fontSize: "8.5",
          fill: hovTile ? "var(--spec-tone, var(--accent))" : "var(--text-dim)"
        },
        hovTile ? "reachability never changes with activation state" : "hover or tab any outline above"
      ),
      PACK3.map((p, i) => {
        const x = P3_NX + i * (NW + P2_GAP);
        return /* @__PURE__ */ React.createElement("g", { key: "p3" + i }, nodeBox(x, P3_NY, "b" + i), packedNode(p, x, P3_NY, "n" + i));
      }),
      (function() {
        const x = P3_NX + PACK3.length * (NW + P2_GAP);
        const hot = beat === 3;
        return /* @__PURE__ */ React.createElement("g", null, /* @__PURE__ */ React.createElement(
          "text",
          {
            x: x + NW + 18,
            y: P3_NY + 26,
            fontFamily: "JetBrains Mono",
            fontSize: "9",
            fill: "var(--text-dim)"
          },
          "this width came back too \u2014 but almost none of it from packing"
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: x + NW + 18,
            y: P3_NY + 40,
            fontFamily: "JetBrains Mono",
            fontSize: "9",
            fill: "var(--text-dim)"
          },
          "it came from the outlines above, which are not running"
        ), nodeBox(
          x,
          P3_NY,
          "hd",
          hot ? "var(--spec-tone, var(--accent))" : "var(--line)",
          true
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: x + NW / 2,
            y: P3_NY + NH + 13,
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "8.5",
            fill: hot ? "var(--spec-tone, var(--accent))" : "var(--text-dim)"
          },
          "headroom"
        ));
      })()
    )), beat === 1 && /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, /* @__PURE__ */ React.createElement("b", null, "Watch the hatched area collapse, then notice where the collapse came from."), " ", "Between panel 1 and panel 2 it is consolidation doing the work: fewer, larger nodes, the same declared requests packed against each other instead of each sitting alone on a host bought for its peak. That is real, and it is the second-order win. The larger collapse is between 2 and 3, and it is not a packing result at all \u2014 most of that estate is not running. Bin packing decides how much machine the running work needs. The claim model decides how much of the work runs. Only one of those two can take the number to zero, and it is not the packer."), beat === 2 && /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, /* @__PURE__ */ React.createElement("b", null, "Hover or tab to any grey outline."), " Its DNS record, its trust relationships and its routes are all still drawn, unbroken, while it is at zero replicas \u2014 because they were never a function of whether a pod existed. ", /* @__PURE__ */ React.createElement("b", null, "Reachability never changes with activation state."), " That is the property that makes the whole panel safe rather than clever: an environment that is off is not an environment that is gone, so nothing has to be rebuilt, re-registered or re-approved to bring it back, and nobody has to remember which of the forty-eight is currently real."), beat === 3 && /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, /* @__PURE__ */ React.createElement("b", null, "One node is empty on purpose, and it is the most expensive thing in the picture."), " ", "It is headroom, and it is sized to the p50 of concurrent first-wave jobs rather than to the peak \u2014 because its SLO is ", /* @__PURE__ */ React.createElement("b", null, "first-job schedule latency"), ", not capacity. A buffer sized to the peak is not headroom, it is a second cluster. Every static node like this one is spot savings deliberately not taken, and that trade is written down where the node is declared rather than discovered later by whoever is reading the bill. I held this space with pause pods before ", /* @__PURE__ */ React.createElement("code", null, "CapacityBuffer"), " existed; it is a first-class object now, and the workload that used to impersonate one is retired."), /* @__PURE__ */ React.createElement("p", { className: "speccc-cap" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-cap-mono" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-illus" }, "Mechanism, not measurement"), " ", "Twelve services, four environments, one cell scale across all three panels. Every area here is arithmetic of this drawing and can be counted off it; none of it is a measurement of any estate, and no packing efficiency, density or utilisation figure is stated \u2014 none exists that this page could source.")));
  };
  const SpecCICD = ({ spec }) => /* @__PURE__ */ React.createElement(SpecPageShell, { spec }, /* @__PURE__ */ React.createElement("style", null, CSS), /* @__PURE__ */ React.createElement(
    SpecSection,
    {
      title: "What descends, and what never needs to",
      hint: "one visual, and it is the argument"
    },
    /* @__PURE__ */ React.createElement(SpecSignature, { spec })
  ), /* @__PURE__ */ React.createElement(
    SpecSection,
    {
      title: "The runner is a trigger, not a computer",
      hint: "thin-client CI"
    },
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p" }, "I treat the CI vendor as a port with five duties and no compute: start the thing, relay the logs, fan the status back out to whatever is watching, re-upload the artifacts, and pass on a cancel. That is the entire contract. Everything expensive \u2014 the builds, the tests, the scanners, the ephemeral clusters \u2014 is submitted to an engine running on infrastructure I already own and already pay for, whether or not a pipeline is running this minute."),
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p" }, "The reason to draw that strip as thin as it is drawn is that the thinness is the argument. A runner that only has to do those five things does not need to be sized for the largest job anybody might submit, does not need the build cache, does not need the credentials the job needs, and \u2014 the part that matters most in practice \u2014 ", /* @__PURE__ */ React.createElement("strong", null, "does not need to be the same thing your developers cannot have."), " Once the work is a submission to an engine rather than a script running inside a vendor\u2019s container, the same submission is available from a laptop, and there stops being a second definition of a build to keep in sync with the first."),
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p" }, "Underneath, the pieces are ordinary and deliberately so. Builds run on rootless BuildKit rather than a privileged Docker-in-Docker sidecar, because a build daemon with the host socket is a hole you have to keep arguing about. Tests run in pods, with the same admission rules and the same network policy as anything else on the cluster, which is what makes a test environment an environment rather than a simulation of one. And every job declares an ", /* @__PURE__ */ React.createElement("strong", null, "execution class"), " rather than a machine \u2014 the six in the legend above \u2014 so that the question \u201Cwhere does this run\u201D has a typed answer that a reviewer can read in the diff instead of a scheduling accident nobody can see."),
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p" }, "That typing is the part I would defend hardest. When a fork\u2019s pull request arrives, the code in it is untrusted input, and it descends into ", /* @__PURE__ */ React.createElement("code", null, "#sandbox-gvisor"), " ", "because of what it is. Not because a node had room. Not because someone remembered. A placement that follows from a property of the work cannot be forgotten under time pressure, and a placement that follows from availability always eventually is.")
  ), /* @__PURE__ */ React.createElement(
    SpecSection,
    {
      title: "The same environments, three substrates",
      hint: "mechanism diagram \u2014 no figure"
    },
    /* @__PURE__ */ React.createElement(CICDSubstrates, null),
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p", style: { marginTop: 18 } }, "Consolidation is the mechanism under panel 2: Karpenter looks at what is actually running, works out whether the same pods fit on fewer or cheaper nodes, and replaces the nodes it can. Bin packing is what makes that possible \u2014 requests declared honestly enough that a scheduler can pack against them, and workloads disruptable enough that it is allowed to try. It is genuinely worth doing and I do it. It is also, on the evidence of panel 3, second-order, because it can only ever optimise the machine under work that is running."),
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p" }, "Spot belongs in the same paragraph for its ", /* @__PURE__ */ React.createElement("strong", null, "operational"), " consequence rather than its price, and the price is not this page\u2019s to argue. What matters here is that interruption risk is a property of a specific pool rather than of spot in general \u2014 the FinOps page draws that distribution, pool by pool, and both of its figures come from the same feed. The consequence for a pipeline is the whole of my interest in it: it is why a hand-placed EC2 instance cannot do this job while a NodePool spanning many instance types can, and it is why ", /* @__PURE__ */ React.createElement("code", null, "#trusted-spot"), " is a class you have to opt into rather than a default. A step goes there when it is re-runnable by construction. The classes that are not re-runnable do not get the discount, and that is a typing decision made in the pipeline rather than a bet placed on a pool."),
    /* @__PURE__ */ React.createElement("p", { className: "speccc-src" }, "The spot distribution behind this paragraph \u2014 the pool count, the median saving and the share of pools in the high-interruption band, all from the AWS Spot Instance Advisor feed \u2014 is drawn and sourced on the FinOps page and is deliberately not restated here. Nothing above is a measurement of any pipeline: no wall-clock, no reuse rate, no cache-hit rate and no cold-start time appears anywhere on this page, because none of them has one behind it.")
  ), /* @__PURE__ */ React.createElement(
    SpecSection,
    {
      title: "Built, designed, and practised",
      hint: "three different words, kept apart"
    },
    /* @__PURE__ */ React.createElement("p", { className: "speccc-p", style: { marginBottom: 16 } }, "These are not the same claim and this page does not blur them. ", /* @__PURE__ */ React.createElement("strong", null, "Built"), " ", "means there is an artifact and a tool run that checks it. ", /* @__PURE__ */ React.createElement("strong", null, "Designed"), " means it is specified to implementation depth and is not running. ", /* @__PURE__ */ React.createElement("strong", null, "Practice"), " ", "means I have done it professionally, which is a r\xE9sum\xE9 claim and needs no repository to be true \u2014 and where that is what I am saying, I say it in the first person and point at nothing."),
    /* @__PURE__ */ React.createElement("div", { className: "speccc-split" }, /* @__PURE__ */ React.createElement("div", { className: "speccc-card is-built" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-card-k" }, "Built \xB7 verifiable"), /* @__PURE__ */ React.createElement("h3", null, "A pipeline generator governed by CUE"), /* @__PURE__ */ React.createElement("p", null, "Pipelines are generated against a typed schema rather than hand-edited. The consequence is the only one worth having: ", /* @__PURE__ */ React.createElement("strong", null, "a non-compliant pipeline does not fail review, it fails to compile."), " Classification, execution class and scope are fields with types, and an unlisted execution class is not a lint warning \u2014 it is not a member of the enum."), /* @__PURE__ */ React.createElement("p", null, "It ships its own negative test: a deliberately over-classified pipeline that exists only to prove the guardrail refuses it. A guardrail with no failing case next to it is a guardrail nobody has watched fail.")), /* @__PURE__ */ React.createElement("div", { className: "speccc-card is-built" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-card-k" }, "Built"), /* @__PURE__ */ React.createElement("h3", null, "Impact-scoped child pipelines"), /* @__PURE__ */ React.createElement("p", null, "Subtree globs map to child pipelines that are only included when the paths they own actually changed. This is the coarse, shipped form of the argument the signature makes at fine grain: work whose inputs did not move does not need to run again."), /* @__PURE__ */ React.createElement("p", null, "Also built: autoscaling GitHub Actions runner scale sets on EKS, via actions-runner-controller \u2014 which is what makes \u201Cthe runner is a trigger\u201D a topology rather than an opinion.")), /* @__PURE__ */ React.createElement("div", { className: "speccc-card" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-card-k" }, "Designed"), /* @__PURE__ */ React.createElement("h3", null, "Execution classes, BuildKit, tests in pods"), /* @__PURE__ */ React.createElement("p", null, "The six classes, the rootless BuildKit topology with no privileged daemon and dual-arch builds without QEMU, and running tests as pods under real admission rules are all specified to implementation depth. Designed, not running \u2014 and that is a fact about a repository, not about the practice.")), /* @__PURE__ */ React.createElement("div", { className: "speccc-card" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-card-k" }, "Split, deliberately"), /* @__PURE__ */ React.createElement("h3", null, "Hermetic proofs"), /* @__PURE__ */ React.createElement("p", null, "Two different words on purpose. ", /* @__PURE__ */ React.createElement("strong", null, "Built, coarsely:"), " path-filtered child pipelines and a content-addressed skip keyed on a run identity. ", /* @__PURE__ */ React.createElement("strong", null, "Designed, to implementation depth:"), " the full derivation \u2014 the key that makes a step\u2019s identity a function of its inputs and of nothing else, so that a match is a proof and not a cache hit."), /* @__PURE__ */ React.createElement("p", null, "The difference matters here more than anywhere: a cache that is wrong is a slow bug, and a proof that is wrong is a wrong build. Only one of those is worth designing at that depth before shipping it.")), /* @__PURE__ */ React.createElement("div", { className: "speccc-card" }, /* @__PURE__ */ React.createElement("span", { className: "speccc-card-k" }, "Practice \xB7 first person"), /* @__PURE__ */ React.createElement("h3", null, "Thin-client CI, and pre-scaling"), /* @__PURE__ */ React.createElement("p", null, "I have run CI as a thin client against an engine on my own cluster, and I plan capacity from the enumerated plan at t=0 rather than reacting to a queue half-way through a run. No repository is offered as evidence for either, because a r\xE9sum\xE9 is first-person testimony and the honest move is to say so rather than to point at something that does not contain it."), /* @__PURE__ */ React.createElement("p", null, "What I will not attach to either is a number. No wall-clock saving, no reuse rate, no cold-start figure appears on this page.")))
  ), /* @__PURE__ */ React.createElement(SpecSection, { title: "Where this page stops", hint: "three doors, one sentence each" }, /* @__PURE__ */ React.createElement("div", { className: "spec-refs" }, /* @__PURE__ */ React.createElement("button", { type: "button", className: "spec-ref", onClick: () => go("finops") }, /* @__PURE__ */ React.createElement("span", { className: "spec-ref-kind" }, "FinOps"), /* @__PURE__ */ React.createElement("span", { className: "spec-ref-title" }, "The largest single CI lever is not running work twice"), /* @__PURE__ */ React.createElement("span", { className: "spec-ref-why" }, "Which is why the cost argument here is about elimination and about capacity you already own; the rate side \u2014 the breakeven, the commitments, the sourced spot median \u2014 is argued there and is not repeated here."), /* @__PURE__ */ React.createElement("span", { className: "role-card-open" }, "Open", /* @__PURE__ */ React.createElement(Icon, { name: "chevronRight", size: 12 }))), /* @__PURE__ */ React.createElement("button", { type: "button", className: "spec-ref", onClick: () => go("governance") }, /* @__PURE__ */ React.createElement("span", { className: "spec-ref-kind" }, "Compliance & Governance"), /* @__PURE__ */ React.createElement("span", { className: "spec-ref-title" }, "A promotion boundary re-proves everything; apex accepts no prior proof"), /* @__PURE__ */ React.createElement("span", { className: "spec-ref-why" }, "So the reuse this page argues for stops dead at a boundary that matters \u2014 and what a gate decides, and how a control becomes an admission decision, is that page\u2019s argument. Mine is only where the check runs and what it costs to run it there."), /* @__PURE__ */ React.createElement("span", { className: "role-card-open" }, "Open", /* @__PURE__ */ React.createElement(Icon, { name: "chevronRight", size: 12 }))), /* @__PURE__ */ React.createElement("button", { type: "button", className: "spec-ref", onClick: () => go("devx") }, /* @__PURE__ */ React.createElement("span", { className: "spec-ref-kind" }, "Developer Experience"), /* @__PURE__ */ React.createElement("span", { className: "spec-ref-title" }, "Local and CI submit to the same engine"), /* @__PURE__ */ React.createElement("span", { className: "spec-ref-why" }, "Which is the reason a thin runner is worth having at all: one definition of a build, reachable from a laptop, so nothing has to be reproduced twice to be trusted once."), /* @__PURE__ */ React.createElement("span", { className: "role-card-open" }, "Open", /* @__PURE__ */ React.createElement(Icon, { name: "chevronRight", size: 12 }))))));
  window.CICDSignature = CICDSignature;
  window.CICDSubstrates = CICDSubstrates;
  window.SpecCICD = SpecCICD;
})();
