// Code generated by tools/jsxbuild from SpecFinOps.jsx. DO NOT EDIT.
(function() {
  const SpecPageShell = window.SpecPageShell;
  const SpecSection = window.SpecSection;
  const SpecSignature = window.SpecSignature;
  const PlaceholderBadge = window.PlaceholderBadge;
  const FIN_CSS = `
.specfin-sig { display:flex; flex-direction:column; gap:15px; --specfin-state: var(--neon-pink);
               --specfin-lit: var(--neon-cyan); }
.specfin-sig.is-after { --specfin-state: var(--neon-cyan); }
/* The light theme's --neon-cyan measures 3.14:1 on white \u2014 fine for a shape,
   thin for a line of type. --specfin-lit is the darkened form, used wherever the
   tone colour has to carry TEXT rather than a mark. SpecFinOpsUtil.jsx declares
   the same variable on its own root: classic scripts share no scope, and a
   second root class inherits none of the custom properties declared on this one. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-sig { --specfin-lit: color-mix(in oklab, var(--neon-cyan) 58%, #000); }

.specfin-ctl { display:flex; flex-wrap:wrap; align-items:center; gap:12px; }
.specfin-seg { display:inline-flex; gap:3px; padding:3px; background:var(--bg); border:1px solid var(--line); border-radius:999px; }
.specfin-seg button { appearance:none; border:0; cursor:pointer; padding:7px 15px; border-radius:999px; background:none;
  font-family:var(--font-mono); font-size:10px; letter-spacing:.1em; text-transform:uppercase; color:var(--text-mute);
  transition:background .2s ease, color .2s ease; }
.specfin-seg button:hover { color:var(--text); }
.specfin-seg button[aria-pressed="true"] { background:var(--specfin-state); color:var(--bg); }
/* The light theme remaps --neon-cyan to a mid green; white-on-that is thin for
   a 10px uppercase label, so the selected pill darkens rather than relying on
   the raw token. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-seg button[aria-pressed="true"] {
  background:color-mix(in oklab, var(--specfin-state) 84%, #000); color:#fff; }
.specfin-seg button:focus-visible { outline:2px solid var(--specfin-state); outline-offset:2px; }
/* --text-mute, not --text-faint: measured, faint is 2.16:1 on the panel in the
   dark theme and 2.67:1 in the light one, and this line is the instruction for
   the control beside it. */
.specfin-hint { font-family:var(--font-mono); font-size:9.5px; letter-spacing:.1em; text-transform:uppercase; color:var(--text-mute); }

.specfin-readout { display:flex; flex-wrap:wrap; align-items:center; gap:12px 22px;
  padding:14px 17px; background:var(--bg); border:1px solid var(--line);
  border-left:2px solid var(--specfin-state); border-radius:var(--rad-sm); transition:border-color .45s ease; }
.specfin-ro-main { display:flex; align-items:baseline; gap:10px; }
.specfin-n { display:inline-block; min-width:68px; text-align:left;
  font-family:var(--font-mono); font-size:36px; font-weight:500; line-height:1; letter-spacing:-.02em;
  color:var(--specfin-state); text-shadow:0 0 22px color-mix(in oklab, var(--specfin-state) 34%, transparent);
  transition:color .45s ease; }
[data-theme="light"] .specfin-n { text-shadow:none; }
.specfin-ro-unit { font-family:var(--font-mono); font-size:10.5px; letter-spacing:.09em; text-transform:uppercase; color:var(--text-mute); }
.specfin-ro-eq { font-family:var(--font-mono); font-size:11.5px; color:var(--text-dim); }
/* The resting delta used to be the same colour at opacity .3, which measured
   1.23:1 dark and 1.28:1 light \u2014 text nobody can read, in an aria-live region a
   screen reader announces in full. The recession is now carried by colour and
   border alone: --text-mute at rest, the tone colour once the state it describes
   is the state on screen. */
.specfin-delta { display:inline-flex; align-items:baseline; gap:8px; padding:5px 12px; border-radius:999px;
  border:1px solid var(--line); font-family:var(--font-mono); font-size:12.5px; color:var(--text-mute);
  transition:color .45s ease, border-color .45s ease; }
.specfin-sig.is-after .specfin-delta { color:var(--specfin-lit); border-color:color-mix(in oklab, var(--neon-cyan) 45%, var(--line)); }
.specfin-delta span { font-size:9.5px; letter-spacing:.08em; text-transform:uppercase; color:var(--text-mute); }
.specfin-ro-badge { margin-left:auto; }

.specfin-svg { display:block; width:100%; height:auto; }
/* The site's diagrams scale to zero on a phone. This one carries 8px mono
   labels, so below ~720px it keeps a floor and scrolls sideways instead of
   becoming unreadable. The scroll is on a wrapper, so the page body never
   scrolls horizontally. */
.specfin-scroll { overflow-x:auto; }
@media (max-width:720px) { .specfin-svg { min-width:600px; } }

/* State is one class on the wrapper; everything below is a transition off it.
   Base opacities live here rather than in presentation attributes so the two
   ends of every transition come from the same cascade. */
.specfin-sig .mesh path { opacity:.24; transition:opacity .5s ease; }
.specfin-sig.is-after .mesh path { opacity:0; }
.specfin-sig .dup { opacity:1; transition:opacity .45s ease; }
.specfin-sig.is-after .dup { opacity:0; }
.specfin-sig .hub, .specfin-sig .spoke path { opacity:0; transition:opacity .5s ease; }
.specfin-sig.is-after .hub, .specfin-sig.is-after .spoke path { opacity:1; }
.specfin-sig .cell { fill:var(--neon-pink); opacity:.55; transition:opacity .45s ease, fill .45s ease; }
/* The removed squares do not vanish, they go to a ghost \u2014 so the "after" state
   still shows the hole it made and carries the comparison on its own, for a
   reader who never touches the toggle. */
.specfin-sig.is-after .cell { opacity:.1; }
.specfin-sig.is-after .cell.is-keep { fill:var(--neon-cyan); opacity:1; }

.specfin-cap { margin:0; max-width:80ch; padding-left:12px; border-left:2px solid var(--specfin-state);
  font-size:13.5px; line-height:1.7; color:var(--text-dim); transition:border-color .45s ease; }
/* This paragraph is where "Geoff's own figure \u2014 an estimate" is written down.
   At --text-faint it measured 2.16:1 dark / 2.67:1 light, which is a caveat
   set quieter than the claim it qualifies. --text-mute, and the emphasis one
   step brighter again so it still reads as emphasis. */
.specfin-foot { margin:0; max-width:82ch; font-family:var(--font-mono); font-size:10.5px; line-height:1.75; color:var(--text-mute); }
.specfin-foot em { font-style:normal; color:var(--text-dim); }

.specfin-prose { margin:0; max-width:74ch; font-size:14px; line-height:1.75; color:var(--text-dim); }
.specfin-prose + .specfin-prose { margin-top:12px; }
.specfin-stack { display:flex; flex-direction:column; gap:20px; }

/* The attributed aside. The verdicts require the FinOps-certification paragraph
   to render as an OPINION, attributed, and explicitly NOT as a stat tile and NOT
   inside a signature visual \u2014 so it is a <figure>/<blockquote>/<figcaption>, the
   markup that says "a person said this" rather than "the platform measured
   this". Deliberately no tone colour and no glow: the one thing this block must
   not look like is a metric. --text on --bg, so it clears contrast in every
   theme without a light-mode override to get wrong. */
.specfin-aside { margin:0; max-width:76ch; padding:16px 20px; background:var(--bg);
  border:1px solid var(--line); border-left:2px solid var(--text-mute); border-radius:var(--rad-sm);
  display:flex; flex-direction:column; gap:12px; }
.specfin-aside blockquote { margin:0; font-size:14.5px; line-height:1.75; color:var(--text); }
.specfin-aside blockquote p { margin:0; }
.specfin-aside blockquote p + p { margin-top:10px; }
/* Sentence case, deliberately. Every other mono eyebrow on this page is
   uppercased, but the verdicts require this aside VERBATIM and the attribution
   is part of it \u2014 "\u2014 Geoff Miller, opinion." is a sentence a person wrote, not
   a label. Uppercasing it would make the rendered page disagree with the text
   it is quoting. */
.specfin-aside figcaption { font-family:var(--font-mono); font-size:11px; letter-spacing:.02em;
  color:var(--text-dim); }

/* The two rate visuals arrive from a third file and bring their own frames.
   This is only the gap between them and the prose that introduces them. */
.specfin-rate-slot { display:flex; flex-direction:column; gap:20px; }

/* The estimate label, rendered from the record so the page cannot print an
   invented cadence without printing what kind of figure it is. It sits inside a
   .spec-rail-note, which is --text-mute; the label goes one step BRIGHTER
   rather than quieter, because a caveat that is the faintest thing in the
   paragraph is a caveat nobody reads. */
.specfin-src { color:var(--text-dim); }

@media (max-width:640px) {
  .specfin-n { font-size:28px; min-width:64px; }
  .specfin-readout { gap:10px 14px; }
  .specfin-ro-badge { margin-left:0; }
}
@media (prefers-reduced-motion: reduce) { .specfin-sig * { transition:none !important; } }
@media print { .specfin-n { text-shadow:none !important; } }
`;
  const VPCS = 12, SVCS = 6, AZS = 3;
  const BEFORE = VPCS * SVCS * AZS;
  const AFTER = SVCS * AZS;
  const CUT = Math.round((1 - AFTER / BEFORE) * 100);
  const SERVICES = ["s3", "ecr.api", "kms", "secretsmgr", "logs", "sts"];
  const vpcTop = (i) => 34 + i * 23;
  const vpcMid = (i) => vpcTop(i) + 8.5;
  const svcTop = (j) => 46 + j * 44;
  const svcMid = (j) => svcTop(j) + 12;
  const HUB_X = 308, HUB_W = 144, HUB_Y = 104, HUB_H = 130, HUB_MID = HUB_Y + HUB_H / 2;
  const IDX_V = [], IDX_S = [];
  for (let i = 0; i < VPCS; i++) IDX_V.push(i);
  for (let j = 0; j < SVCS; j++) IDX_S.push(j);
  const MESH = [];
  IDX_V.forEach((i) => IDX_S.forEach((j) => MESH.push({ i, j })));
  const LEDGER_COLS = 108, LEDGER_X = 14, LEDGER_Y = 353, LEDGER_PX = 6.8, LEDGER_PY = 9.5, LEDGER_SQ = 4.4;
  const CELLS = [];
  for (let k = 0; k < BEFORE; k++) CELLS.push(k);
  const pad2 = (n) => n < 10 ? "0" + n : String(n);
  const reduced = () => typeof window.matchMedia === "function" && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const useCountTo = (target) => {
    const [v, setV] = React.useState(target);
    const shown = React.useRef(target);
    React.useEffect(() => {
      shown.current = v;
    });
    React.useEffect(() => {
      const from = shown.current;
      if (from === target || reduced() || typeof requestAnimationFrame !== "function") {
        setV(target);
        return void 0;
      }
      const t0 = Date.now(), dur = 620;
      let id = 0;
      const step = () => {
        const p = Math.min(1, (Date.now() - t0) / dur);
        const e = 1 - Math.pow(1 - p, 3);
        setV(Math.round(from + (target - from) * e));
        if (p < 1) id = requestAnimationFrame(step);
      };
      id = requestAnimationFrame(step);
      return () => cancelAnimationFrame(id);
    }, [target]);
    return v;
  };
  const FinOpsSignature = () => {
    const [after, setAfter] = React.useState(false);
    const [touched, setTouched] = React.useState(false);
    const host = React.useRef(null);
    const shownN = useCountTo(after ? AFTER : BEFORE);
    React.useEffect(() => {
      if (touched || !host.current) return void 0;
      if (reduced() || typeof IntersectionObserver !== "function") return void 0;
      let timer = 0;
      const io = new IntersectionObserver((entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            if (timer) return;
            timer = setTimeout(() => {
              io.disconnect();
              setAfter(true);
            }, 1400);
          } else if (timer) {
            clearTimeout(timer);
            timer = 0;
          }
        });
      }, { threshold: 0.3 });
      io.observe(host.current);
      return () => {
        io.disconnect();
        if (timer) clearTimeout(timer);
      };
    }, [touched]);
    const pick = (v) => {
      setTouched(true);
      setAfter(v);
    };
    const label = after ? "After. The same twelve VPCs, now empty, each connected once to a single shared endpoint hub holding six interface endpoints in each of three availability zones, which reaches the same six AWS services. Eighteen squares remain in the ledger below." : "Before. Twelve VPCs on the left, each carrying its own copy of six interface endpoints, linked by seventy-two connections to six AWS services on the right. Below, two hundred and sixteen squares \u2014 one for every interface endpoint billed in one availability zone.";
    return /* @__PURE__ */ React.createElement("div", { className: "specfin-sig" + (after ? " is-after" : ""), ref: host }, /* @__PURE__ */ React.createElement("div", { className: "specfin-ctl" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-seg", role: "group", "aria-label": "Cost topology, before and after" }, /* @__PURE__ */ React.createElement("button", { type: "button", "aria-pressed": !after, onClick: () => pick(false) }, "Before \xB7 endpoint per VPC"), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-pressed": after, onClick: () => pick(true) }, "After \xB7 one shared hub")), /* @__PURE__ */ React.createElement("span", { className: "specfin-hint" }, "Same reachability. Same six services. Compare the count.")), /* @__PURE__ */ React.createElement("div", { className: "specfin-readout", "aria-live": "polite" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-main" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-n" }, shownN), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-unit" }, "billed endpoint\u2011AZs")), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-eq" }, after ? SVCS + " services \xD7 " + AZS + " AZs, once" : VPCS + " VPCs \xD7 " + SVCS + " services \xD7 " + AZS + " AZs"), /* @__PURE__ */ React.createElement("span", { className: "specfin-delta" }, "\u2212", CUT, "%", /* @__PURE__ */ React.createElement("span", null, "of the endpoint line item")), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-badge" }, /* @__PURE__ */ React.createElement(
      PlaceholderBadge,
      {
        label: "Illustrative",
        title: "The estate drawn here is illustrative \u2014 " + VPCS + " VPCs and " + SVCS + " services. The counts are arithmetic of the picture, computed from it, not a measurement of any real estate."
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "specfin-scroll" }, /* @__PURE__ */ React.createElement(
      "svg",
      {
        className: "specfin-svg",
        viewBox: "0 0 760 376",
        xmlns: "http://www.w3.org/2000/svg",
        role: "img",
        "aria-label": label
      },
      /* @__PURE__ */ React.createElement("text", { x: "10", y: "20", fontFamily: "JetBrains Mono", fontSize: "8.5", letterSpacing: "1.3", fill: "var(--text-faint)" }, "VPCS (", VPCS, ")"),
      /* @__PURE__ */ React.createElement("text", { x: "750", y: "20", textAnchor: "end", fontFamily: "JetBrains Mono", fontSize: "8.5", letterSpacing: "1.3", fill: "var(--text-faint)" }, "AWS SERVICE ENDPOINTS (", SVCS, ")"),
      /* @__PURE__ */ React.createElement("g", { className: "mesh", pointerEvents: "none" }, MESH.map((m) => /* @__PURE__ */ React.createElement(
        "path",
        {
          key: m.i + "-" + m.j,
          d: "M104 " + vpcMid(m.i) + " L656 " + svcMid(m.j),
          fill: "none",
          stroke: "var(--neon-pink)",
          strokeWidth: "0.55",
          style: { transitionDelay: Math.min(300, (m.i * SVCS + m.j) * 3) + "ms" }
        }
      ))),
      /* @__PURE__ */ React.createElement("g", { className: "spoke", pointerEvents: "none" }, IDX_V.map((i) => /* @__PURE__ */ React.createElement(
        "path",
        {
          key: "in" + i,
          d: "M104 " + vpcMid(i) + " C 190 " + vpcMid(i) + ", 244 " + HUB_MID + ", " + HUB_X + " " + HUB_MID,
          fill: "none",
          stroke: "var(--neon-cyan)",
          strokeWidth: "1",
          strokeOpacity: ".7",
          style: { transitionDelay: 120 + i * 22 + "ms" }
        }
      )), IDX_S.map((j) => /* @__PURE__ */ React.createElement(
        "path",
        {
          key: "out" + j,
          d: "M" + (HUB_X + HUB_W) + " " + HUB_MID + " C 530 " + HUB_MID + ", 578 " + svcMid(j) + ", 656 " + svcMid(j),
          fill: "none",
          stroke: "var(--neon-cyan)",
          strokeWidth: "1",
          strokeOpacity: ".7",
          style: { transitionDelay: 200 + j * 30 + "ms" }
        }
      ))),
      /* @__PURE__ */ React.createElement("g", { className: "hub", pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
        "rect",
        {
          x: HUB_X,
          y: HUB_Y,
          width: HUB_W,
          height: HUB_H,
          rx: "6",
          fill: "var(--bg-1)",
          stroke: "var(--neon-cyan)",
          strokeWidth: "1.4"
        }
      ), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: HUB_X + HUB_W / 2,
          y: HUB_Y + 18,
          textAnchor: "middle",
          fontFamily: "JetBrains Mono",
          fontSize: "9",
          letterSpacing: "0.9",
          fill: "var(--neon-cyan)"
        },
        "SHARED ENDPOINT HUB"
      ), [0, 1, 2].map((a) => {
        const ly = HUB_Y + 26 + a * 32;
        return /* @__PURE__ */ React.createElement("g", { key: a }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: HUB_X + 10,
            y: ly,
            width: HUB_W - 20,
            height: "28",
            rx: "3",
            fill: "var(--bg-2)",
            stroke: "var(--line)",
            strokeWidth: "1"
          }
        ), /* @__PURE__ */ React.createElement("text", { x: HUB_X + 18, y: ly + 17.5, fontFamily: "JetBrains Mono", fontSize: "7.5", fill: "var(--text-mute)" }, "az-", ["a", "b", "c"][a]), IDX_S.map((k) => /* @__PURE__ */ React.createElement("circle", { key: k, cx: HUB_X + 52 + k * 13, cy: ly + 14, r: "2.6", fill: "var(--neon-cyan)" })));
      })),
      IDX_V.map((i) => /* @__PURE__ */ React.createElement("g", { key: "v" + i }, /* @__PURE__ */ React.createElement(
        "rect",
        {
          x: "10",
          y: vpcTop(i),
          width: "94",
          height: "17",
          rx: "3",
          fill: "var(--bg-1)",
          stroke: "var(--line)",
          strokeWidth: "1"
        }
      ), /* @__PURE__ */ React.createElement("text", { x: "17", y: vpcMid(i) + 3, fontFamily: "JetBrains Mono", fontSize: "8", fill: "var(--text-mute)" }, "vpc-", pad2(i + 1)), /* @__PURE__ */ React.createElement("g", { className: "dup", pointerEvents: "none", style: { transitionDelay: i * 14 + "ms" } }, IDX_S.map((k) => /* @__PURE__ */ React.createElement("circle", { key: k, cx: 58 + k * 6, cy: vpcMid(i), r: "1.9", fill: "var(--neon-pink)" }))))),
      IDX_S.map((j) => /* @__PURE__ */ React.createElement("g", { key: "s" + j }, /* @__PURE__ */ React.createElement(
        "rect",
        {
          x: "656",
          y: svcTop(j),
          width: "94",
          height: "24",
          rx: "3",
          fill: "var(--bg-1)",
          stroke: "var(--line)",
          strokeWidth: "1"
        }
      ), /* @__PURE__ */ React.createElement(
        "text",
        {
          x: "703",
          y: svcMid(j) + 3.5,
          textAnchor: "middle",
          fontFamily: "JetBrains Mono",
          fontSize: "8.5",
          fill: "var(--text-dim)"
        },
        SERVICES[j]
      ))),
      /* @__PURE__ */ React.createElement("text", { x: "703", y: "308", textAnchor: "middle", fontFamily: "JetBrains Mono", fontSize: "7.5", fill: "var(--text-faint)" }, "same six, either way"),
      /* @__PURE__ */ React.createElement("line", { x1: "14", y1: "328", x2: "746", y2: "328", stroke: "var(--line-soft)", strokeWidth: "1" }),
      /* @__PURE__ */ React.createElement("text", { x: "14", y: "344", fontFamily: "JetBrains Mono", fontSize: "8", letterSpacing: "1.2", fill: "var(--text-faint)" }, "ONE SQUARE = ONE INTERFACE ENDPOINT, BILLED IN ONE AZ"),
      /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, CELLS.map((k) => /* @__PURE__ */ React.createElement(
        "rect",
        {
          key: k,
          className: "cell" + (k < AFTER ? " is-keep" : ""),
          x: LEDGER_X + k % LEDGER_COLS * LEDGER_PX,
          y: LEDGER_Y + Math.floor(k / LEDGER_COLS) * LEDGER_PY,
          width: LEDGER_SQ,
          height: LEDGER_SQ,
          rx: "1",
          style: { transitionDelay: Math.min(420, k % LEDGER_COLS * 3.4) + "ms" }
        }
      )))
    )), /* @__PURE__ */ React.createElement("p", { className: "specfin-cap" }, after ? "One hub, three availability zones, six endpoints. Every VPC still reaches every service \u2014 the routing moved, the reachability did not. What disappeared was " + (BEFORE - AFTER) + " copies of something that only ever needed to exist once." : "Every VPC pays for its own copy of every endpoint, in every availability zone it runs in. Nothing here is misconfigured, nothing is failing, and no architecture review would flag it \u2014 which is exactly why it survives until somebody reads the invoice instead of the diagram."), /* @__PURE__ */ React.createElement("p", { className: "specfin-foot" }, "What this removed in dollars is a ", /* @__PURE__ */ React.createElement("em", null, "draft figure"), " and sits in ", /* @__PURE__ */ React.createElement("em", null, "The numbers"), " below, marked as one. The claim it comes from is rendered verbatim from the r\xE9sum\xE9 in ", /* @__PURE__ */ React.createElement("em", null, "On the r\xE9sum\xE9"), ". The structure above is the part that is not in question: the count of billed endpoints is a property of the topology, and the topology is a choice."));
  };
  const f1nChart = (id) => {
    const projects = window.SITE_DATA && window.SITE_DATA.projects || [];
    const p = projects.find((x) => x.id === "f1n");
    if (!p) return null;
    const pools = [p.charts || []].concat((p.subpages || []).map((s) => s.charts || []));
    for (let i = 0; i < pools.length; i++) {
      const hit = (pools[i] || []).find((c) => c && c.id === id);
      if (hit) return hit;
    }
    return null;
  };
  const CLAIM_FLOW = {
    kind: "gateflow",
    title: "An environment's resting state is off",
    note: "Fixed shutdown schedules are the usual answer and they lose to a global team, to a sprint whose load moves from dev to test mid-week, and to anyone who starts something by hand at 2am. A claim does not: it is a lease with an expiry, and the platform is the one holding the clock.",
    source: "Mechanism, not measurement \u2014 no figure appears in this diagram. Built first at REPAY, rebuilt at First Citizens Bank, generalized afterwards into the f1n control plane; both role bullets are rendered verbatim below and the design is on the f1n project page.",
    stages: [
      { id: "off", label: "Off", sub: "the default, not the exception" },
      { id: "claim", label: "Claim", sub: "a bounded request, with a reason" },
      { id: "lease", label: "Lease", sub: "the live contract" },
      { id: "back", label: "Off again", sub: "without anyone remembering" }
    ],
    gates: [
      {
        id: "policy",
        label: "Policy decides",
        title: "Policy decides, and says why",
        border: "off \u2192 claim",
        detail: "A control plane that only governs the traffic it initiates is governing the wrong set. The interesting claim is the one nobody files.",
        checks: [
          {
            what: "A start nobody filed becomes a bounded system claim",
            why: "A console click or an unregistered pipeline is detected and converted into a claim with a policy TTL, rather than leaking as an environment that is simply on."
          },
          {
            what: "Every denial carries a stable reason code",
            why: '"What is our top deny reason this month" becomes a dashboard query, which turns developer friction into a measurable quantity instead of a complaint in a retro.'
          },
          {
            what: "Policy runs in simulation before it runs in anger",
            why: "Disabled, observe, enforce \u2014 staged modes rather than a boolean, because every governance feature needs a period where it reports what it would have done."
          }
        ]
      },
      {
        id: "lease",
        label: "The clock starts",
        title: "A lease, not a switch",
        border: "claim \u2192 lease",
        detail: "Granting a claim creates a lease: the persisted contract about what should be running, and the only source of truth for it.",
        checks: [
          {
            what: "The window is bounded at the moment it opens",
            why: "Nothing has to be remembered later. The expiry is part of the grant, not a follow-up ticket."
          },
          {
            what: "Unaccepted claims cancel themselves",
            why: "A load test that got dropped does not silently stand an estate up on Thursday morning."
          },
          {
            what: "Shut down, never destroy",
            why: "Cost comes from active runtime, not from resources existing \u2014 and a tool that can lose a developer's work will not be trusted with the estate."
          }
        ]
      },
      {
        id: "reclaim",
        label: "Expiry and drift",
        title: "The platform takes it back",
        border: "lease \u2192 off",
        detail: "This is the border that turns a policy document into a control loop: the reclaim happens whether or not anybody is watching the bill.",
        checks: [
          {
            what: "Leases expire on their own",
            why: "The saving does not depend on discipline, a calendar invite, or the person who set it up still working there."
          },
          {
            what: "Drift is assumed, not hoped against",
            why: "Desired state and provider state diverge. Reconciliation detects the mismatch, remediates it, and escalates explicitly when it cannot."
          },
          {
            what: "Unresolvable drift ends somewhere a human can stand",
            why: 'A named manual-review state rather than a retry loop, because "it did not come back up and nobody noticed" is the failure that makes a team stop trusting automation.'
          }
        ]
      }
    ]
  };
  const SpecFinOps = ({ spec }) => {
    const split = f1nChart("share");
    const returns = f1nChart("returns");
    const util = spec && spec.utilization || null;
    const uc = util && util.components || {};
    const Ledger = uc.ledger ? window[uc.ledger] : null;
    const Reductio = uc.reductio ? window[uc.reductio] : null;
    const rc = spec && spec.rate && spec.rate.components || {};
    const Breakeven = rc.breakeven ? window[rc.breakeven] : null;
    const SpotCloud = rc.spotcloud ? window[rc.spotcloud] : null;
    const env = spec && spec.environments || null;
    const ec = env && env.components || {};
    const Threshold = ec.threshold ? window[ec.threshold] : null;
    return /* @__PURE__ */ React.createElement(SpecPageShell, { spec }, /* @__PURE__ */ React.createElement("style", null, FIN_CSS), /* @__PURE__ */ React.createElement(SpecSection, { title: "The waste was structural", hint: "one visual, and it is the argument" }, /* @__PURE__ */ React.createElement(SpecSignature, { spec })), /* @__PURE__ */ React.createElement(SpecSection, { title: "The half of the bill nobody optimises", hint: "a published median, and a regulated estate" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "A cost programme aimed at production is aimed at the constrained half of the estate. Every change there trades against latency, availability and blast radius, and each one needs a change window and a rollback plan. The other half has none of those constraints \u2014 and in a regulated bank it is the larger half, because compliance is precisely the thing that multiplies environments."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "Count the tiers: development, system integration test, user acceptance test, a performance tier that has to be production-sized to be worth anything, a DR tier that exists to be proven, and increasingly a preview environment per team. Most of them held near production parity, because when an auditor asks where a control was tested, \u201Cin something a quarter the size of production\u201D is a finding rather than an answer."), split && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("p", { className: "spec-rail-note" }, "This is the same chart that appears on the f1n project page, rendered from the same record rather than re-typed \u2014 including the line that says the second row is Geoff's own estimate and not a published benchmark."), /* @__PURE__ */ React.createElement(VizChart, { key: "fin-split", data: split })))), /* @__PURE__ */ React.createElement(SpecSection, { title: "What you pay for, and what you used", hint: "six environments, fourteen days, counted" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "The cadence in this section is Geoff's, not a benchmark's \u2014 nobody publishes per-tier non-production utilization at this resolution, and the paragraph below is what he has watched happen in the estates he has run. A development environment might genuinely be needed for a few hours on a Tuesday. Not all of Tuesday, not every Tuesday, and \u2014 the part a single-environment view hides \u2014 not by every team that has one of its own. Test and staging sit further from continuous still. A team releasing every day touches staging for a small part of that day; the pattern he has met most often is development on most days and QA once or twice a sprint, clustered where the release is."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "Those are four different rhythms, and the difference between them is the argument. It is also why the picture below runs for a fortnight rather than a day. \u201CTwice a sprint\u201D is not a shape a chart of one working day can draw: the tier is either absent from it or present at whatever it happened to be doing on the day somebody chose. Billing has no rhythm at all. An environment left running bills the same six hours whether a team spent them in it or in a stand-up, bills Saturday at the rate it bills Tuesday, and bills a whole fortnight for a tier that was wanted on two afternoons of it."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "Three of the six environments below are development environments, one per team, because a hosted dev environment per squad is the arrangement Geoff has most often inherited. They are also the tier most exposed to how development is actually done. Better local development does not have to remove them to be worth money \u2014 a team that can run the thing on the machine in front of it needs the hosted copy less often, and less often is a smaller bill. But the end of that gradient is the case worth naming. When a change can be built and exercised locally and then go straight to QA or staging for final integration, the hosted development tier is not being scheduled more tightly. It is not being provisioned. The rest of this section argues about ", /* @__PURE__ */ React.createElement("em", null, "when"), " an environment is billed; that one argues about whether it exists at all \u2014 which is the same kind of move as the endpoint hub in 01, applied to environments instead of endpoints."), Ledger && /* @__PURE__ */ React.createElement(React.Fragment, null, util.placeholder && util.note && /* @__PURE__ */ React.createElement("p", { className: "spec-rail-note" }, util.note, " ", /* @__PURE__ */ React.createElement("span", { className: "specfin-src" }, util.source)), /* @__PURE__ */ React.createElement(Ledger, { spec })))), /* @__PURE__ */ React.createElement(SpecSection, { title: "Why a schedule is the half you can delete", hint: "four steps, and no new figures" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "A business-hours schedule is the standard answer to all of that, and it is a good one. AWS puts the saving from switching non-production off outside working hours at up to 76%, and that figure \u2014 with its source \u2014 is in the benchmark chart at the end of the next section. Nothing here disputes it, and the first of the four steps below concedes it before anything else happens."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "The argument is about what a schedule leaves behind once it has done its work. Followed four steps, it argues itself out of existence. No measurement is involved and none of the steps needs one, which is why the block below carries no source line \u2014 there is nothing in it to source."), Reductio && /* @__PURE__ */ React.createElement(Reductio, { spec }))), /* @__PURE__ */ React.createElement(SpecSection, { title: "Why three copies are standing", hint: "one axis, and the moment it flips" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "Section 03 counts one environment's idleness against the calendar. This is the other half of the same bill and it has nothing to do with the calendar: at any instant there are three of them, and they are copies of each other. Dev, qa and staging is the common mix. The expensive property they share is not size \u2014 nobody runs dev at production's capacity \u2014 it is ", /* @__PURE__ */ React.createElement("em", null, "shape"), ": an environment is worth having because it has production's parts list and production's topology, which is exactly what an auditor is asking about, and that is what makes each one a copy rather than a sample. Three exist at the same moment. In a regulated estate the count is larger, and the band under the drawing names the ones it does not draw."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "The four steps above end by asking for a boot path and deleting the schedule. They do not say how fast the boot path has to be, and that number decides everything. An environment stands because standing one up is slow \u2014 that is the entire reason it is standing. Drop the time-to-usable below what a team will absorb and the standing copy loses its reason to exist. But \u201Cwhat a team will absorb\u201D is not a duration: four hours is absurd before a twelve-minute edit-and-run loop and arguable before a two-day release rehearsal. It is a ratio between the wait and the work, which is why the picture below needs two axes and why the boundary on it is a diagonal rather than a line on a clock. Geoff aims for fifteen minutes."), Threshold && /* @__PURE__ */ React.createElement(React.Fragment, null, env.placeholder && env.note && /* @__PURE__ */ React.createElement("p", { className: "spec-rail-note" }, env.note, " ", /* @__PURE__ */ React.createElement("span", { className: "specfin-src" }, env.source)), /* @__PURE__ */ React.createElement(Threshold, { spec })))), /* @__PURE__ */ React.createElement(SpecSection, { title: "Environments that take themselves back", hint: "the same mechanism, built twice" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "The sprint drawn above is the fleet version of a much simpler fact. A non-production environment that is needed during business hours is used for about forty hours a week and, left alone, bills for a hundred and sixty-eight \u2014 and forty is the generous case, the one where somebody wants it every working day. That gap is not a forecast; it is what a week contains. The only question a platform team actually faces is whether the default is ", /* @__PURE__ */ React.createElement("em", null, "on"), " ", "or ", /* @__PURE__ */ React.createElement("em", null, "off"), ", and everything else is a consequence of that choice."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "The same inversion was built first at REPAY and rebuilt at First Citizens Bank in an estate shaped nothing like it \u2014 a hundred-plus accounts, a landing zone, and compliance tiers that could not simply be switched off because somebody thought they looked idle. Doing it twice, in two differently-shaped organisations, is what turned a script into an architecture."), /* @__PURE__ */ React.createElement(VizChart, { key: "fin-claimflow", data: CLAIM_FLOW }), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "A claim that expires has to reach something that can actually switch the resource off, and there are only two such things. In AWS proper the actuator is an API call against a named resource \u2014 stop the EC2 instance, take the Auto Scaling group's desired capacity to zero, stop the RDS instance, scale the ECS service down \u2014 and billing follows that resource's own lifecycle. On Kubernetes the resource is a workload rather than a machine, and off means moving a deployment between zero and N, which is what KEDA exists to make a state something can come back from. Right-sizing is the smaller third move in the same loop: VPA sets requests from observed usage instead of from whatever number was typed a year ago."), /* @__PURE__ */ React.createElement("p", { className: "specfin-foot" }, "The Karpenter, KEDA and VPA components are ", /* @__PURE__ */ React.createElement("em", null, "built and running"), " in Geoff's Pulumi EKS component library. Chaining an environment claim through them \u2014 so that a lease expiring is the thing that takes a workload to zero \u2014 is his approach, and it is stated here in the first person: the local repository has that chain designed rather than implemented, and this page does not point at it as evidence. What Karpenter then does with the freed capacity is argued on the CI/CD page, not on this one."), returns && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("p", { className: "spec-rail-note" }, "And the concession, borrowed intact from the f1n page: both bars are AWS's own published figures for what a schedule returns, and this page agrees with them rather than competing with them. There is no figure of Geoff's own in this chart and no claim that claim-leasing beats these numbers \u2014 its argument is coverage, the cases a schedule cannot express, which is not a percentage at all."), /* @__PURE__ */ React.createElement(VizChart, { key: "fin-returns", data: returns })))), /* @__PURE__ */ React.createElement(SpecSection, { title: "The other lever, and what it actually buys", hint: "one inequality, and you can redo it in your head" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "Everything above is the usage lever \u2014 change how many hours you buy. The other lever is rate: pay less for each hour by promising to keep paying for them. It is where most cost programmes begin, and part of the reason is simply where it sits. AWS Cost Explorer surfaces commitment purchase recommendations prominently, and a recommendation shaped like a button is a default."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "A commitment is not a discount on the hours you use. It is an hourly spend commitment that bills every hour of its term whether or not anything consumed it \u2014 so it beats on-demand-with-the-thing-switched-off only above a threshold, and the threshold is one minus the discount. Commit if ", "u > 1 \u2212 d", ", where u is the share of the term the workload will actually run and d is the discount. That is the whole derivation. It is a division, and the chart below is it drawn to scale."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "The discount is real and it is large. What it is not is larger than the idle it locks in. For anything not running most of the time the commitment's floor sits above the on-demand cost of the hours actually wanted, and no amount of discount moves a floor. So the claim here is narrower than the usual one and harder to argue with: not that commitments fail to save money, but that the discount is smaller than the idle waste it commits you to."), Breakeven && /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-slot" }, /* @__PURE__ */ React.createElement(Breakeven, null)), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "And the exit is narrow, which is the part the recommendation does not show you. A Savings Plan can be returned only if it was bought in the same calendar month, within seven days, at under $100 an hour of commitment. The RDS User Guide is blunter:", " ", /* @__PURE__ */ React.createElement("em", null, "\u201CYou can't cancel a reserved DB instance.\u201D"), " There is no Convertible class for RDS and no resale market for either. Of everything on this page, the commitment is the one decision a platform cannot quietly take back on your behalf."), /* @__PURE__ */ React.createElement("figure", { className: "specfin-aside" }, /* @__PURE__ */ React.createElement("blockquote", null, /* @__PURE__ */ React.createElement("p", null, "The FinOps Certified Practitioner curriculum has no Kubernetes, spot, bin-packing or CI/CD module \u2014 verifiable from its published module list. That is not a criticism of the credential; it is the reason this page is about engineering rather than about a framework.")), /* @__PURE__ */ React.createElement("figcaption", null, "\u2014 Geoff Miller, opinion."), /* @__PURE__ */ React.createElement("p", { className: "specfin-foot" }, "Curriculum verified at learn.finops.org/finops-certified-practitioner-course-materials and learn.finops.org/page/complete-catalog, accessed 2026-08-07. The FinOps Framework's own Rate Optimization guidance says commitments should be sized only to resources actually required", " \u2014 ", "this page agrees with it.")))), /* @__PURE__ */ React.createElement(SpecSection, { title: "The market rate, and the dispersion nobody quotes", hint: "a published median, with its risk drawn beside it" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-stack" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "There is a third rate lever, and it is the only one that is not a promise. Spot is the price of accepting that the capacity can be taken back \u2014 which means, unlike a commitment, it is a cheaper rate you can hold while still switching the thing off. That is why it belongs on this page instead of contradicting it. What it needs is the number quoted honestly."), /* @__PURE__ */ React.createElement("p", { className: "specfin-prose" }, "So the figure below is the measured median across every pool in the region rather than the best pool in it, and the dispersion is drawn beside it instead of left out \u2014 including the share of pools that go away often enough that no single one is something to plan on. That disclosure is not a hedge. It is the argument: the answer to dispersion is diversification, and diversification is a property of whatever is placing the capacity, not of the price."), SpotCloud && /* @__PURE__ */ React.createElement("div", { className: "specfin-rate-slot" }, /* @__PURE__ */ React.createElement(SpotCloud, null)))));
  };
  window.FinOpsSignature = FinOpsSignature;
  window.SpecFinOps = SpecFinOps;
})();
