// Code generated by tools/jsxbuild from SpecFinOpsUtil.jsx. DO NOT EDIT.
(function() {
  const PlaceholderBadge = window.PlaceholderBadge;
  const UTIL_CSS = `
.specfin-util { display:flex; flex-direction:column; gap:15px; --specfin-state: var(--neon-pink);
                --specfin-lit: var(--neon-cyan); }
.specfin-util.is-after { --specfin-state: var(--neon-cyan); }
/* --specfin-lit, not --neon-cyan: this pill is 12.5px TEXT, and raw light-theme
   cyan on the readout measured 3.03:1 there. */
.specfin-util.is-after .specfin-delta { color:var(--specfin-lit);
  border-color:color-mix(in oklab, var(--neon-cyan) 45%, var(--line)); }
/* MEASURED, not assumed. --neon-cyan at full opacity is 11.8:1 on the dark panel
   but only 3.14:1 on the white one \u2014 level with the pink ground it is supposed
   to stand out FROM, once that ground is lifted to the 3:1 it needs to be
   visible at all. So the light theme darkens the lit ink, and only the lit ink.
   58% is not a taste call: the number that decides whether the encoding reads is
   the lit mark against the IDLE MARK BESIDE IT, and measured against the #df65b3
   ground that is 3.14:1 at 58%, 2.54:1 at 66% and 2.12:1 at 72%. Anything lighter
   is a mark you cannot pick out of the field it is drawn on. Same escape-hatch
   idiom the segmented pill in FIN_CSS already uses, for the same reason.

   THE :not() CHAIN IS LOAD-BEARING. styles.css declares [data-aesthetic="terminal"]
   and [data-aesthetic="outrun"] AFTER [data-theme="light"], at equal specificity,
   and both remap --bg* and the neon tokens back to a DARK palette. "Light theme"
   and "light background" are therefore not the same condition, and a rule keyed
   on data-theme alone fires on a dark panel. Measured, that inverted this encoding
   outright: light+terminal put the idle GROUND at 8.51:1 and the lit FIGURE at
   3.39:1 \u2014 ground brighter than figure, 2.51:1 between them \u2014 and light+outrun
   left 1.35:1. Gate every light override on the aesthetic actually being light. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-util {
  --specfin-lit: color-mix(in oklab, var(--neon-cyan) 58%, #000); }

/* Colour and opacity live here, never in presentation attributes, so both ends
   of every transition come from one cascade \u2014 the endpoint ledger's rule.
   Every alpha below was solved against the panel it actually composites onto,
   in both themes, rather than picked by eye. The billed field is not decoration:
   "you are charged for all of this and you used that" IS the argument, and a
   billed field at 1.3:1 does not make it. */
.specfin-util .usched  { fill:var(--bg-2); fill-opacity:.9; stroke:var(--text-mute);
                         stroke-dasharray:4 4; stroke-width:1; }
.specfin-util .urail   { fill:var(--neon-pink); opacity:.56; transition:opacity .5s ease; }
.specfin-util .ublock  { fill:var(--neon-pink); opacity:.56;
                         transition:opacity .5s ease, fill .5s ease; }
.specfin-util .ublock.is-used { fill:var(--specfin-lit); opacity:1; }
.specfin-util .ugain   { opacity:0; }
.specfin-util.is-after .ugain { opacity:1; }
/* The light theme needs more alpha for the same ratio on white. Scoped
   :not(.is-used) deliberately: an unscoped [data-theme="light"] .ublock rule
   TIES on specificity with the .is-used rule above and, being later in source
   order, silently wins. That is not hypothetical \u2014 it shipped, and it dropped
   every lit block to .24 (1.30:1 measured) and un-hid the three .ugain squares
   in the BEFORE state, where the gutter still read 5/56. */
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-util .ublock:not(.is-used) { opacity:.78; }
:root[data-theme="light"]:not([data-aesthetic="terminal"]):not([data-aesthetic="outrun"]) .specfin-util .urail { opacity:.78; }

/* The deleted rows go to a ghost rather than vanishing \u2014 the endpoint ledger's
   stated rule \u2014 so the "after" state still shows the hole it made, for a reader
   who never touches the control. */
.specfin-util .urow-local { transition:opacity .55s ease; }
.specfin-util.is-after .urow-local { opacity:.07; }
.specfin-util .ugone { opacity:0; transition:opacity .5s ease .35s; }
.specfin-util.is-after .ugone { opacity:1; }

/* The two numbered pips in the field are duplicated as HTML below the drawing,
   so a screen reader gets them in reading order rather than as orphaned SVG
   text sitting between two <rect>s. */
.specfin-util .specfin-marks { margin:0; padding:0; list-style:none;
  display:flex; flex-direction:column; gap:6px; }
.specfin-util .specfin-marks li { display:flex; align-items:baseline; gap:9px;
  font-family:var(--font-mono); font-size:10.5px; line-height:1.65; color:var(--text-mute); }
.specfin-util .specfin-mark-n { flex:0 0 auto; box-sizing:border-box;
  display:inline-flex; align-items:center; justify-content:center;
  width:16px; height:16px; border-radius:999px;
  border:1px solid var(--specfin-lit); color:var(--text); font-size:8.5px; }

.specfin-util .specfin-foot + .specfin-foot { margin-top:9px; }
/* The link takes its contrast from --text and its accent from the underline.
   Tinting 10.5px type with --specfin-state measured 3.14:1 in the light theme
   after the reveal \u2014 fine for a mark, not for a sentence somebody has to read. */
.specfin-util .specfin-link { appearance:none; border:0; padding:0; background:none;
  cursor:pointer; font:inherit; color:var(--text);
  border-bottom:1px solid var(--specfin-state); }
.specfin-util .specfin-link:focus-visible { outline:2px solid var(--specfin-state); outline-offset:2px; }

/* Below 720px the SVG keeps a 1:1 floor and scrolls sideways inside its own
   wrapper, so an 8px label stays an 8px label and the page body never scrolls
   horizontally. More specific than FIN_CSS's .specfin-svg { min-width:600px },
   so it wins without redeclaring it. */
@media (max-width:720px) { .specfin-util .specfin-svg { min-width:760px; } }

/* \u2026but the ratio argument must not REQUIRE that scroll. Below 720px an HTML
   row list renders above the scroller, driven by the same is-after class: the
   whole of MOVE 1's counting survives without a gesture, and the cadence \u2014 which
   is a rhythm and genuinely needs the axis \u2014 is what the reader scrubs for. */
.specfin-util .specfin-util-rows { display:none; }
.specfin-util .specfin-util-hint { display:none; }
@media (max-width:720px) {
  .specfin-util .specfin-util-rows { display:flex; flex-direction:column; gap:8px;
    padding:13px 14px; background:var(--bg); border:1px solid var(--line); border-radius:var(--rad-sm); }
  .specfin-util .specfin-util-hint { display:block; margin:0; text-align:right;
    font-family:var(--font-mono); font-size:9px; letter-spacing:.1em;
    text-transform:uppercase; color:var(--text-mute); }
}
.specfin-util .ur { display:grid; grid-template-columns:84px minmax(0,1fr) 58px;
  gap:3px 8px; align-items:center;
  font-family:var(--font-mono); font-size:9.5px; color:var(--text-mute); }
.specfin-util .ur-label { color:var(--text-dim); overflow:hidden; text-overflow:ellipsis; }
.specfin-util .ur.is-removed .ur-label { text-decoration:line-through; color:var(--text-mute); }
.specfin-util .ur-track { position:relative; height:9px; border-radius:2px;
  background:var(--bg-2); border:1px solid var(--line); overflow:hidden; }
.specfin-util .ur.is-removed .ur-track { background:none; border-style:dashed; }
.specfin-util .ur-fill { position:absolute; top:0; bottom:0; left:0;
  background:var(--specfin-lit); transition:width .5s ease, opacity .5s ease; }
.specfin-util .ur.is-removed .ur-fill { opacity:0; }
.specfin-util .ur-n { text-align:right; }
.specfin-util .ur-gloss { grid-column:2 / -1; font-size:8.5px; color:var(--text-mute); }
.specfin-util .ur-rule { height:1px; background:var(--line); }
.specfin-util .ur-total { display:flex; flex-wrap:wrap; gap:4px 10px;
  font-family:var(--font-mono); font-size:9.5px; color:var(--text-dim); }
.specfin-util .ur-note { margin:0; font-family:var(--font-mono); font-size:8.5px;
  line-height:1.6; color:var(--text-mute); }

@media (prefers-reduced-motion: reduce) { .specfin-util * { transition:none !important; } }
`;
  const STEPS_CSS = `
/* --bg-1 rather than --bg: this block sits directly on the page, and every other
   block at that level (.vizchart, .spec-metric) is a --bg-1 panel. On --bg it was
   the same colour as the page behind it and had only a 1px --line to say it was
   a panel at all. */
.specfin-steps { display:flex; flex-direction:column; gap:14px;
  max-width:82ch; padding:17px 19px;
  background:var(--bg-1); border:1px solid var(--line); border-radius:var(--rad-sm); }
.specfin-steps-head { margin:0; font-family:var(--font-mono); font-size:9.5px;
  letter-spacing:.11em; text-transform:uppercase; color:var(--text-mute); }
.specfin-steps-list { margin:0; padding-left:1.55em; display:flex; flex-direction:column; gap:13px;
  font-size:13.5px; line-height:1.7; color:var(--text-dim); }
.specfin-steps-list li::marker { font-family:var(--font-mono); font-size:11px; color:var(--text-mute); }
.specfin-steps-list b { font-weight:600; color:var(--text); }
.specfin-steps-close { margin:0; padding-top:2px;
  font-family:var(--font-mono); font-size:10.5px; line-height:1.75;
  font-style:italic; color:var(--text-mute); }
@media (max-width:640px) { .specfin-steps { padding:15px 15px; } }
`;
  const BPD = 4, DAYS = 14, SPAN = BPD * DAYS;
  const BLOCK_H = 6;
  const WEEKENDS = [6, 7, 13, 14];
  const WEEKDAYS = [1, 2, 3, 4, 5, 8, 9, 10, 11, 12];
  const RELEASE = 11;
  const SCHED_B = [1, 2];
  const DOW = ["M", "T", "W", "T", "F", "S", "S", "M", "T", "W", "T", "F", "S", "S"];
  const at = (d, b) => (d - 1) * BPD + b;
  const LANES = [
    {
      id: "deva",
      env: "dev \xB7 team a",
      tier: "dev",
      gloss: "most weekdays",
      local: true,
      use: [[1, 1], [2, 1], [4, 1], [5, 2], [8, 1], [9, 1], [9, 2], [10, 2], [12, 1], [1, 2]]
    },
    // 10
    {
      id: "devb",
      env: "dev \xB7 team b",
      tier: "dev",
      gloss: "nights only",
      local: true,
      note: "offshore \u2014 their morning is our 03:00",
      use: [[2, 0], [3, 0], [5, 0], [5, 1], [9, 0], [11, 0]]
    },
    //  6
    {
      id: "devc",
      env: "dev \xB7 team c",
      tier: "dev",
      gloss: "3 days in 14",
      local: true,
      use: [[1, 2], [8, 2], [12, 2]]
    },
    //  3
    {
      id: "test",
      env: "test / sit",
      tier: "test",
      gloss: "twice a sprint",
      use: [[4, 2], [10, 1], [10, 2], [11, 1], [11, 2]],
      /* work that migrates here when hosted dev goes away */
      gains: [[2, 2], [5, 1], [9, 2]]
    },
    //  5 (+3)
    {
      id: "stage",
      env: "staging / uat",
      tier: "stage",
      gloss: "at the cut",
      use: [[10, 2], [11, 2], [11, 3], [14, 0]]
    },
    //  4
    {
      id: "perf",
      env: "performance",
      tier: "perf",
      gloss: "one soak run",
      use: [[9, 3], [10, 0]]
    }
    //  2
  ];
  const MARKS = {
    "14-0": { n: 1, lane: "stage", note: "Sun 02:00 \u2014 an incident. No schedule covers this." },
    "11-3": { n: 2, lane: "stage", note: "Thu 21:00 \u2014 the release slipped past the window." }
  };
  const UNCOUNTED = [
    "\u2026AND IN A REGULATED ESTATE: DR-TEST, AND A PREVIEW ENVIRONMENT PER TEAM",
    "NAMED, NOT DRAWN \u2014 COUNTING THEM WOULD ONLY WIDEN THE GAP, NEVER CLOSE IT"
  ];
  const cols = (l, on) => l.use.concat(on && l.gains ? l.gains : []).map((p) => at(p[0], p[1]));
  const live = (on) => LANES.filter((l) => !(on && l.local));
  const inSched = (c) => WEEKDAYS.indexOf(Math.floor(c / BPD) + 1) >= 0 && SCHED_B.indexOf(c % BPD) >= 0;
  const billed = (on) => live(on).length * SPAN;
  const used = (on) => live(on).reduce((n, l) => n + cols(l, on).length, 0);
  const escaped = (on) => live(on).reduce((n, l) => n + cols(l, on).filter((c) => !inSched(c)).length, 0);
  const schedBlocks = (on) => live(on).length * WEEKDAYS.length * SCHED_B.length;
  const weekendBilled = (on) => live(on).length * WEEKENDS.length * BPD;
  const weekendUsed = (on) => live(on).reduce(
    (n, l) => n + cols(l, on).filter((c) => WEEKENDS.indexOf(Math.floor(c / BPD) + 1) >= 0).length,
    0
  );
  const hrs = (b) => b * BLOCK_H;
  const pct = (u, b) => Math.round(u / b * 100);
  const GONE_BLOCKS = billed(false) - billed(true);
  const GONE_HOURS = hrs(GONE_BLOCKS);
  const GONE_LANES = LANES.filter((l) => l.local).length;
  const W = 760, H = 300;
  const GX = 96, GW = 580;
  const DAYW = GW / DAYS;
  const BLK = 9.6, SQ = 8;
  const dayX = (d) => GX + (d - 1) * DAYW;
  const colX = (c) => GX + Math.floor(c / BPD) * DAYW + c % BPD * BLK;
  const dayMid = (d) => dayX(d) + 18.4;
  const BAND = 14, IDLE_H = 8, IDLE_DY = 3;
  const LANE_TOP = { deva: 62, devb: 82, devc: 102, test: 132, stage: 164, perf: 196 };
  const GRID_TOP = 58, GRID_BOT = 212;
  const DEV_TOP = 62, DEV_BOT = 116;
  const COLUMNS = [];
  for (let c = 0; c < SPAN; c++) COLUMNS.push(c);
  const DAYNUMS = [];
  for (let d = 1; d <= DAYS; d++) DAYNUMS.push(d);
  const LANE_VIEW = LANES.map((l) => {
    const lit = {};
    l.use.forEach((p) => {
      lit[at(p[0], p[1])] = 1;
    });
    return {
      lane: l,
      top: LANE_TOP[l.id],
      lit,
      gains: (l.gains || []).map((p) => at(p[0], p[1])),
      nUse: l.use.length,
      nGain: (l.gains || []).length
    };
  });
  const PIPS = Object.keys(MARKS).map((k) => {
    const m = MARKS[k];
    const parts = k.split("-");
    const c = at(parseInt(parts[0], 10), parseInt(parts[1], 10));
    return { n: m.n, note: m.note, x: colX(c) + SQ / 2, y: LANE_TOP[m.lane] - 5 };
  }).sort((a, b) => a.n - b.n);
  const fmt = (n) => String(n).replace(/\B(?=(\d{3})+(?!\d))/g, ",");
  const WORDS = { 96: "NINETY-SIX", 48: "FORTY-EIGHT" };
  const say = (n) => WORDS[n] || String(n);
  const reduced = () => typeof window.matchMedia === "function" && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const useCountTo = (target) => {
    const [v, setV] = React.useState(target);
    const shown = React.useRef(target);
    React.useEffect(() => {
      shown.current = v;
    });
    React.useEffect(() => {
      const from = shown.current;
      if (from === target || reduced() || typeof requestAnimationFrame !== "function") {
        setV(target);
        return void 0;
      }
      const t0 = Date.now(), dur = 620;
      let id = 0;
      const step = () => {
        const p = Math.min(1, (Date.now() - t0) / dur);
        const e = 1 - Math.pow(1 - p, 3);
        setV(Math.round(from + (target - from) * e));
        if (p < 1) id = requestAnimationFrame(step);
      };
      id = requestAnimationFrame(step);
      return () => cancelAnimationFrame(id);
    }, [target]);
    return v;
  };
  const LABEL_BEFORE = "A ledger of one sprint. Six billed non-production environments down the left \u2014 three development environments, one per team, then test and system integration, staging and user acceptance, and a production-sized performance tier \u2014 each drawn as fifty-six squares. One square is one six-hour block of that environment, billed: four blocks a day across fourteen calendar days. Three hundred and thirty-six squares are billed. Thirty are lit, meaning somebody used the environment in that block. The first development team is lit on eight weekdays, one or two blocks each. The second is offshore and is lit almost only in the block before six in the morning, five times. The third is lit three times in the whole sprint. Test is lit once mid-sprint and then four times across the release on days ten and eleven. Staging is lit twice at the cut, once at nine in the evening when the release slipped, and once at two on a Sunday morning during an incident. Performance runs once, overnight, across a day boundary. Ten dashed boxes mark what a generous schedule \u2014 six in the morning to six in the evening, weekdays only \u2014 would leave running: a hundred and twenty of the three hundred and thirty-six squares. Nine of the thirty lit blocks fall outside those boxes. Ninety-six weekend squares are billed and one is used. Two thousand and sixteen environment-hours billed, one hundred and eighty used: nine per cent. A dashed band below the rows names two more tiers a regulated estate also runs \u2014 a DR-test tier and a preview environment per team \u2014 named but not drawn and not counted, so including them would widen the gap this picture shows rather than close it.";
  const LABEL_AFTER = "Local dev. The three development rows have gone to a ghost inside a dashed outline reading not provisioned. With development happening on the developer's own machine there is no hosted dev tier to bill, so those hundred and sixty-eight squares \u2014 one thousand and eight environment-hours, exactly half the estate drawn here \u2014 are deleted rather than scheduled. Three new squares light on the test row, on days two, five and nine, where the integration checks that used to run in hosted dev now go instead. Three environments remain: a hundred and sixty-eight squares billed, fourteen lit, one thousand and eight environment-hours against eighty-four used \u2014 eight per cent. The share used did not improve; it fell by a point, because the tier that left was the best-used one. The billed hours halved. What share of the money went with them is not something this drawing can say: the tiers are not the same size.";
  const FRAME_SOURCE = "ILLUSTRATIVE LEDGER \u2014 two kinds of figure appear here and they are not the same kind of thing. The three hundred and thirty-six squares are arithmetic of the picture: six environments \xD7 four six-hour blocks \xD7 fourteen days, and an environment left running bills every one of them, weekends at the same rate as Tuesdays. Which squares are lit is Geoff's own figure \u2014 an estimate from regulated estates he has run; no published benchmark reports per-tier utilization at this resolution. A part-used block is drawn as a full one, so the lit area is generous and the waste figure is a floor. The savings a schedule actually returns are measured, published, and in the chart below.";
  const BADGE_TITLE = "The cadence drawn here is an estimate, not a measurement. The counts and percentages are arithmetic of the illustration, computed from it, not a measurement of any real estate.";
  const UtilizationVisual = () => {
    const [after, setAfter] = React.useState(false);
    const [touched, setTouched] = React.useState(false);
    const host = React.useRef(null);
    const shownH = useCountTo(hrs(billed(after)));
    const nLanes = live(after).length;
    const nBilled = billed(after);
    const nUsed = used(after);
    const nPct = pct(nUsed, nBilled);
    React.useEffect(() => {
      if (touched || !host.current) return void 0;
      if (reduced() || typeof IntersectionObserver !== "function") return void 0;
      let timer = 0;
      const io = new IntersectionObserver((entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            if (timer) return;
            timer = setTimeout(() => {
              io.disconnect();
              setAfter(true);
            }, 2400);
          } else if (timer) {
            clearTimeout(timer);
            timer = 0;
          }
        });
      }, { threshold: 0.3 });
      io.observe(host.current);
      return () => {
        io.disconnect();
        if (timer) clearTimeout(timer);
      };
    }, [touched]);
    const pick = (v) => {
      setTouched(true);
      setAfter(v);
    };
    return /* @__PURE__ */ React.createElement(
      VizFrame,
      {
        title: "One sprint, at the resolution the bill runs at",
        note: "Environments bill in one rhythm and are used in another, and the rhythms are not the same from one tier to the next. That is why the axis is a sprint and not a day: \u201Ctwice a sprint\u201D is not a shape an hours-of-one-day chart can draw.",
        source: FRAME_SOURCE
      },
      /* @__PURE__ */ React.createElement("style", null, UTIL_CSS),
      /* @__PURE__ */ React.createElement("div", { className: "specfin-util" + (after ? " is-after" : ""), ref: host }, /* @__PURE__ */ React.createElement("div", { className: "specfin-ctl" }, /* @__PURE__ */ React.createElement("div", { className: "specfin-seg", role: "group", "aria-label": "Hosted dev tier, kept and removed" }, /* @__PURE__ */ React.createElement("button", { type: "button", "aria-pressed": !after, onClick: () => pick(false) }, "Hosted dev \xB7 six environments"), /* @__PURE__ */ React.createElement("button", { type: "button", "aria-pressed": after, onClick: () => pick(true) }, "Local dev \xB7 three environments")), /* @__PURE__ */ React.createElement("span", { className: "specfin-hint" }, "Same sprint. Same work. Watch three rows leave \u2014 and watch the ratio not improve.")), /* @__PURE__ */ React.createElement("div", { className: "specfin-readout", "aria-live": "polite" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-main" }, /* @__PURE__ */ React.createElement("span", { className: "specfin-n" }, fmt(shownH)), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-unit" }, "environment\u2011hours billed")), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-eq" }, nLanes + " environments \xD7 " + DAYS + " days \xD7 24 h"), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-eq" }, fmt(hrs(nUsed)) + " used \xB7 " + nPct + " % of the sprint"), /* @__PURE__ */ React.createElement("span", { className: "specfin-delta" }, "\u2212", fmt(GONE_HOURS), " h", /* @__PURE__ */ React.createElement("span", null, GONE_LANES, " environments deleted")), /* @__PURE__ */ React.createElement("span", { className: "specfin-ro-badge" }, /* @__PURE__ */ React.createElement(PlaceholderBadge, { label: "Illustrative", title: BADGE_TITLE }))), /* @__PURE__ */ React.createElement("div", { className: "specfin-util-rows", "aria-hidden": "true" }, LANE_VIEW.map((v) => {
        const off = after && v.lane.local;
        const n = off ? 0 : v.nUse + (after ? v.nGain : 0);
        return /* @__PURE__ */ React.createElement("div", { key: v.lane.id, className: "ur" + (off ? " is-removed" : "") }, /* @__PURE__ */ React.createElement("span", { className: "ur-label" }, v.lane.env), /* @__PURE__ */ React.createElement("span", { className: "ur-track" }, /* @__PURE__ */ React.createElement("span", { className: "ur-fill", style: { width: n / SPAN * 100 + "%" } })), /* @__PURE__ */ React.createElement("span", { className: "ur-n" }, off ? "removed" : n + "/" + SPAN), /* @__PURE__ */ React.createElement("span", { className: "ur-gloss" }, off ? "not provisioned" : v.lane.gloss + " \xB7 " + pct(n, SPAN) + " %"));
      }), /* @__PURE__ */ React.createElement("div", { className: "ur-rule" }), /* @__PURE__ */ React.createElement("div", { className: "ur-total" }, /* @__PURE__ */ React.createElement("span", null, "all " + nLanes), /* @__PURE__ */ React.createElement("span", null, fmt(hrs(nUsed)) + " h used of " + fmt(hrs(nBilled)) + " h billed"), /* @__PURE__ */ React.createElement("span", null, nPct + " %")), /* @__PURE__ */ React.createElement("p", { className: "ur-note" }, escaped(after) + " of the " + nUsed + " lit blocks fall outside a 06:00\u201318:00 weekday schedule.")), /* @__PURE__ */ React.createElement("p", { className: "specfin-util-hint", "aria-hidden": "true" }, "Scroll the ledger sideways \u2192"), /* @__PURE__ */ React.createElement("div", { className: "specfin-scroll" }, /* @__PURE__ */ React.createElement(
        "svg",
        {
          className: "specfin-svg",
          viewBox: "0 0 " + W + " " + H,
          xmlns: "http://www.w3.org/2000/svg",
          role: "img",
          "aria-label": after ? LABEL_AFTER : LABEL_BEFORE
        },
        /* @__PURE__ */ React.createElement("text", { x: "10", y: "20", fontFamily: "JetBrains Mono", fontSize: "8.5", letterSpacing: "1.3", fill: "var(--text-mute)" }, "ONE SPRINT \xB7 " + DAYS + " CALENDAR DAYS \xB7 " + nLanes + " BILLED ENVIRONMENTS"),
        /* @__PURE__ */ React.createElement("text", { x: "750", y: "20", textAnchor: "end", fontFamily: "JetBrains Mono", fontSize: "8.5", letterSpacing: "1.3", fill: "var(--text-mute)" }, "BILLED EVERY BLOCK OF ALL FOURTEEN"),
        /* @__PURE__ */ React.createElement("text", { x: "750", y: "34", textAnchor: "end", fontFamily: "JetBrains Mono", fontSize: "7", letterSpacing: "1.1", fill: "var(--text-mute)" }, "DASHED BOXES \u2014 A GENEROUS SCHEDULE: 06:00\u201318:00, MON\u2013FRI"),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, WEEKDAYS.map((d) => /* @__PURE__ */ React.createElement(
          "rect",
          {
            key: "sch" + d,
            className: "usched",
            x: dayX(d) + 8.4,
            y: GRID_TOP,
            width: "19.6",
            height: GRID_BOT - GRID_TOP,
            rx: "2"
          }
        ))),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, DAYNUMS.map((d) => {
          const wk = WEEKENDS.indexOf(d) >= 0;
          return /* @__PURE__ */ React.createElement("g", { key: "d" + d }, /* @__PURE__ */ React.createElement(
            "text",
            {
              className: "udow",
              x: dayMid(d),
              y: "46",
              textAnchor: "middle",
              fontFamily: "JetBrains Mono",
              fontSize: "8",
              letterSpacing: "0.6",
              fill: wk ? "var(--text)" : "var(--text-mute)"
            },
            wk ? "\u27E8" + DOW[d - 1] + "\u27E9" : DOW[d - 1]
          ), /* @__PURE__ */ React.createElement(
            "text",
            {
              x: dayMid(d),
              y: "56",
              textAnchor: "middle",
              fontFamily: "JetBrains Mono",
              fontSize: "6.4",
              fill: "var(--text-mute)"
            },
            "D" + (d < 10 ? "0" + d : d)
          ));
        })),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + dayMid(RELEASE) + " " + GRID_TOP + " V" + GRID_BOT,
            fill: "none",
            stroke: "var(--line)",
            strokeWidth: "1",
            strokeDasharray: "3 3"
          }
        ), /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M" + dayMid(RELEASE) + " 117 l4 4 l-4 4 l-4 -4 Z",
            fill: "var(--text-mute)"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: dayMid(RELEASE) + 9,
            y: "124",
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: "1.1",
            fill: "var(--text-mute)"
          },
          "RELEASE \xB7 D" + RELEASE
        )),
        LANE_VIEW.map((v) => {
          const top = v.top;
          const nGut = after && v.lane.local ? "\u2014" : String(v.nUse + (after ? v.nGain : 0));
          return /* @__PURE__ */ React.createElement("g", { key: v.lane.id, className: v.lane.local ? "urow-local" : null }, /* @__PURE__ */ React.createElement(
            "text",
            {
              x: "88",
              y: top + 10,
              textAnchor: "end",
              fontFamily: "JetBrains Mono",
              fontSize: "7.5",
              fill: "var(--text-dim)",
              textDecoration: after && v.lane.local ? "line-through" : null
            },
            v.lane.env
          ), /* @__PURE__ */ React.createElement("rect", { className: "urail", x: GX, y: top + 6, width: GW, height: "2" }), /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, COLUMNS.map((c) => {
            const litc = !!v.lit[c];
            return /* @__PURE__ */ React.createElement(
              "rect",
              {
                key: c,
                className: "ublock" + (litc ? " is-used" : ""),
                x: colX(c),
                y: litc ? top : top + IDLE_DY,
                width: SQ,
                height: litc ? BAND : IDLE_H,
                rx: "1",
                style: { transitionDelay: Math.min(420, c * 5.6) + "ms" }
              }
            );
          }), v.gains.map((c, i) => /* @__PURE__ */ React.createElement(
            "rect",
            {
              key: "g" + c,
              className: "ublock is-used ugain",
              x: colX(c),
              y: top,
              width: SQ,
              height: BAND,
              rx: "1",
              style: { transitionDelay: 520 + i * 90 + "ms" }
            }
          ))), /* @__PURE__ */ React.createElement(
            "text",
            {
              x: "752",
              y: top + 7,
              textAnchor: "end",
              fontFamily: "JetBrains Mono",
              fontSize: "8",
              fill: "var(--text-mute)"
            },
            nGut + "/" + SPAN
          ), /* @__PURE__ */ React.createElement(
            "text",
            {
              x: "752",
              y: top + 16,
              textAnchor: "end",
              fontFamily: "JetBrains Mono",
              fontSize: "6.4",
              fill: "var(--text-mute)"
            },
            v.lane.gloss
          ));
        }),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, PIPS.map((p) => /* @__PURE__ */ React.createElement("g", { key: p.n }, /* @__PURE__ */ React.createElement(
          "circle",
          {
            cx: p.x,
            cy: p.y,
            r: "4",
            fill: "var(--bg-1)",
            stroke: "var(--specfin-lit)",
            strokeWidth: "1.1"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: p.x,
            y: p.y + 2.2,
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "6",
            fill: "var(--text)"
          },
          p.n
        )))),
        /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M94 " + DEV_TOP + " H90 V" + DEV_BOT + " H94",
            fill: "none",
            stroke: "var(--line)",
            strokeWidth: "1"
          }
        ),
        /* @__PURE__ */ React.createElement("g", { className: "ugone", pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: GX - 6,
            y: "56",
            width: GW + 12,
            height: "60",
            rx: "3",
            fill: "none",
            stroke: "var(--specfin-lit)",
            strokeWidth: "1",
            strokeDasharray: "4 4"
          }
        ), /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: GX - 6 + (GW + 12) / 2 - 198,
            y: "80",
            width: "396",
            height: "14",
            fill: "var(--bg-1)",
            opacity: ".93"
          }
        ), /* @__PURE__ */ React.createElement(
          "text",
          {
            x: GX - 6 + (GW + 12) / 2,
            y: "90",
            textAnchor: "middle",
            fontFamily: "JetBrains Mono",
            fontSize: "7.5",
            letterSpacing: "1",
            fill: "var(--specfin-lit)"
          },
          "NOT PROVISIONED \u2014 " + GONE_BLOCKS + " BLOCKS \xB7 " + fmt(GONE_HOURS) + " BILLED HOURS THAT NO LONGER EXIST"
        )),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement(
          "rect",
          {
            x: "10",
            y: "222",
            width: "740",
            height: "28",
            rx: "3",
            fill: "none",
            stroke: "var(--line)",
            strokeWidth: "1",
            strokeDasharray: "4 4"
          }
        ), UNCOUNTED.map((t, i) => /* @__PURE__ */ React.createElement(
          "text",
          {
            key: i,
            x: "22",
            y: 236 + i * 10,
            fontFamily: "JetBrains Mono",
            fontSize: "7",
            letterSpacing: "1",
            fill: "var(--text-mute)"
          },
          t
        ))),
        /* @__PURE__ */ React.createElement("text", { x: "10", y: "264", fontFamily: "JetBrains Mono", fontSize: "8", letterSpacing: "1.1", fill: "var(--text-mute)" }, say(weekendBilled(after)) + " WEEKEND BLOCKS BILLED. " + (weekendUsed(after) === 1 ? "ONE IS IN USE \u2014 AT 02:00, BY AN INCIDENT." : weekendUsed(after) + " ARE IN USE.")),
        /* @__PURE__ */ React.createElement("line", { x1: "10", y1: "272", x2: "750", y2: "272", stroke: "var(--line-soft)", strokeWidth: "1" }),
        /* @__PURE__ */ React.createElement("text", { x: "10", y: "284", fontFamily: "JetBrains Mono", fontSize: "8", letterSpacing: "1.2", fill: "var(--text-mute)" }, "ONE SQUARE = ONE SIX-HOUR BLOCK OF ONE ENVIRONMENT, BILLED \xB7 " + BPD + " A DAY \xB7 " + DAYS + " DAYS \xB7 " + SPAN + " A ROW"),
        /* @__PURE__ */ React.createElement("g", { pointerEvents: "none" }, /* @__PURE__ */ React.createElement("rect", { className: "ublock is-used", x: "10", y: "285", width: "5", height: "10", rx: "1" }), /* @__PURE__ */ React.createElement("text", { x: "20", y: "293", fontFamily: "JetBrains Mono", fontSize: "7", fill: "var(--text-mute)" }, "USED"), /* @__PURE__ */ React.createElement("rect", { className: "ublock", x: "54", y: "288", width: "5", height: "5", rx: "1" }), /* @__PURE__ */ React.createElement("text", { x: "64", y: "293", fontFamily: "JetBrains Mono", fontSize: "7", fill: "var(--text-mute)" }, "BILLED, IDLE"), /* @__PURE__ */ React.createElement("rect", { x: "130", y: "285", width: "14", height: "10", rx: "1", className: "usched" }), /* @__PURE__ */ React.createElement("text", { x: "150", y: "293", fontFamily: "JetBrains Mono", fontSize: "7", fill: "var(--text-mute)" }, "INSIDE THE SCHEDULE"), /* @__PURE__ */ React.createElement("text", { x: "240", y: "293", fontFamily: "JetBrains Mono", fontSize: "7", fill: "var(--text-mute)" }, "A PART-USED BLOCK IS DRAWN FULL, SO THE LIT AREA IS GENEROUS"))
      )), /* @__PURE__ */ React.createElement("ul", { className: "specfin-marks" }, PIPS.map((p) => /* @__PURE__ */ React.createElement("li", { key: p.n }, /* @__PURE__ */ React.createElement("span", { className: "specfin-mark-n", "aria-hidden": "true" }, p.n), /* @__PURE__ */ React.createElement("span", null, p.note)))), /* @__PURE__ */ React.createElement("p", { className: "specfin-cap" }, after ? "Utilization barely moves: nine per cent becomes eight, because the tier that left was the best-used one. The billed hours halve. That gap is the difference between optimising an environment and not having one, and dropping a tier is the only move in this section that changes what is billed rather than when." : "Nothing above is misconfigured. Every one of these environments does what it was built to do on the days somebody needs it, and bills identically on the days nobody does. A part-used block is drawn as a full one, so this picture understates the empty half rather than flattering it \u2014 a twenty-minute deploy check lights a full six hours here."), /* @__PURE__ */ React.createElement("p", { className: "specfin-foot" }, "The hours above are ", /* @__PURE__ */ React.createElement("em", null, "arithmetic of the illustration"), ", not a measurement: the billed field is a property of the drawing \u2014 ", live(false).length, " environments \xD7 ", BPD, " six\u2011hour blocks \xD7 ", DAYS, " days \u2014 and which squares are lit is ", /* @__PURE__ */ React.createElement("em", null, "Geoff's own figure"), ", an estimate from regulated estates he has run, because no published benchmark reports per\u2011tier utilization at this resolution. The only sourced savings figures in this section are in the benchmark chart below, and they are measured and published."), /* @__PURE__ */ React.createElement("p", { className: "specfin-foot" }, "Three of the six rows above are hosted development environments \u2014 one per team. When development happens on the machine in front of the developer, a change can go straight to test for integration and those three rows stop being provisioned at all: ", /* @__PURE__ */ React.createElement("em", null, "half the billed hours drawn here"), ", deleted rather than scheduled. Hours, not dollars \u2014 one of the three tiers left standing is a production\u2011sized performance environment, and this drawing knows nothing about what any of them costs.", /* @__PURE__ */ React.createElement("em", null, " How"), " local development is made to work \u2014 the parity problem, the data problem, the minutes it is allowed to cost \u2014 is the subject of", " ", /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: "specfin-link",
          onClick: () => window.GO && window.GO("specializations", { specId: "devx" })
        },
        "DevX"
      ), ", and belongs there. What belongs here is the consequence: it is the only change on this page that removes an environment instead of optimising one."))
    );
  };
  const ScheduleReductio = () => /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("style", null, STEPS_CSS), /* @__PURE__ */ React.createElement("section", { className: "specfin-steps", "aria-label": "The schedule, followed through" }, /* @__PURE__ */ React.createElement("p", { className: "specfin-steps-head" }, "THE SCHEDULE, FOLLOWED THROUGH \u2014 REASONING, NOT MEASUREMENT"), /* @__PURE__ */ React.createElement("ol", { className: "specfin-steps-list" }, /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("b", null, "A schedule covers the window it was written for."), " The dashed boxes above are a generous one \u2014 twelve hours, weekdays only. Applied to the sprint drawn here it removes about two thirds of the squares, and that is a real win; the published figures for it are in the savings chart below, and they are the sourced ones."), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("b", null, "Real work escapes the window."), " Nine of the thirty lit blocks above sit on bare ground: a team whose morning is our small hours, a soak test that runs overnight, a release that slipped past six, an incident at two on a Sunday. In Geoff's experience none of those is unusual \u2014 and none of them is inside the window somebody already wrote. Widen the window and you have moved it, not closed it; the incident at two still is not in there."), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("b", null, "So you build a way to bring an environment up outside the schedule."), " You have to. Otherwise those nine blocks are nine tickets, or nine people with a console open at the wrong hour. The boot path is not an enhancement; it is the cost of having a schedule at all."), /* @__PURE__ */ React.createElement("li", null, /* @__PURE__ */ React.createElement("b", null, "Once the boot path exists, the schedule is the redundant half."), " Delete it. Leave everything off, and let the boot path be the whole mechanism \u2014 a bounded request that expires on its own, so shutdown is not a thing anyone has to remember.")), /* @__PURE__ */ React.createElement("p", { className: "specfin-steps-close" }, "No figure is claimed in steps three and four. The conclusion is reached by argument, and a different dataset cannot move it.")));
  window.UtilizationVisual = UtilizationVisual;
  window.SprintLedger = UtilizationVisual;
  window.ScheduleReductio = ScheduleReductio;
})();
